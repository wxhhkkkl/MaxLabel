# P4「四件套」工单（由普查结果自动生成）

- 数据来源：`tools/loop/cov-all.md`（`node tools/parity/survey-evidence-coverage.cjs --missing-all --out …`）
- 生成时间：2026/9/21 16:25:39
- 条目总数：**605**（全部未凑齐四件套）

## 一、缺口分布（按"缺哪几件"）

| 缺的件 | 条目数 |
| --- | --- |
| 真机证据+复刻版证据+并排图 | 221 |
| 真机证据+并排图 | 210 |
| 真机证据+复刻版证据+并排图+自动断言 | 91 |
| 复刻版证据+并排图 | 35 |
| 真机证据+并排图+自动断言 | 27 |
| 并排图 | 15 |
| 真机证据+复刻版证据+自动断言 | 3 |
| 复刻版证据+并排图+自动断言 | 2 |
| 真机证据+复刻版证据 | 1 |

## 二、最便宜的胜利：只缺**一件**的条目

| 只缺 | 条目数 | 谁来做 |
| --- | --- | --- |
| 并排图 | **15** | 验收方：`New-ParityShot.ps1` 出复刻图并拼并排图，再把文件名写进对应矩阵行 |
| 真机证据 | **0** | 需上真机（按界面聚类后一轮拍一批） |
| 复刻版证据 | 0 | 验收方：`Capture-CloneShot.cjs`（choose/custom/editor 三场景） |
| 自动断言 | 0 | 循环：补 `ui-vNNN.cjs` 断言（照现有加严先例，别只断"存在"） |

## 三、只缺「并排图」的条目（可直接补）

| id | 标题 | 出处 |
| --- | --- | --- |
| A-42 | 文件菜单 → 模板属性设置 | menu_file.html |
| A-121 | 主工具栏 → 添加或删除按钮 | toolbar_mainbar.html |
| A-165 | 状态栏 → 打印机 | toolbar_status.html |
| A-174 | 主界面 → 界面元素 1 至 12 | interface_interface.html |
| A-206 | 编辑并打印第一个标签 → 第1、2步 新建模板与选择标签 | getstart_firstprint.html |
| A-271 | 起始页右区 → 广告位、最新文章与下载块 | label_main_page.html |
| B-28 | 修改对象属性 → 双击对象或选择属性菜单命令打开属性对话 | label_object_modify.html |
| B-70 | 对象属性 → 条码 → 条码特殊选项分组（Code 39 | label_object_page_barcode.html |
| C-76 | 新建标签模板 → 文件菜单"新建"或工具栏新建弹出"选择 | label_create_new.html |
| C-84 | 选择系统预定义的标签格式 → 选择/自定义/取消/帮助按 | label_select_new.html |
| D-22 | 打印机属性端口页 → 端口选择与USB自动识别 | print_printer_cfg_port.html |
| D-23 | 打印机属性端口页 → 端口类型枚举与默认值 | print_printer_cfg_port.html |
| D-28 | 打印机属性端口页 → 打印机端口(COM) | print_printer_cfg_port.html |
| D-34 | 安装打印机 → 安装或移除（卸载）签赋LabelShop | print_printer_labelshop.html |
| D-35 | 安装打印机 → 可安装的打印机列表 | print_printer_labelshop.html |

## 四、只缺「真机证据」的条目（需要真机）

| id | 标题 | 出处 |
| --- | --- | --- |


## 五、只缺「复刻版证据」的条目

| id | 标题 | 出处 |
| --- | --- | --- |


## 六、只缺「自动断言」的条目

| id | 标题 | 出处 |
| --- | --- | --- |


---

## 使用说明

1. **并排图那条最容易推进**：先用 `New-ParityShot.ps1 -Round <当前轮次>` 把已有真机图的界面出成并排图，
   再让循环把文件名写进 `parity/matrix.md` 对应行的证据列（**只放文件不算，必须被引用**——普查就是按引用判定的）。
2. 真机证据那条要**按界面聚类**批量拍，避免一轮只拍一个控件。
3. 本工单是"派生文件"，会随普查重跑而更新；**不要**把结论只写在这里，最终的证据引用仍以 `parity/matrix.md` 为准。
