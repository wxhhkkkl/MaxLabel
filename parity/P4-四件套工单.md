# P4「四件套」工单（由普查结果自动生成）

- 数据来源：`tools/loop/cov-all.md`（`node tools/parity/survey-evidence-coverage.cjs --missing-all --out …`）
- 生成时间：2026/9/21 18:22:31
- 条目总数：**40**（全部未凑齐四件套）

## 一、缺口分布（按"缺哪几件"）

| 缺的件 | 条目数 |
| --- | --- |
| 真机证据+复刻版证据+并排图 | 31 |
| 并排图 | 5 |
| 复刻版证据+并排图 | 3 |
| 真机证据+并排图 | 1 |

## 二、最便宜的胜利：只缺**一件**的条目

| 只缺 | 条目数 | 谁来做 |
| --- | --- | --- |
| 并排图 | **5** | 验收方：`New-ParityShot.ps1` 出复刻图并拼并排图，再把文件名写进对应矩阵行 |
| 真机证据 | **0** | 需上真机（按界面聚类后一轮拍一批） |
| 复刻版证据 | 0 | 验收方：`Capture-CloneShot.cjs`（choose/custom/editor 三场景） |
| 自动断言 | 0 | 循环：补 `ui-vNNN.cjs` 断言（照现有加严先例，别只断"存在"） |

## 三、只缺「并排图」的条目（可直接补）

| id | 标题 | 出处 |
| --- | --- | --- |
| A-34 | 文件菜单 → 新建条幅飘带 | menu_file.html |
| A-35 | 文件菜单 → 打开 | menu_file.html |
| A-37 | 文件菜单 → 保存 | menu_file.html |
| A-38 | 文件菜单 → 另存为 | menu_file.html |
| A-39 | 文件菜单 → 分享 | menu_file.html |

## 四、只缺「真机证据」的条目（需要真机）

| id | 标题 | 出处 |
| --- | --- | --- |


## 五、只缺「复刻版证据」的条目

| id | 标题 | 出处 |
| --- | --- | --- |


## 六、只缺「自动断言」的条目

| id | 标题 | 出处 |
| --- | --- | --- |


---

## 使用说明

1. **并排图那条最容易推进**：先用 `New-ParityShot.ps1 -Round <当前轮次>` 把已有真机图的界面出成并排图，
   再让循环把文件名写进 `parity/matrix.md` 对应行的证据列（**只放文件不算，必须被引用**——普查就是按引用判定的）。
2. 真机证据那条要**按界面聚类**批量拍，避免一轮只拍一个控件。
3. 本工单是"派生文件"，会随普查重跑而更新；**不要**把结论只写在这里，最终的证据引用仍以 `parity/matrix.md` 为准。
