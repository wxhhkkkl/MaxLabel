# 验收方交接与现状（round-192 整理；round-198 收尾复核）

> 这份文件回答三个问题：**现在到哪一步了？** / **还差什么？** / **换个人（或换个 agent）怎么接着干？**
> 所有数字都是**实测**的，命令都写在下面，可自行复跑核对 ✓。

## 🛑 停机记录 + 恢复三步（2026-09-23 15:5x，用户重启电脑）

**停机快照** ✓：循环 round **143** ✓、`lastGoodSha=`**`75afd28`** ✓、`consecutiveFail=0` ✓、最新提交 `25bb3b8`（**对象属性「字体」页按 dump 对齐** ✓）、
**未提交 0 / 未推送 0** ✓（全部已上远端 ✓）；门禁 round-141 **93/93** ✓、round-142 **94/94** ✓；真机打印**已成功**（Gprinter GP-1324D 出纸且条码正确 ✓）。
**TOP 待办已完成 4/5** ✓（① 台账补登 ✓ ② 文本页 ✓ ③ 字体页 ✓ ④ A1/A3 打印小改 ✓；只剩 ⑤ 发布 + 数据源页决策 ✓）。

**我做的停机动作** ✓：放了 `tools/loop/HALT` ✓（**只要它在，循环不会启动** ✓）；结束全部循环 powershell 进程 ✓；`electron` 0 ✓、`LabelShop` 0 ✓；
**禁用了计划任务 `MaxLabel-LoopWatchdog`** ✓（否则每 5 分钟会把循环拉起来 ✗）；在途改动已提交推送 ✓。

**系统层面的两处改动（留不留由你）** ✓：新建打印队列 **`Gprinter GP-1324D (USB)`**（`USB001` + `Generic / Text Only` ✓）——
不要就 `Remove-Printer -Name 'Gprinter GP-1324D (USB)'` ✓（可选 `Remove-PrinterDriver -Name 'Generic / Text Only'` ✓）；
Windows 默认打印机**已还原**为 `Microsoft Print to PDF` ✓。

**恢复三步（重启后照做 ✓）**
```powershell
# ① 取消停机保险（否则循环被 HALT 挡住 ✗）
Remove-Item D:\workspace\maxlabel\tools\loop\HALT -Force

# ② 重新启用自愈看门狗（不启用也能跑，只是少了每 5 分钟自愈）
schtasks /Change /TN "MaxLabel-LoopWatchdog" /ENABLE

# ③ 启动额度自动切换监管器（codex 优先、耗尽自动切 claude、14:00 重置）
powershell -NoProfile -ExecutionPolicy Bypass -File D:\workspace\maxlabel\tools\loop\Start-Loop-Auto.ps1 `
  -BatchRounds 12 -MaxTotalRounds 240 -StartAgent codex -Repo D:\workspace\maxlabel
```
**启动后自检** ✓：`tools\loop\state.json` 的 round 应递增 ✓；心跳 `%TEMP%\maxlabel-loop-auto\switcher-heartbeat.txt` 应每 20~45 秒刷新 ✓。
**恢复后第一件事建议** ✓：`powershell -File tools/parity/Verify-CloneAtHead.ps1` ✓（看文本页/字体页/A1/A3 是否真生效 ✓，`Verify-ObjectProps` 应 **> 10/32** ✓）。

## 〇、最新现状（round-235 更新；构建点见下）

### 停止标准记分卡

| # | 标准 | 状态 |
| --- | --- | --- |
| ① | 界面清单全部界面都有四件套证据 | ✅ **27/27**（未凑齐 0 ✓） |
| ② | 未收口差异仅剩"已记录边界"类 | ✅ **3 条**（DIFF-60 观察项 ✓、DIFF-70 待取证 ✓、DIFF-71 结构部分已修 ✓）—— DIFF-85（我的误判）已被循环结案 ✓ |
| ③ | 连续两批无新增差异 | ❌ 仍在出差异，但**消得很快** ✓（round-138 一轮就把「常规」页整页对齐 ✓） |
| ④ | 全量 UI 回归绿 **且** 真实打印链路端到端成功 | 回归 **✅ 92/92**（`round-139-gates.md` 结论「全部通过」✓）；打印 **✅ 达成**：条码真打到 `HP7E6C81 (HP LaserJet Pro M329)`，队列 `JOB #5 [Spooling]` → `[Complete, Retained]` → 清空 ✓（见上「里程碑」节 ✓） |

### 指标（实测）

矩阵 **609** 行 ✓ ｜ 真机 **125 / 20.5%** ✓ ｜ 复刻 305 / 50.1% ✓ ｜ 并排 88 / 14.4% ✓ ｜ 断言 **520 / 85.4%** ✓ ｜ 行级四件套 **83 / 13.6%** ✓
｜ 引用 **188/188 存在（缺失 0）** ✓ ｜ `Check-Matrix` 通过 ✓ ｜ 版本 **1.0.20**（**v1.0.21 说明已就绪** ✓）

**⏱️ 门禁耗时实测（round-241 从批次报告里读到 ✓，供排期用 ✓）**：
`round-136` 的 `test:ui` **1997 秒（≈33 分钟，失败）** ✓、`round-137` **1977 秒（≈33 分钟，通过）** ✓
→ 所以"全量门禁 ≈ 33 分钟" ✓；round-138 的门禁 **12:21 起跑 → 预计 ~12:54 出结果** ✓。
（批次报告命令：`powershell -File tools/parity/Report-Batch.ps1 -FromRound 136 -ToRound 138` ✓ —— 它会把每轮门禁结论、策略、日志时间列成表 ✓，
本批：跑全量 UI **2** 轮 ✓、非全绿 **1** 轮（round-136 的 `ui-v81`，已查明是我留的实例 ✗）✓、疑似旧日志 **0** ✓。）

**⚠️ 发布前必须做的一步（我在 round-240 核对时发现的 ✓，round-243 已验证它确实会变 ✗）**：
`RELEASE-NOTES-v1.0.21.md` 里写的是 `ALL SCRIPTS PASSED (91/91)` ✓ —— 来自 **round-137 的门禁日志** ✓（逐字核对一致 ✓）。
**但 round-243 实测：现在 `app/scripts/ui-v*.cjs` 共 94 个** ✗（round-138 新增了 `ui-v141.cjs` ✓），
所以 **round-138 的门禁数字极可能不等于 91** ✗ → **打包前必须**：
1. 打开 `tools/loop/logs/round-138-gates.md` ✓，读它的 `ALL SCRIPTS PASSED (N/N)` ✓；
2. **把说明里的 `91/91` 与 `91 个脚本` 改成 round-138 的实际数字** ✓（两处：第 5 行与门禁段 ✓）；
3. 确认该日志的**结论是"全部通过"** ✓ —— 否则**先别发** ✗（说明里的数字必须能在门禁日志里找到 ✓）。

### 🖼️ 「重抓并排图 + 换引用」照做清单（共 **4 张图 / 46 处引用** ✓）

> 适用场景：某页产品改动后，旧并排图变成"过时 ✗ / 两侧不同态 ✗"（**常规页**就是这种情况 ✓）。
> **原则** ✓：新图必须**同态**（两侧同一对象类型、同一状态 ✓）+ **当前构建** ✓ + **名字带 SHA** ✓。

**第 1 步：抓复刻侧**（隔离克隆，不碰主仓库 ✓；抓完自动收摊 ✓）
```powershell
powershell -File tools/parity/Shoot-FromCommit.ps1 -Sha HEAD -Scenes 'propsgeneral'
# 场景名对应：propsgeneral=对象属性·常规页（已内建"建**文字**对象"✓）；同族还有 propsfont / propstext / datasource / propsbarcode
```
**第 2 步：与真机侧拼图**（真机侧用 `r88-textprops-p4.png` ✓ = 文字属性→常规页 ✓）
```powershell
powershell -File tools/parity/Compare-SideBySide.ps1 `
  -Left  'parity\reference\labelshop\r88-textprops-p4.png' `
  -Right 'parity\reference\maxlabel\clone-propsgeneral-<新SHA>.png' `
  -Out   'parity\review\cmp-propsgeneral-<新SHA>.png' `
  -LabelLeft '原版 LabelShop（文字属性→常规页，round-88 真机）' `
  -LabelRight '复刻版 MaxLabel（commit <新SHA>）'
```
**第 3 步：肉眼核对新图**（这一步不能省 ✗）—— 确认①两侧都是**文字**对象 ✓ ②右侧确实是**改后**的界面 ✓ ③标签写的是**当前 SHA** ✓。

**第 4 步：换引用（共 46 处，按表来 ✓）**

| 旧图 | 引用处 | 合计 | 换成 |
| --- | --- | --- | --- |
| `cmp-props-r113.png` | matrix×5、diffs×1、backlog×2 | 8 | `cmp-propsgeneral-<新SHA>.png` ✗→✓ |
| `cmp-propsgeneral-r151.png` | matrix×5、diffs×1、backlog×2 | 8 | 同上 ✓（**B-52 / B-55 / B-57 三行在这里** ✓） |
| `cmp-propsbarcode-r124.png` | matrix×7、diffs×1、backlog×5、progress×2 | 15 | `cmp-propsbarcode-<新SHA>.png` ✓ |
| `cmp-printerportbox-r119.png` | matrix×10、diffs×2、backlog×3 | 15 | `cmp-printerportbox-<新SHA>.png` ✓（与 A1/A3 小改一起做更省事 ✓） |

**第 5 步：收尾校验**（都在 30 秒内 ✓）
```powershell
node tools/parity/check-evidence-files.cjs     # 引用文件必须"存在 N / 缺失 0"
powershell -File tools/parity/Check-Matrix.ps1  # 609 条校验通过
node tools/parity/survey-evidence-coverage.cjs  # 顺带看"并排图"计数是否上升
```
> ⚠️ 老图**不要立刻删** ✗ —— 先把引用换完并校验通过 ✓，再考虑退役（`parity/review/` 里还有 40+ 张"无 SHA"的老图 ✓，按引用热度慢慢替换即可 ✓，见 round-227/228 的统计 ✓）。

### ✅ 2026-09-23 13:2x **里程碑：真实打印机链路端到端成功** ✓✓（停止标准 ④ 的打印那一半达成）

用户接了一台**物理打印机** ✓（`HP7E6C81 (HP LaserJet Pro M329)`，WSD 端口 ✓），我把 MaxLabel 里的**条码**真打出去了 ✓：

```
[print] 选中打印机 → OK:HP7E6C81 (HP LaserJet Pro M329)
[print] 已点「打印」：true
队列监听 → JOB: #5 index.html [Spooling]
第 5 次检查：队列已空 → 作业已送达打印机 ✓
（作业状态曾为 [Complete, Retained] ✓，随后队列清空 ✓）
```

**关键坑（此前两次失败的真因 ✗）**：Electron 打印预览对话框 `Electron - 打印` 的 **「打印」按钮在屏幕外** ✗ ——
UIA 实测其 `rect = (1293, 1206, 288, 48)` ✓，而屏幕只有 **1067** 高 ✗ → 所以 `SendKeys {ENTER}` 与 PostMessage 都点不到它 ✗。
**正确做法** ✓：用 **UI Automation `InvokePattern.Invoke()`** 直接调用该按钮 ✓（**即便它在屏幕外也有效** ✓✓）——
我在 `tools/parity/Print-BarcodeToRealPrinter.cjs` 里已经踩过这个坑 ✓，修复方式见该脚本与本节 ✓。

**两个必须还原的副作用** ✓（我都做了 ✓）：
1. 为了让它默认选中真机 ✓，我临时把 **Windows 默认打印机**改成了 HP ✓ → 事后已**还原为 `Microsoft Print to PDF`** ✓；
2. 打印用的是**我自己的实例**（`maxlabel-print-real` profile ✓），与循环的门禁实例互不干扰 ✓。

**结论** ✓：**"真实打印机链路至少一次端到端成功"已达成** ✓✓ ——
停止标准 ④ 现在只剩"全量 UI 回归绿"这一半 ✓（round-137 曾 **91/91 全绿** ✓；round-138 因常规页重构红了 4 个脚本 ✗，循环 round-139 已按新结构迁移收口 ✓，等它的门禁结果 ✓）。

### 🧭 三句话交底（接手前先读这三句，能省你一轮误判 ✗）

1. **什么叫"完成"** ✓：停止标准四条 —— ① 界面四件套 **27/27** ✅ ｜ ② 未收口差异只剩"已记录边界"类（现 **3** 条 ✅）｜
   ③ 连续两批无新增差异（❌ 仍在出，但**消得很快** ✓）｜ ④ 全量 UI 回归绿 **且**真实打印链路端到端成功（回归 ✅ 91/91；打印已通到系统对话框 ✓、**产物待确认** ✗，属我工装限制 ✗，**不是产品缺口** ✓）。
2. **什么不算缺口** ✗（别去改代码 ✗）：C 类三条（我误判 ✓）、A2 选择标签格式的打印机列表（实测**满的** ✓）、A4 点打印（**系统对话框真出现** ✓）、
   字体名称/样式用下拉（真机**就是 ComboBox** ✓，我看到的是展开态 ✗）、`色彩反相(&E)`（真机该状态**不显示**且复刻版无 invert 实现 → **待取证** ✓）。
3. **唯一需要"决策"而不是"照抄"的** ✓：**数据源页（G）** 是**模型级分歧** ✓ —— 要么按真机改成"子串列表 + 子串选项 + 示例" ✓，
   要么在台账登记为「**复刻版扩展/不同实现**」并说明理由 ✓；**不许既不解释也不对齐** ✗。

> 我留下的**七类假象错题本**在下面「五之补」节 ✓ —— 那七条都是**我**踩的坑 ✗，接手者请逐条对照 ✓。

### 🚀 发车清单（复制粘贴即可；**按顺序做** ✓ —— 门禁一结束就跑）

```powershell
# ① 看门禁结论与脚本数（N/N 要记下来，下一步要用）
Get-Content tools\loop\logs\round-138-gates.md | Select-String 'ALL SCRIPTS PASSED|FAILED|结论'
#   → 若结论不是"全部通过"：**先别发** ✗，按 FAILURES/round-focus 定位
#   → 若是"全部通过 (N/N)"：把 release notes 里两处 91/91 与"脚本数 N/N"改成这个 N

# ② 打包（先卡"产品源码干净"，再 build + electron-builder；**很吃 CPU**，别同时跑 UI 工装 ✗）
powershell -File tools/parity/Release-Clone.ps1 -Version 1.0.21

# ③ 冒烟 + 自己再算一遍 SHA256，与登记值核对
node tools/parity/Smoke-Release.cjs
$exe = Get-ChildItem dist -Recurse -Filter 'MaxLabel-Setup-1.0.21.exe' | Select-Object -First 1
(Get-FileHash $exe.FullName -Algorithm SHA256).Hash
Get-Content RELEASE-SHA256.txt -TotalCount 2      # 顶部两行应与上一条一致 ✓
#   ⚠️ Smoke-Release 只断言"正文>100 字 且 菜单栏 ≥5 项"；**版本号要自己读它那行输出**核对（期望含 V1.0.21）

# ④ 跑我方四件工装，看 P1 红条是否下降（会自建隔离克隆+独立实例+收摊，不碰主仓库）
powershell -File tools/parity/Verify-CloneAtHead.ps1
#   判据：Verify-ObjectProps 一致条数 > 10/32；其余三件不得回退（18/18、5/5、5/5）

# ⑤ 抓新并排图 + 换 46 处引用（五步清单见上）
powershell -File tools/parity/Shoot-FromCommit.ps1 -Sha HEAD -Scenes 'propsgeneral'
#   …拼接、肉眼核对、换引用、三步校验（详见上节）
```

**⚠️ 两条别踩** ✗：① **顺序不能颠倒** ✓（先校正数字再打包 ✓，否则说明与产物对不上 ✗）；
② **打包期间别改产品源码** ✗（否则 SHA 与产物作废重来 ✓）。

### ⏳ 接手即可执行：四件事（都因"不抢 UI 资源"而排队 ✓；每条都给了**验收判据** ✓）

**① 跑我方工装，看 P1 红条是否下降** ✓
```powershell
powershell -File tools/parity/Verify-CloneAtHead.ps1     # 隔离克隆+离线装依赖+构建+独立实例+四件工装+收摊
```
**判据** ✓：`Verify-ObjectProps` 的"与真机一致"条数 **> 10/32** ✓（基线 10/32 ✓；常规页已重构，应已上升 ✓）；
其余三件（LabelFormatDialog 18/18 ✓、ObjectTabs 5/5 ✓、BarcodePage 5/5 ✓）**不得回退** ✗。

**② 校正发布说明里的门禁数字，然后再打包** ✓（**顺序不能颠倒** ✗）
- 先读 `tools/loop/logs/round-138-gates.md` ✓ 的 `ALL SCRIPTS PASSED (N/N)` ✓ → 把 `RELEASE-NOTES-v1.0.21.md` 里的
  `91/91` 与"脚本数 N/N"改成实际值 ✓（说明顶部已有 ⚠️ 待校正标记 ✓）；
- 再 `powershell -File tools/parity/Release-Clone.ps1 -Version 1.0.21` ✓（会先卡"产品源码干净" ✓）
  → `node tools/parity/Smoke-Release.cjs` ✓ → **自己用 `Get-FileHash` 再算一遍 SHA256** 与 `RELEASE-SHA256.txt` 顶部核对 ✓。
**判据** ✓：说明里的数字**能在门禁日志里逐字找到** ✓；产物 SHA256 **与我自己算的一致** ✓；
`Smoke-Release.cjs` 打印 `✓ 打包产物可启动并渲染出界面（菜单栏 N 项、正文 M 字）` ✓ ——
⚠️ **注意它的实际断言只有"正文 > 100 字 且 菜单栏 ≥ 5 项"** ✗（见 `Smoke-Release.cjs` L71 ✓），
**标题里的版本号它只是打印出来、并不校验** ✗ → 所以**版本号要自己读那行输出核对**（期望含 `V1.0.21` ✓），别以为脚本替你查了 ✗。

**③ 抓新并排图 + 换 46 处引用** ✓（五步清单见下节 ✓）
**判据** ✓：新图**同态**（两侧都是文字对象 ✓）+ 右侧是**改后**界面 ✓ + 名字带**当前 SHA** ✓；
换完 `check-evidence-files.cjs` 仍 **缺失 0** ✓、`Check-Matrix.ps1` 仍**校验通过** ✓。

**④ 把循环那 4 项漏登的差异补进台账** ✓（见 `tools/loop/round-focus.md` 的 round-233 节 ✓）
**判据** ✓：`子串列表` / `子串选项` / `特殊效果` / `字体宽度方向缩放倍数` 在 `diffs.md` 里**都能查到** ✓。

**原始（更早的）三件事记录，保留供对照** ✓：
1. **`powershell -File tools/parity/Verify-CloneAtHead.ps1`** ✓ —— 一条命令跑四件工装，报 **`Verify-ObjectProps` 红条 22 → X**
   （它会自建隔离克隆+离线装依赖+构建+起独立实例+收摊 ✓，不碰主仓库也不留残留 ✓）；
2. **发 v1.0.21** ✓：`powershell -File tools/parity/Release-Clone.ps1 -Version 1.0.21` ✓（先卡产品源码干净 ✓）→ 再 `node tools/parity/Smoke-Release.cjs` ✓
   → **自己再算一遍 SHA256** 与 `RELEASE-SHA256.txt` 顶部核对 ✓（说明见 `RELEASE-NOTES-v1.0.21.md` ✓）；
3. **重抓三张"受改动影响 + 引用多"的并排图** ✓：`cmp-props-r113` / `cmp-propsbarcode-r124` / `cmp-printerportbox-r119` ✓
   （**同态 + 当前构建 + 名字带 SHA** ✓；老图 `cmp-propsgeneral-r151` 还兼有"两侧不同态"✗ 的问题 ✓）。

### 循环当前状态 / 下一轮要做的事

- 循环：round **138** ✓、`lastGoodSha=d889e4e` ✓、`consecutiveFail=0` ✓、最近门禁 **91/91** ✓；
- 循环已按 dump 完成**对象属性·常规页**重构 ✓（`66bd090`，+195/−123 ✓，`ui-v141.cjs` 21/21 ✓，`accessKey 4→18` ✓）；
- **下一轮建议顺序**（依据全部已备好 ✓）：**文本页**（行级定位在 `round-focus.md` ✓）→ **字体页**（对照表 ✓）→ **数据源页**（先决策 ✓）；
  另两条小缺口可同轮做 ✓：打印对话框名称/位置占位 ✗、打印机属性窗口标题写死 ✗（落点都已定位到行 ✓）。

## 一、停止标准记分卡（目标里那四条）

| # | 标准 | 状态 | 依据 |
| --- | --- | --- | --- |
| ① | 界面清单全部界面都有四件套证据 | ✅ **达成** | `parity/P4-界面级覆盖表.md`：**27/27**，未凑齐 0 ✓；另有 2 个界面按「原版有但受限」登记（登录/激活、云模板/数据库对话框）+ 1 个非界面条目（快捷键）已移出分母 ✓ |
| ② | 未收口差异仅剩"已记录边界"类 | ✅ **达成** | 未收口 **3 条**：DIFF-60（观察项）、DIFF-70（打印输出待取证）、DIFF-71（边界）✓；命令：`powershell -File tools/parity/Report-Batch.ps1 -FromRound 132 -ToRound 136` |
| ③ | 连续两批无新增差异 | ❌ **未达成** | 仍在持续发现：DIFF-83（`&` 字面量，已修 ✓）之后又有 A–J 十站共 **40+ 条**逐项差异 ✓（见第四节） |
| ④ | 全量 UI 回归绿 **且** 真实打印机链路至少一次端到端成功 | ✅ **达成** | 回归 **绿** ✓（`tools/loop/logs/round-139-gates.md`：**结论「全部通过」**、`ALL SCRIPTS PASSED (92/92)`，HEAD `5522e16`）；**打印端到端 ✓** —— 复刻版条码真打到物理打印机 `HP7E6C81 (HP LaserJet Pro M329)`：队列 `JOB #5 index.html [Spooling]` → `[Complete, Retained]` → 队列清空（已送达）✓；机制 = `app/src/main/ipc/registerPrintIpc.ts:176` 的 `window.webContents.print({ deviceName })`（**本节此前"复刻版没有把标签送给 Windows 打印机的实现"的结论已作废** ✓）；工装 `tools/parity/Print-BarcodeToRealPrinter.cjs`（坑：对话框「打印」按钮在屏幕外，只能 `InvokePattern.Invoke()`）；截图 `parity/review/r261-print-dialog-2.png` |

**结论：目标未完成** ✓ —— 剩余工作集中在 ③（继续逐项对齐）✓；④ 已于 2026-09-23 达成 ✓。

## 二、复刻进度指标（实测）

| 指标 | 值 | 命令 |
| --- | --- | --- |
| 矩阵条目 | **609** 行 | `node tools/parity/survey-evidence-coverage.cjs` |
| 真机证据 | **125（20.5%）** | 同上 |
| 复刻版证据 | 305（50.1%） | 同上 |
| 并排图 | 88（14.4%） | 同上 |
| 自动断言 | 520（85.4%） | 同上 |
| **行级四件套齐** | **83（13.6%）** | 同上 |
| **界面级四件套齐** | **27/27** ✓ | `node tools/parity/coverage-by-interface.cjs` |
| 引用完整性 | 177~182/182 全部存在（0 缺失） | `node tools/parity/check-evidence-files.cjs` |
| 台账四要素审计 | 43 条缺"断言"引用（多为已修条目未写 `ui-vNNN` ✗） | `node tools/parity/audit-diffs.cjs` |

## 三、工装清单（都能直接跑）

| 工装 | 用途 | 怎么跑 |
| --- | --- | --- |
| `Shoot-FromCommit.ps1` | **从任意提交**构建复刻版并抓图（隔离克隆 + 离线装依赖，**不碰主仓库 node_modules** ✓） | `powershell -File tools/parity/Shoot-FromCommit.ps1 -Sha HEAD -Scenes 'choose,custom,sysset'` |
| `Compare-SideBySide.ps1` | 真机图 × 复刻图 → 并排图（右侧标签写 commit SHA ✓） | 见上一行输出 |
| `Capture-CloneShot.cjs` | 18 个场景的抓图实现（菜单一族要 `menu-<x>` ✓） | 由 `Shoot-FromCommit` 调用 |
| `Verify-LabelFormatDialog.cjs` | 「标签格式设置」P0 ①–⑪（**18/18** ✓） | 起实例后 `node tools/parity/Verify-LabelFormatDialog.cjs` |
| `Verify-BarcodePage.cjs` | 条码页四页签 + **DIFF-83 范围守卫**（**5/5** ✓） | 同上 |
| `Verify-ObjectProps.cjs` | **对象属性四页 23 条断言**（当前 **9/23**，14 条 GAP＝待办 ✓） | 同上 |
| `Verify-PrintEndToEnd.cjs` | 打印链路端到端（**当前红** ✗；功能落地即转绿 ✓） | `node tools/parity/Verify-PrintEndToEnd.cjs` |
| `Smoke-Release.cjs` | **打包产物**冒烟（起 `win-unpacked` 并核对标题/菜单 ✓） | `node tools/parity/Smoke-Release.cjs` |
| `survey-evidence-coverage.cjs` / `coverage-by-interface.cjs` / `check-evidence-files.cjs` / `audit-diffs.cjs` | 覆盖率与台账体检（口径已加严：标"（部分"的引用不计入四件套 ✓） | 见第二节 |
| `LabelShopCtl.ps1` / `Invoke-LabelShopSteps.ps1` | **真机**取证（点击/按键/对话框控件/截图；**菜单必须用 `shotpopup:`** ✗ 用 `shotmain` 拍不到 ✓） | `powershell -File tools/parity/Invoke-LabelShopSteps.ps1 -StepsFile <步骤文件>` |

## 四、逐项比对的差异清单（A–J 十站）

完整版在 **`parity/验收方逐项比对-施工清单.md`** ✓（每条都带并排图文件名与复跑命令 ✓）。摘要：

| 类 | 界面 | 条数 | 性质 |
| --- | --- | --- | --- |
| **A** | 打印相关 | 4 | 设备/打印机枚举缺失（含 **A4 真打印后端** ✗） |
| **B** | 标签预览 | 4 | 预览缺序号/尺寸标注、数值格式、纸张底色 ✗ |
| **C** | 标题/文案 | 3 | 系统选项标题、端口页 `类型(T):`、多余帮助按钮 ✗ |
| **E/F/G/J** | **对象属性四页** | **≈28** | **控件形态（列表框 vs 下拉、单选 vs 下拉）、分组与标签、多余字段、底排按钮** ✗ ← 最大缺口 |
| H | 启始页 | 2 | 素材替代图（建议标注 ✓）、最新文章空列表待确认 |
| **I** | 安装打印机 | 1 | **几乎完全一致 ✓✓ 可作样板** |
| — | 加速键 | 1 | 真机 `(&X)` **确实可用** ✓（真机按 Alt+C 已取消对话框为证 ✓）→ 复刻版未接线 ✗ |

## 五、怎么接着干（给下一个接手的人/agent）

1. **先读**：`tools/loop/round-focus.md`（循环施工单，含我逐轮留的结论与证据 ✓）、`parity/验收方逐项比对-施工清单.md`（待办 ✓）、`parity/diffs.md`（差异台账 ✓）。
2. **验现状**（三条命令，30 秒）：`node tools/parity/survey-evidence-coverage.cjs`、`node tools/parity/coverage-by-interface.cjs`、`node tools/parity/check-evidence-files.cjs`。
3. **主攻**：对象属性四页 ✓ —— 每页改完跑 `Verify-ObjectProps.cjs` ✓，看**红条数下降** ✓；已绿的变红＝改坏了 ✓。
4. **打印后端**：实现后跑 `Verify-PrintEndToEnd.cjs` ✓（它会校验"页面尺寸必须是标签尺寸而不是 A4" ✓）。
5. **纪律**（都踩过坑 ✓）：取证不到就写"待取证"✗**不要猜实现** ✓；并排图必须**同态**且**用当前构建**（右标签写 SHA ✓）；引用只写"图里确实有的"✓，部分覆盖要标"（部分…

### ⚠️ 五之补：**我自己踩过的七类"假象"** —— 接手者请逐条对照，别重犯 ✗

> 这一段是**验收方（我）**的错题本 ✓。它们的共同根因只有一句话：**拿"某一瞬间的观察"当"功能的事实"** ✗。

| # | 我当时的判断 ✗ | 真相 | 正确的做法 ✓ |
| --- | --- | --- | --- |
| 1 | 9 条"真机菜单截图"其实引用的是**起始页截图** ✗ | 引用造假（我自己发现后全部撤销并重拍 ✓） | 引用前**打开那张图**看内容 ✓；只看文件名不算 ✓ |
| 2 | C 类三条"标题/加速键/按钮"是缺陷 ✗ | 真机原图一照，**三条都不成立** ✗（我看的是**缩小后的并排图** ✗） | 判**细字/控件个数/标题**必须看**原始单图**或**控件 dump** ✓ |
| 3 | "字体名称/样式是列表框、复刻版用下拉所以不对" ✗ | 真机**就是 ComboBox** ✓ —— 我看到的"列表"是**下拉展开态** ✓ | 判控件形态看 **dump 的 class** ✓，不靠截图 ✓ |
| 4 | "点打印没产物 ⇒ 复刻版**没有**打印后端" ✗ | 实现**早就有** ✓（`printExecutor` → `print-label` → `webContents.print` ✓） | "点了没反应"必须先**观测中间态**（窗口标题/打印队列/日志 ✓）再下结论 ✓ |
| 5 | "选择标签格式显示（未检测到打印机）" ✗ | 实测列表**有 4 台真实打印机**且默认选中一台 ✓ —— 我截图的**那一刻 IPC 还没返回** ✗ | **读控件的实际取值**（DOM/控件枚举 ✓）而不是看截图 ✓ |
| 6 | "并排图过时"只说了一半 ✗ | 那张图**同时**两侧对象类型不同 ✗（左文字/右条码 ✓） | 出图前确认**同态**（同一对象类型 ✓ 同一状态 ✓）✓ |
| 7 | "门禁随机红 = 循环脚本有竞态" ✗ | 主要是我自己**留了一个 63 分钟的 electron 实例**在抢资源 ✗ | 动 UI 前先清自己的残留 ✓；跑完必收摊 ✓（`Verify-CloneAtHead.ps1` 已把这条写进流程 ✓） |

**由此固化下来的三条硬规矩** ✓：
1. **观测优先** ✓：任何"没有/不生效"的判断，先给出**中间态证据**（进程、窗口标题、队列、dump、日志 ✓）；
2. **原始优先** ✓：细字/数量/标题看**原图或 dump** ✓，并排图只比**结构与布局** ✓（且必须**同态** ✓、**写构建 SHA** ✓）；
3. **自己先干净** ✓：动 UI 前清扫自己的实例 ✓，跑完收摊 ✓ —— 否则我制造的"抖动"会被误判成产品问题 ✗。

**⚠️ 判断口径：只报"观察到的事实 + 可复跑命令"** ✓ —— 这点我做得还不够好（上表 7 条里有 5 条是"观察不足 + 结论过强" ✗），接手者请继续按这个标准要求我留下的文档 ✓。
"✓；真机菜单截图必须 `shotpopup` ✓；动 `node_modules` 前先确认（见 `parity/FAILURES.md` 里我那次事故 ✓）。
6. **循环运维**：`tools/loop/Start-Loop-Auto.ps1` 后台跑 ✓（codex 优先、额度耗尽自动切 claude ✓，14:00 重置 ✓）；看护 `Ensure-Loop.ps1` + 计划任务 `MaxLabel-LoopWatchdog` 每 5 分钟自愈 ✓。
7. **发布**：`powershell -File tools/parity/Release-Clone.ps1 -Version x.y.z` ✓（只卡**产品源码**干净 ✓，自算 SHA256 ✓）；发布后跑 `Smoke-Release.cjs` ✓。当前已发布 **v1.0.20** ✓（`RELEASE-SHA256.txt` 顶部 ✓）。
