# 验收方取证（EXE 字符串资源）：真机「标签格式设置」对话框的控件清单交叉验证

- 日期：2026-09-21（round-23）
- 对象：`C:\Program Files (x86)\LabelShop\LabelShop\LabelShop.exe`
- 方法：把 EXE 按 **UTF-16LE** 全量解码，取关键词附近 ±260 字符做中文连续串提取（与 round-60 判定「边界值」时同一套方法，见 `PROBE-round60.md`）
- 目的：**不依赖真机交互**地独立核对 codex 的控件树 dump（`probe-round105-custom-label-tree.txt`），并判断复刻版缺的那些字段是"真缺"还是"dump 解析假象"

## 一、结果：对话框字符串块（按出现顺序）

以 `孔洞` / `多行标签` / `形状` 三个关键词分别取上下文，得到同一段资源块，顺序为：

```
… 行数(&R): | 毫米 | 标签 | 间距 | 行列 | 形状 | 孔洞 | 多行标签 | 打印机 |
  名称(&N): | 标准驱动(&S) | 设置(&S) | 高级设置(&A) | 输出方式: | 安装(&I) | 整页反相打印 | …
```

与 codex 的控件树 dump **完全对得上**：分组框 `标签/间距/行列/形状/孔洞`、紧邻的（树里不可见的）按钮 `多行标签`、
以及 `打印机` 页的 `名称(N)` / `标准驱动(S)` / `设置(S)` / `高级设置(A)` / `输出方式:` / `安装(I)` / `整页反相打印`。

## 二、计数佐证（确认这些不是 dump 假象）

| 字符串 | EXE 中出现次数 | 说明 |
| --- | --- | --- |
| `整页反相打印` | **1** | 真机打印机页按钮，复刻版全库搜不到 → **真缺口** |
| `镜像输出` | **1** | 同上 |
| `单页任务模式` | **1** | 同上 |
| `标准驱动` | **1** | 同上 |
| `高级设置` | 6 | 多处复用（其它对话框也有） |
| `圆洞` | **1** | 真机「孔洞」下拉的选项之一 —— 与复刻版现有 `圆洞` 一致 ✓ |

## 三、仍未解的一点（不要猜）

真机「孔洞」下拉是 **3 项**（codex dump：`无  (选中 0 / 共 3 项)`），但字符串资源里**只能确认 `圆洞` 存在**，
另外两项无法由字符串定序（`圆孔/方孔/方洞/椭圆孔/矩形孔/中心孔` 等候选计数全为 0，疑为短串或空串或运行时拼装）。
→ **必须真机枚举**：`tools/parity/Probe-LabelShopCombos.ps1 -TitleLike '*标签格式设置*' -ListOnly`
（注意：该对话框当前页是「标签」页，枚举脚本默认只看可见页控件 ✓ 正好）。

## 四、复刻版对照（round-23 实测 grep）

在 `app/src/renderer/src` 全库搜索：`反相` **0**、`单页` **0**、`镜像` 仅出现在对象属性（不是打印页的镜像输出）、
`标准驱动` **0**、`高级设置` **0**。即：**打印机页的这些控件在复刻版里是缺的**（不是"藏在别处"），
与 P0「追加 2」清单里的待办一致；实现时若选择"等价替代"（例如复用 `PrinterSettings` 里的同类项），
必须在 `parity/diffs.md` 写明替代关系，不要默默当已有。
