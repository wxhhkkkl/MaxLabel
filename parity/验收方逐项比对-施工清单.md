# 验收方「逐项比对」施工清单（round-179 ~ 181 四站结果归并）

> 方法：用 `tools/parity/Shoot-FromCommit.ps1` 从**某个提交**构建复刻版并抓图 ✓，与真机实拍**并排逐项比对** ✓。
> 为什么需要它：`parity/P4-界面级覆盖表.md` 的"四件套齐 27/27"只说明**证据覆盖** ✓，不等于**逐项一致** ✗ ——
> 四站比下来，复刻版的**结构与文案已经很好** ✓，差异集中在下面 **A/B/C** 三类，都是**可一次成批修**的 ✓。
> 每条的并排图都在 `parity/review/cmp-*-<sha>.png` ✓（图名里的 sha 就是当时的构建点 ✓）。

## A. 设备/打印机枚举与打印后端 —— ⚠️ **round-209 重大更正：实现其实存在 ✗，我原先说"缺打印后端"是误判**

> **我有错在先，先更正** ✗：我在 round-174 说"复刻版**没有**把标签送给 Windows 打印机的实现"✗ ——
> 那是**我用错了搜索词**（只搜了渲染层的 `window.print` / `printToPDF` ✗，漏了主进程的 `webContents.print` ✗）。
> round-209 复查源码，**实现是齐的** ✓：
> - 主进程 `app/src/main/ipc/registerPrintIpc.ts` ✓ 注册了 **`print-label`** ✓、**`printers:list`** ✓、`ports:list` ✓、`print:command` ✓、`print:cancel` ✓；
> - `print-label` 的做法**正确** ✓：把页面写成临时 PNG ✓ → 生成 `printHtml(pages, widthMm, heightMm)` ✓ → 隐藏 `BrowserWindow` 载入 ✓ →
>   **`window.webContents.print({ silent: false, deviceName: payload.printerName, pageSize: { width: mm×1000, height: mm×1000 } })`** ✓✓
>   —— **页面尺寸用的是微米单位的标签尺寸** ✓（正是我在端到端工装里想校验的那一条 ✓）；
> - 渲染层 `features/printing/printExecutor.ts` L243 ✓ 通过 `window.maxlabel.printLabel({ pages, widthMm, heightMm, printerName? })` ✓ 调用它 ✓；
>   入口是 `usePrintWorkflow` → `executePrint(test, deps, …)` ✓。

**所以 A4 这条要重新定义** ✓：不是"没有实现"✗，而是"**我按下去的那条路径没走到它**" ✗ ——
我的端到端工装点的是**右侧面板的「打印」** ✓ → 那条走 `handlePrintNow` → `runPrint` ✓（**内置指令链** ✓，对应真机"签赋LabelShop 打印机"✓）；
而**系统打印机**那条是 `executePrint` ✓。**待查**：哪个 UI 入口触发 `executePrint` ✓（候选：打印对话框里的「打印(P)」✓ / 「测试打印」✓）→
定位后我再跑端到端 ✓；若确实**没有任何入口**能触发 `executePrint` ✓，那才是真缺口 ✗（且缺口是"**没接线**"而不是"没实现"✓，工作量小得多 ✓）。

| # | 现象 | 真机 ✓ | 复刻版 | 结论 |
| --- | --- | --- | --- | --- |
| A1 | 打印对话框「打印机 名称/位置」 | `Microsoft Print to PDF` / `PORTPROMPT:` | `打印机` / `Windows 打印机驱动端口` ✗ | 仍待查（可能是"选的是系统默认"这一态的显示问题 ✓） |
| A2 | 选择标签格式的 `打印机(P):` | 真实设备名 | `（未检测到打印机）` ✗ | 待查：`printers:list` 存在 ✓，可能是**该对话框没调用它** ✗ |
| A3 | 打印机属性窗口标题 | `Gprinter GPL-N (203 dpi) 属性` | `打印机设置` ✗ | 待查（同上：设备名没取到 ✓） |
| A4 | **点「打印」是否真送出** | 支持系统打印机 ✓ | 我的工装那次**作业数 0** ✗ | **重新定义为"入口走错"** ✓（见上 ✓），不再写"后端缺失" ✗ |

## A⁗. round-216 **定论：A4 不是缺口** ✓✓ —— 我的 round-174 结论彻底推翻 ✗（附硬证据）

用升级后的工装在**隔离实例**上实测 ✓（提交 `acf5b33` ✓，`electron=0` 的空闲窗口 ✓）：
```
[e2e] 打印机下拉选项：["系统默认打印机","OneNote (Desktop)","Microsoft Print to PDF","HP7E6C81 (HP LaserJet Pro M329)","Gprinter  GP-1324D","HP LaserJet Pro M329 PCL-6 (V4)"]
[e2e] 选中打印机：Microsoft Print to PDF
[e2e] 已点 dock 的打印按钮：true
[e2e] 第 1 次探测 → DLG: Electron - 打印 | Windows 打印      ← ★ 系统打印对话框**真的出现了** ✓✓
```
→ **点「打印」确实把标签送进了操作系统的打印链路** ✓✓ —— A4 **不是缺口** ✗✓，我 round-174 那句"复刻版没有把标签送给 Windows 打印机的实现"**是错的** ✗（错因：只搜了渲染层 `window.print`/`printToPDF` ✗，漏了主进程 `webContents.print` ✗）。

**P2 的真实剩余工作因此缩到很小** ✓：
1. **把我的工装补完** ✗（这步是我这边的活 ✓）：现在出现的是 **Windows/Electron 打印对话框** ✓（不是"另存为"✗），
   我的按键发给了应用窗口 ✗ → 应该**先给这个打印对话框发确认（Enter/确定）** ✓，之后才轮到"另存为"填文件名 ✓ → 然后校验 PDF 是否**标签尺寸** ✓；
2. **A1/A2/A3 的"设备名显示"** ✓ 仍待判定（A2 的两种可能 ✓，判定方法见上一节 ✓）；
3. **P2 里"造打印后端"这件事——不存在** ✗✓（实现早已在 `printExecutor` → `print-label` → `webContents.print` ✓）。

> 教训（写给我自己）✗：**"我按下去没看到产物" ≠ "功能不存在"** —— 这次我连错三轮（缺后端 ✗ → 走错入口 ✗ → 缺对话框处理 ✓），
> 根因都是**没有观测中间态** ✗。以后凡是"点了没反应"的判断，必须先做**中间态观测**（窗口标题/队列/日志 ✓）再下结论 ✓。

> #### round-217 追加：我又试着把预览确认掉，**没成** ✗（照实记录），结论不变 ✓
> 我加了"给该窗口发 Enter"的一步 ✓，结果：
> ```
> [e2e] 发按键 {ENTER} → SENT: Electron - 打印
> [e2e] 确认后第 1..8 次探测 → DLG: Electron - 打印 | Windows 打印   ← 它一直没关 ✗
> [e2e] 产物：…\maxlabel-print-e2e.pdf  存在=false 大小=0 字节
> ```
> 也就是说：那个窗口是 **Electron 的打印预览/打印对话框** ✓（应用主窗口标题是 `MaxLabel [未激活] V1.0.20 …` ✓，与它不同 ✓），
> **光发 Enter 确认不掉它** ✗ —— 需要在它里面**点到「打印」按钮** ✓（我下一步会用"枚举子控件 + BM_CLICK"的办法做 ✓）。
> **但 A4 的结论不受影响** ✓：**系统打印链路确实被走到了** ✓（对话框出现就是证据 ✓）；
> 拿不到 PDF 是**我的自动化没走完对话框** ✗，不是产品缺陷 ✗。
>
> **给停止标准 ④ 的诚实口径** ✓：
> - "回归绿" ✓ **已达成**（91/91 ✓，且是在无我干扰的干净环境 ✓）；
> - "真实打印机链路至少一次端到端成功" → **现状：链路已通到系统打印对话框 ✓；最终产物（PDF/纸张）尚未由我确认 ✗**。
>   要么我再用"点对话框里的打印按钮"攻一次 ✓，要么**人工点一次**（1 分钟 ✓）即可结案 ✓ —— 建议**不**把它算作产品缺口 ✗。

## A‴. round-214 把 A2（选择标签格式里"未检测到打印机" ✗）追到只剩两种可能

读代码 ✓：`dialogs/NewLabelDialog.tsx` **确实调用了** `window.maxlabel.listPrinters()` ✓（L126–142 ✓），
并且当 `printers` 为空时会落到"未检测到打印机"的显示 ✓；而同一台机器上 **`PrintDock` 的打印机下拉是满的** ✓
（`系统默认打印机` / `Microsoft Print to PDF` / `HP7E6C81 …` / `Gprinter GP-1324D` ✓ —— 我在 round-174 亲眼见过 ✓）。

→ 所以只剩两种可能 ✓：
1. **对话框里那次 `listPrinters()` 返回了空** ✗（例如 `.catch(() => {})` 把错误吞掉了 ✓ —— L143 ✓ 就是个空 catch ✗；或对话框在 IPC 就绪前就跑了 ✓）；
2. 或者**列表其实有值，只是"默认选中项"落在了占位项** ✗（L137–141 的偏好逻辑 ✓：新 profile 下 `readDefaultPrinter()` 为空 ✓，
   理论上应回落到 `items[0].name` ✓ —— 若 `items[0]` 恰好是占位项，就解释得通 ✓）。

**判定方法（等 UI 空闲，5 分钟）** ✓：起实例 → 打开「选择标签格式」→ 用 CDP 读 `[data-testid=printer-select]`（或该组合框）的
`options.length` 与 `selectedIndex` ✓：
- `options.length ≤ 1` → 属可能 1 ✗（**真缺口**：列表没取到 ✓ → 需查 IPC/时序 ✓）；
- `options.length > 1` → 属可能 2 ✓（**小问题**：默认选中项该回落到第一个真实打印机 ✓）。
（**待 UI 空闲再跑** ✓ —— 现在循环的全量门禁在跑，按纪律不抢资源 ✗。）

## A″. round-210 继续追这条：**A4 很可能根本不是缺口** ✗✓（待 UI 复测确认）

读了 `App.tsx` L125 ✓：
```ts
const { run: runPrint, cancel: cancelPrintWorkflow } = usePrintWorkflow(beginAsyncOperation)
```
→ **`runPrint` 就是 `usePrintWorkflow` 的 `run`** ✓ = `executePrint`（**系统打印机那条** ✓），
而 `doPrint` 里调用的也正是 **`runPrint`** ✓ —— 也就是说：**点右侧面板的「打印」走的就是 OS 打印路径** ✓✓（不是我 round-174/209 以为的"另一条内置指令链" ✗）。

**那么我的端到端为什么"没有产物、队列 0"** ✗？最合理的解释 ✓：
1. `executePrint` 走到 `webContents.print({ silent:false, deviceName:'Microsoft Print to PDF' })` ✓ →
2. Windows 弹出**「将打印输出另存为」对话框** ✓（`PORTPROMPT:` 的行为 ✓）→
3. **我的工装那次没能把这个系统对话框填完** ✗（我只对应用窗口发了 SendKeys ✓，且没有截图确认对话框是否出现 ✗）→
4. 于是：**没有 PDF** ✓ + **打印队列显示 0 个作业** ✓（作业在等文件名，尚未落到队列 ✓）—— 两个现象**都能被这一条解释** ✓✓。

**所以结论要改成** ✓：**A4 大概率不是"缺后端"，而是"我的工装没把系统对话框走完"** ✗✓ ——
**待 UI 复测确认** ✓（步骤：起实例 → 放对象 → 选 `Microsoft Print to PDF` → 点打印 → **立刻截图看有没有「另存为」对话框** ✓ → 有则说明链路通 ✓，
再把填对话框这步做对 ✓）。`Verify-PrintEndToEnd.cjs` 我会照此升级 ✓（加一步"打印中截图"+ 更稳的对话框处理 ✓）。

> ⚠️ 因为循环的**全量门禁正在跑** ✗（我绝不能在这时抢 UI 资源 ✗ —— 这正是我 round-204 那次残留实例造成的假抖动的教训 ✓），
> 这一步 UI 复测**等门禁结束再做** ✓。

## A′. 原 P2 的剩余工作（更正后）

1. **定位触发 `executePrint` 的 UI 入口** ✓（读 `App.tsx` 里 `usePrintWorkflow` 的调用点 ✓）；
2. 若入口存在 ✓ → 把我的 `Verify-PrintEndToEnd.cjs` 改成走**那条入口** ✓ → 端到端应能产出**标签尺寸**的 PDF ✓；
3. 若入口不存在 ✗ → 才需要"接线"（把系统打印机分支接到 `executePrint` ✓）—— 这是**小改** ✓；
4. A1–A3 的"设备名显示"问题 ✓：既然 `printers:list` 已有 ✓，多半是**显示/取值**环节没接 ✓（小改 ✓）。

## B. 标签预览绘制细节 —— ✅ **两个对话框都已修并复验通过** ✓（round-194/195/196 复核完毕）

> 循环 commit `2476966`（DIFF-84：预览字号按渲染像素算 + 毫米字段两位小数 ✓）+ `e603721`（台账收口 ✓，断言 `ui-v140` 14/14 ✓）。
> **我两边都复验过**（用安全工装从 HEAD 出图，看**原始单图** ✓）：
> - 「选择标签格式」`clone-choose-f9d3343.png` ✓ → 序号 1…8 ✓、红色 `100mm`/`70mm` ✓、`100.00 毫米 X 70.00 毫米` ✓；
> - 「标签格式设置」`clone-custom-6071782.png` ✓ → 序号 1…8 ✓、红色 `100mm`/`70mm` ✓、`宽度(W): 100.00`/`高度(H): 70.00`/`列距(P): 2.00`/`行距(L): 2.00` ✓。
> **B4（纸张底色灰 vs 真机白）** 仍未改 ✓ —— 保留为待定小样式 ✓（不影响功能 ✓）。
> ⚠️ 顺带再次确认：这一页底排是 **`确定 / 取消 / 帮助`** ✓ 三个 —— 印证我原 C3 的判断是**误判** ✗（真机同样三个 ✓）。

| # | 现象 | 真机 ✓ | 复刻版（round-194 前 ✗ → 现在 ✓） | 证据图 |
| --- | --- | --- | --- | --- |
| B1 | 每格**序号** | 1–8 编号 ✓ | ✗ → **✓ 已修（两个对话框都是）** | `clone-choose-f9d3343.png`、`clone-custom-6071782.png` |
| B2 | 图上**尺寸标注** | `100mm`（上）/ `70mm`（右） | ✗ → **✓ 已修** | 同上 |
| B3 | **数值格式** | `70.00` / `2.00` / `2.00`（两位小数） | ✗ → **✓ 已修** | 同上 |
| B4 | 纸张底色 | 白 ✓ | 仍为灰 ✗（小样式，待定 ✓） | 同上 |

**建议**：B1/B2 一次修可覆盖两个对话框 ✓（DIFF-73 只改了「标签格式设置」的整版网格 ✓，序号/标注没跟上 ✗）。

## C. 标题 / 文案类小改 —— ⚠️ **本节三条经复核全部是验收方的误判 ✗，已作废（不要按它改代码）**

> **作废原因（round-194 更正）**：循环在 round-136 逐条回看**真机原图**后指出本节三条都不成立 ✓，
> 我又亲自打开 `parity/reference/labelshop/probe-r112-sysset.png` **原图**核对 —— **它是对的，我错了** ✗✗。
> 根因：我读的是**缩小后的并排图**（`cmp-*.png` ✓），细字（标题、加速键字母、按钮个数）在小图上被我看错了 ✗。
> **方法更正**：判断"细字/控件个数"必须看**原始单图**或**控件值 dump** ✓；并排图只用来比**结构与布局** ✓。

| # | 我原来的说法 ✗ | 真机原图实际 ✓ | 结论 |
| --- | --- | --- | --- |
| C1 | 系统设置标题应是 `系统选项` | `probe-r112-sysset.png` 标题栏写的就是 **`系统设置`** ✓（`系统选项(C)...` 是**菜单项**名 ✓） | **误判 → 复刻版正确 ✓ 不改** |
| C2 | 端口页 `类型:` 漏了 `(T)` | 真机是 **`类型(I):`** ✓（加速键是 I，我连自己早先的记录都写反了 ✗） | **误判 → 复刻版已是 `类型(I):` ✓ 不改** |
| C3 | 标签格式设置底排多一个 `帮助` | `verifier-r44-hole-circle-20b.png` 与 `round105-custom-label.png` 底排都是 **`确定 / 取消 / 帮助`** ✓ | **误判 → 复刻版正确 ✓ 不改** |

**教训（写给我自己）**：列出"真机有/没有"这种断言前，**必须打开对应原始图**逐字核对 ✓；
拿不准就写"待取证" ✓ —— 这正是我一直要求循环做的，这次轮到我自己违反 ✗。

## B. 标签预览绘制细节（**同一个渲染器**：选择标签格式 + 标签格式设置都用它 ✗）

## L. 文件菜单（round-197 新比，**条目已对齐** ✓；差异是状态类 ✓）

证据图：`parity/review/cmp-filemenu-e603721.png` ✓（左真机 `probe-19-filemenu.png` × 右复刻版 commit e603721 ✓，两侧都是**有文档态** ✓）

**逐项对得上** ✓：`新建(N) Ctrl+N`、`新建条幅飘带`、`打开(O)... Ctrl+O`、`关闭(C) Ctrl+W`、`保存(S) Ctrl+S`、`另存为(A)...`、
`分享(T)...`、`打印(P)... Ctrl+P`、`打印预览(V)`、`导出打印机指令文件(E)`（两侧都灰 ✓）、`标签格式设置(L)...`、`模板属性设置(M)...`、`最近的文件`、`退出(X)` ✓ —— **条目集合与顺序一致** ✓✓。

**差异（都是状态类 ✓，不算缺陷 ✗）**：
- 复刻版 `保存(S)` 与 `最近的文件` 是**灰的** ✓ —— 因为这份文档**未修改**且**没有最近文件** ✓（真机那张是改过的状态 ✓）；
- 分隔线位置/子菜单箭头这类**细部**，本图缩小后看不清 ✗ → **按新规矩不在并排图上判细字** ✓（要看就看原始单图 ✓）。

→ 结论：**文件菜单与真机基本一致** ✓（与 ①「界面四件套」里 A-34/A-43 的引用也吻合 ✓）。

## K. 模板向导（round-193 新比，**几乎完全一致** ✓✓ —— 第二个"复刻到位"样板）

证据图：`parity/review/cmp-wizard-615557e.png` ✓（左真机 `probe-63-01-template-wizard.png` × 右复刻版 commit 615557e ✓）

| 元素 | 真机 ✓ | 复刻版 ✓ |
| --- | --- | --- |
| 标题 | `模板向导` | **同** ✓ |
| 说明文字 | `您可以选择打开一个现有的标签模板文档进行工作，也可以新建一个标签模板。` | **逐字一致** ✓ |
| `请选择:` 四个单选项 | `打开一个现有的标签模板` / `新建标签模板`（默认选中）/ `查看 LabelShop 联机帮助` / `查看 LabelShop 在线使用教程` | **四项同序同文案、默认项也一致** ✓ |
| 复选框 | `下次启动时不再使用向导` ✓ | **同** ✓ |
| 底排按钮 | `下一步` / `取消` | **同序同文案** ✓ |

→ 这一屏**零差异** ✓。与 I（安装打印机）一起说明：**不涉及"对象属性"的界面，复刻版已经做到很像** ✓✓；
差距确实集中在**对象属性四页**（E/F/G/J ≈28 条 ✗）与**打印后端** ✗ 两处 ✓。

> **⚖️ round-215 补充：这一页现在有一个"决策"要你拍板** ✓（和 G 数据源页同类 ✓）
> 我在代码里看到 `dialogs/ObjectPropsDialog.tsx`：
> - 常规页渲染在 **L1746** ✓（`{tab === 'general' && (`）；
> - L1750 / L1777 用 `<FormField label="水平位置" …>` / `"垂直位置"` ✗，提示语是"相对标签边对齐（保持当前尺寸）"✓ ——
>   也就是**把真机的两组并成了一条**：真机是 ①`位置` 组（`水平(&H):` / `垂直(&V):` 两个 **Edit + 毫米**，见 dump ✓）
>   与 ②`对齐` 组（`水平(&W):` / `垂直(&T):` 两个 **ComboBox 且 DISABLED** ✓），**两组语义不同** ✓；
> - 而 **L1177 的注释里你已经写明**："真机的 `对齐` 是「常规」页的分组框…语义不同" ✓ —— 说明**你是知情地合并的** ✓。
>
> 所以请像 G 一样**明确处置** ✓：
> (a) **按真机拆回两组** ✓（`位置` 用 Edit+毫米 ✓、`对齐` 组用两个下拉且此状态下禁用 ✓）；或
> (b) **保留合并实现** ✓，但在 `diffs.md` 登记为「**复刻版扩展/不同实现**」并说明理由 ✓（例如"合并减少歧义"✓）。
> **不要既不改也不登记** ✗ —— 否则我的清单和 `Verify-ObjectProps` 会一直把它算成缺口 ✗，而你的判断可能其实更好 ✓。
> （其余 F′ 条目是**纯增补**、无争议 ✓：`色彩反相(&E)` ✗缺、`设置颜色`（禁用态）✗缺、`对象名称标识：`/`图层：`（全角冒号）✗缺、各标签补加速键 `(&R)/(&M)/(&B)/(&L)/(&N)` ✗。）

## F′. 对象属性「常规」页 —— **照 dump 的权威对照表**（round-202 补，取代原来的 F 节描述）

> 依据：`parity/reference/labelshop/probe-44-two-objects-tree.txt` ✓（真机控件树）。**可见控件逐条如下**（→ 右侧是复刻版该做什么 ✓）：

| 真机 class | 真机文案（逐字） | 复刻版要做 |
| --- | --- | --- |
| `Button`（组框） | `位置` | **要有这个分组框** ✓（复刻版现在是"水平位置/垂直位置"两列 ✗） |
| `Static` + `Edit` | `水平(&H):` | 标签改成 `水平(&H):` ✓、值单位 `毫米` ✓（复刻版是 `X（毫米）` ✗） |
| `Static` + `Edit` | `垂直(&V):` | 同上 ✓（复刻版是 `Y（毫米）` ✗） |
| `Static` + `ComboBox`（**DISABLED**） | `水平(&W):` | **补上「对齐」组并让它在此状态下禁用** ✓（不是删除 ✗） |
| `Static` + `ComboBox`（**DISABLED**） | `垂直(&T):` | 同上 ✓ |
| `Button`（组框） | `其它` | ✓ 已有 |
| `Static` + `ComboBox` | `旋转(&R):` | 补 `(&R)` ✓ |
| `Static` + `ComboBox` | `镜像(&M):` | 补 `(&M)` ✓ |
| `Static` + `ComboBox` | `背景(&B):` | 补 `(&B)` ✓ |
| `Button` | `色彩反相(&E)` | **复刻版没有 ✗ → 要补** ✓ |
| `Button` | `位置锁定(&L)` | 补 `(&L)` ✓ |
| `Button` | `不打印输出(&N)` | 补 `(&N)` ✓ |
| `Static` + `Edit` | `对象附加说明(&C)` | 位置照真机（独立一行 ✓，复刻版塞进两列区域 ✗） |
| `Button`（组框） | `颜色` | ✓ 有 |
| `Static` + `ComboBox` | `颜色(&C):` | ✓ 有 |
| `Button` | `颜色` | 真机这里还有一个 `颜色` 按钮 ✓（复刻版形态待核） |
| `Button`（**DISABLED**） | `设置颜色` | 复刻版看不到 ✗ → 补上（该状态下禁用 ✓） |
| `Static` + `Edit` | `对象名称标识：` | **复刻版缺 ✗ → 补** ✓（注意真机用的是**全角冒号** `：` ✓） |
| `Static` | `图层：` | **复刻版缺 ✗ → 补** ✓（另一块 dump 里可见 ✓，全角冒号 ✓） |

**⚠️ 两条要按 dump 的"可见性/启用态"来做，别一律当"缺失"** ✗：
- 真机的 `水平(&W):`/`垂直(&T):` 是**存在但 DISABLED** ✓（该对象类型不支持时灰掉 ✓）—— 复刻版应"有控件但禁用" ✓，而不是"整组没有" ✗；
- 真机的 `设置颜色` 也是 **DISABLED** ✓。

## J. 对象属性「文本」页（round-190 新比，**控件形态与标签都不同** ✗）—— 与 E/F/G 同一族

证据图：`parity/review/cmp-propstext-d40daad.png` ✓（左真机 `r88-textprops-p3.png` × 右复刻版 commit d40daad ✓，两侧同为**文字**对象 ✓）

| # | 元素 | 真机 ✓ | 复刻版 ✗ |
| --- | --- | --- | --- |
| J1 | `类型` | **三个单选按钮**：`单行(S)` / `多行(M)` / `圆形(C)` | **下拉框**（`单行`）✗ 控件类型不同 |
| J2 | `属性` 组的对齐 | `水平对齐(A):` 下拉（值 `左齐` ✓）+ `行宽度(W): 15.58 毫米` | 拆成两列：`对齐 居中` ✗ + `文字停靠 两端` ✗ + `行宽度（毫米）40` ✗ —— **标签与语义都不同** ✗ |
| J3 | `字符模板(T):` | 底部一个**复选框** ✓ | 一个**下拉 + 转义码文本域** ✗ |
| J4 | 复刻版多出的字段 | —— | `大小写转换 无` ✗、`子串起始（0 起）` + `子串长度（0=全部）` ✗、`截断 无` + `字符数限制` ✗、以及多行提示 |
| J5 | 底排按钮 | `确定 / 取消 / 帮助` | `取消 / 确定` ✗（同 E8/F8） |

> **对象属性四页合计**（E 字体 9 + F 常规 8 + G 数据源 6+ + J 文本 5）≈ **28 条** ✗ ——
> 这是目前**最大的相似度缺口** ✓，也正好是目标里 P1.5/P2 指的方向 ✓。
> 建议循环**集中几轮专门啃这四页** ✓：先按"控件形态 + 标签 + 分组"三件事对齐 ✓，再补断言 ✓。

## I. 安装打印机（round-187 新比，**几乎完全一致** ✓✓ —— 目前最贴近的一页）

证据图：`parity/review/cmp-install-6f8f9c3.png` ✓（左真机 `probe-07-install-printer.png` × 右复刻版 commit 6f8f9c3 ✓）

| 元素 | 真机 ✓ | 复刻版 ✓ |
| --- | --- | --- |
| 标题 | `安装 LabelShop 打印机` | **同** ✓ |
| `可安装的打印机:` + 筛选下拉 | `全部` ✓ | **同** ✓ |
| 表格列 | `打印机` / `状态` | **同** ✓ |
| **列表内容与顺序** | Gprinter GPL-N (203/300 dpi) → GPLZ-N (203/300) → ESCPOS-N (200) ×2 → GPLZ-N (203) → Ledan APL-N (203/300/600) → Ledan RFID-N (203/600) | **逐项一致** ✓✓ |
| 说明文字 | `安装 LabelShop 打印机，可以在LabelShop中实现一般的标签打印功能。…请安装官方提供的驱动程序。` | **逐字一致** ✓ |
| 底排按钮 | `安装 / 移除 / 帮助 / 返回` | **同序同文案** ✓ |

**唯一差异** ✓：复刻版多一行提示 `已安装 0 台：装好的打印机会出现在「选择标签格式」里的"打印机"下拉框…` ✗ ——
属**复刻版扩展**（对用户有帮助 ✓）→ 建议在台账**标注为扩展**即可 ✓，不必删 ✓。

**结论**：这一页可以作为"复刻到位"的样板 ✓ —— 说明循环在**有明确真机证据**的地方做得很好 ✓；
反过来说，E/F/G（对象属性三页）的差距**不是能力问题** ✓，而是**缺证据/缺比对** ✗ —— 这也正是我在补的 ✓。

## H. 启始页（round-186 新比，**结构已经很接近** ✓，差异多为"素材/网络"类 ✓）

证据图：`parity/review/cmp-start-886388d.png` ✓（左真机 `92-00-startup.png` × 右复刻版 commit 886388d ✓）

**对得上** ✓：左栏结构完全一致 ✓（未登录 + 头像、三个计数格 `0/0/0`、`标签商城`/`新手入门`、`开始` 列表 `客服1QQ/客服2QQ/客服电话/新建标签模板/打开标签模板/打开本机模板/下载云马通APP`、`最近 → 暂无最近文件` ✓）、
`云马通首页` 页签 ✓、`重要通知` + `购买点击` ✓、`签赋学堂` ✓、`各类不干胶标签` + `联系客服即刻购买` ✓、`最新文章` 区块标题 ✓。

| # | 差异 | 判断 |
| --- | --- | --- |
| H1 | 横幅/广告位：真机是**实拍图**（仓库照、贴纸图 ✓），复刻版是**简化插画/占位图形** ✗ | **多半是有意的**（版权素材 ✓）→ 建议**在台账写明**"素材用替代图" ✓，别当缺陷 ✗ |
| H2 | `最新文章`：真机有 3 条带日期 ✓，复刻版此图为空 ✗ | **需确认**：可能是**本机无外网**导致列表取不到 ✓（DIFF-79 修过并验过 ✓）→ 请说明空列表的触发条件 ✓；若离线就该显示内置示例，那属于缺陷 ✗ |

## G. 对象属性「数据源」页（round-185 新比，**结构完全不同** ✗✗ —— 目前发现的最大 UI 分歧）

证据图：`parity/review/cmp-datasource-381a31e.png` ✓（左真机 `r88-textprops-p1.png` × 右复刻版 commit 381a31e ✓，两侧同为**文字**对象 ✓）

**真机的模型**（"子串"为中心 ✓）：
- `子串列表` **列表框**（可见一条 `12345678`）+ 下方一排小工具按钮（新建/复制/粘贴/删除/上移/下移 图标 ✓）；
- `子串选项` 组：`数据源(S):` 下拉（当前 `常量`）+ `显示数据(D):` 多行文本（`12345678`）+ 放大镜图标；
- `变量共享名称(N):` 输入框 + `源自其它变量` 复选框；
- `高级选项(A)...` 按钮；
- `示例`：一个**列表框**显示 `12345678`。

**复刻版的实际**（"主数据源 + 附加子串"为中心 ✗）：
- 一排按钮 `常量 / 序列号 / 日期 / 时间 / 数据库 / 键盘输入 / 脚本`（"主数据源"）；
- `常量内容` 多行文本，且里面列的是**转义码表**（`<SOH> <STX> <ETX> <EOT> <ENQ> <ACK> <BEL> …`）✗；
- `附加数据源（子串）`：`粘贴` / `添加子串` 按钮 + `(无子串)`；
- 底排 `取消 / 确定`（真机 `确定 / 取消 / 帮助` ✗）。

| # | 真机有、复刻版**缺** ✗ | 真机没有、复刻版**多** ✗ |
| --- | --- | --- |
| G1 | `子串列表` 列表框及其工具按钮排 | `主数据源` 一排按钮的呈现方式 |
| G2 | `子串选项` 组（`数据源(S):` + `显示数据(D):` + 放大镜） | `常量内容` 里直接铺一张转义码表（真机没有这个表） |
| G3 | `变量共享名称(N):` + `源自其它变量` | `附加数据源（子串）` 这个分组的呈现 |
| G4 | `高级选项(A)...` 按钮 | —— |
| G5 | `示例` 列表框 | —— |
| G6 | 底排 `帮助` 按钮、按钮顺序 | —— |

**建议**：这一页**不是小改** ✗，是**交互模型的选择** ✓ —— 请先决策：
(a) 按真机改成「子串列表 + 子串选项 + 示例」的模型 ✓（工作量大但最贴近"复刻" ✓）；
(b) 或保留现有模型、但在台账里明确登记为「**复刻版扩展/不同实现**」✓ 并说明理由 ✓。
**不要**既不解释也不对齐 ✗ —— 因为这是**每个对象都要用的核心对话框** ✓。

## F. 对象属性「常规」页（round-184 新比，**结构差异较大** ✗）

证据图：`parity/review/cmp-propsgeneral-381a31e.png` ✓（左真机 `r88-textprops-p4.png` × 右复刻版 commit 381a31e ✓，两侧同为**文字**对象 ✓）

| # | 元素 | 真机 ✓ | 复刻版 ✗ |
| --- | --- | --- | --- |
| F1 | `位置` 组 | 一个组里 `水平(H): 18.59 毫米` / `垂直(V): -18.79 毫米` | 拆成两列 ✗，且标签是 `X（毫米）` / `Y（毫米）` ✗（真机是"水平/垂直" ✓） |
| F2 | `对齐` 组 | `水平(W):` 下拉 + `垂直(T):` 下拉 ✓ | **整组缺失** ✗ |
| F3 | 复刻版两列上的说明 | —— | 每列都写着 `当前对象类型不支持该选项（通用机）` ✗ ← **疑似缺陷**：这明明是文字对象 ✓，不该判成"通用机"✗ |
| F4 | `对象名称标识:` + `图层:` 下拉 | ✓ 有（底部一行 ✓） | **缺失** ✗ |
| F5 | `对象附加说明(C):` | 底部独立一行 ✓ | 挪进了两列区域 ✗ |
| F6 | 复刻版多出的字段 | —— | `宽度（毫米）40` / `高度（毫米）8` ✗、`打印时可见` 复选框 ✗、`变色设置` 按钮 ✗（真机此页没有 ✓） |
| F7 | `颜色` 组 | `颜色(C): 固定颜色` + `设置颜色` 按钮 ✓ | `颜色(C): 固定颜色` ✓，但**看不到"设置颜色"按钮** ✗（真机有 ✓） |
| F8 | 底排按钮 | `确定 / 取消 / 帮助` | `取消 / 确定` ✗（顺序相反、缺 `帮助` ✗，与 E8 同一问题 ✓） |

**建议**：F1/F2/F4 是"少了真机有的结构" ✗、F6 是"多了真机没有的字段" ✗、F3 需要单独查（**是不是把文字对象误判成了通用机类型** ✗）。

## E. 对象属性「字体」页（round-183 新比，**差异最多的一页** ✗）

> ⚠️ 先说方法上的一个坑（我自己踩的 ✓）：真机对照图 `r88-textprops-p*.png` 是「**文字**属性」✓，
> 而我的抓图场景原来一律建**条码**对象 ✗ → 条码对象下"字体页本来就是空的" ✓，我一度差点把它当成缺陷 ✗。
> **已修**：`datasource`/`propsfont`/`propsgeneral` 三个场景改为建**文字**对象 ✓（`tools/parity/Capture-CloneShot.cjs`），
> 重抓后才是可比的 ✓。证据图：`parity/review/cmp-propsfont-381a31e.png` ✓（左真机 × 右复刻版 commit 381a31e）

**（本表已被下方 round-200 的更正版取代 —— E1/E2 经真机控件树复核为验收方误判 ✗，保留在此仅供追溯。）**

## E. 对象属性「字体」页（round-183 新比；**round-200 用真机控件树复核后，E1/E2 是我误判 ✗，其余成立**）

> **权威依据**：真机控件树 dump `parity/reference/labelshop/probe-60-barcode-props-tree.txt` ✓
> （它是**多页 dump**，覆盖 数据源/条码/字体/常规 四页 ✓；另有 `probe-37-text-props-tree.txt`（文字属性·数据源页）与
> `probe-44-two-objects-tree.txt` ✓）。**判"控件形态/精确文案/可见性"一律以这些 dump 为准** ✓ —— 比看截图可靠 ✓。

**round-200 更正（我 E1/E2 错了 ✗）**：
- dump 显示 `字体名称(&T):` / `字体样式(&Y):` / `大小(&P):` 旁边都是 **`ComboBox` + `ComboLBox`** ✓ ——
  即真机用的是**下拉框（展开时带列表）** ✓，**不是**列表框 ✗；我在 `r88-textprops-p2.png` 上看到的"列表"是**下拉展开态** ✓✗
  → **E1、E2 作废** ✗（那是**状态差异** ✓：真机那张图是**展开**的 ✓，复刻版是**收起**的 ✓，控件类型其实一样 ✓）。
- **E3 保留但改写** ✓：真机 `大小(&P):` 是 **ComboBox** ✓，复刻版是**数字输入框（带上下箭头）** ✗ → **控件类型确有不同** ✓。

| # | 元素 | 真机 ✓（依据 dump） | 复刻版 ✗ | 状态 |
| --- | --- | --- | --- | --- |
| ~~E1~~ | ~~字体名称是列表框~~ | `ComboBox`+`ComboLBox` ✓ | 也是下拉 ✓ | **作废（我误判）** ✗ |
| ~~E2~~ | ~~字体样式是列表框~~ | `ComboBox`+`ComboLBox` ✓ | 也是下拉 ✓ | **作废（我误判）** ✗ |
| E3 | `大小(&P):` | **ComboBox** ✓ | 数字输入框 ✗ | **保留** |
| E4 | `特殊效果` 组 | `Button`（组框）`特殊效果` ✓ + `删除线(&S)` / `下划线(&U)` / `黑底白字(&W)` 都是 `Button`（复选框）✓ | 一排按钮形态 ✗ | **保留** |
| E5 | 缩放倍数标签 | `字体宽度方向缩放倍数(&H):` ✓（dump 原文，含"方向"与 `(&H)`） | `字体宽度缩放倍数` ✗ | **保留** |
| E6 | 字间距 | `字间距(&J):` ✓（dump 原文） | `字间距` ✗ | **保留** |
| E7 | **示例** | `Button`（组框）`示例` + 内含 `Edit` ✓（dump 原文） | 整块缺失 ✗ | **保留** |
| E8 | 底排按钮 | `确定` / `取消` / `应用(&A)`（**DISABLED** ✓）/ `帮助` ✓ | `取消 / 确定` ✗（顺序反、缺 `应用` 与 `帮助`） | **保留** |
| E9 | 复刻版多出项 | —— | `打印机内建字体` 下拉 ✗ | **保留（待标注为扩展或删）** |

> ⚠️ 另记（来自 `probe-37` ✓，数据源页）：`子串选项` 与 `示例` 在真机里是 **`Button`（组框）** ✓、
  `源自其它变量` 是 `Button`（复选框）✓、放大镜 `Ω` 是 **`Button`** ✓、子串列表下有一根 **`ToolbarWindow32`**（180×30 ✓）✓ ——
> 这些都写明了"该用什么控件" ✓，做 G 类时直接照 dump 来 ✓。

## D. 已登记项（此处仅确认仍存在，不作为新差异）

- **DIFF-75**：打印机属性·端口页复刻版多出 `指令编码` 下拉 + 一段长提示 ✗（真机该页只有 `类型` + `云盒` + `设置` ✓）；
- **DIFF-71 边界**：系统设置「其它」组缺 `启动时运行模板向导` / `自动旋转输出` / `新建对象后自动打开属性页`，以及缺 `标签工作区背景颜色:` + `恢复默认` ✗
  —— 口径是"不留占位/假实现" ✓，但**真机这三项有真实行为** ✓，建议台账写明"**待补齐**" ✓，避免将来误当成已完成 ✗。

## 复跑方式（验收方）

```powershell
# 1) 从某个提交构建并抓图（隔离克隆，不碰主仓库 node_modules ✓）
powershell -File tools/parity/Shoot-FromCommit.ps1 -Sha HEAD -Scenes 'choose,custom,sysset,printerportbox,printdialog'
# 2) 与真机图并排（右标签写 commit SHA ✓）
powershell -File tools/parity/Compare-SideBySide.ps1 -Left <真机图> -Right <复刻图> -Out <并排图> -LabelLeft <真机说明> -LabelRight "复刻版 MaxLabel（commit <sha>）"
# 3) 打印链路端到端（A4 一旦落地就会转绿 ✓）
node tools/parity/Verify-PrintEndToEnd.cjs
```
