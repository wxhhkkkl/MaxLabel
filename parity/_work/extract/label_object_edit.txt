=== label_object_edit.html === TITLE: 编辑标签模板
签赋LabelShop
采用所见即所得的编辑模式，可以方便快捷的在标签模板中排入各种对象，并编辑对象的属性。
在标签模板中创建对象
使用对象工具创建对象，也可以使用拷贝粘贴的方式在标签模板上创建对象。
选中和移动对象
可以使用鼠标和键盘便捷地选中和移动标签模板中的对象。
修改对象的属性
对象的属性，可以通过双击对象，然后在属性页中进行修改。对于字体等属性，也可以直接操作工具栏进行修改。
排列标签对象
签赋LabelShop 可以便捷地对齐选中的标签对象，调整对象的前后层次、旋转对象等。
