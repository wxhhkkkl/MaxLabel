=== label_object_align.html === TITLE: 排列标签对象
排列标签中的对象实现以下功能：
将多个对象结合成一组，组中的对象可以同时移动。
提供一些方法，用来更改对象彼此间的位置或尺寸。
通常使用基本的鼠标拖动就可以安排和移动标签中的对象。然而有时使用排列菜单中提供的功能来安排标签中对象的布局会更加方便。
如果想精确定位一个对象，可以通过修改对象的常规属性中的位置属性来实现。
