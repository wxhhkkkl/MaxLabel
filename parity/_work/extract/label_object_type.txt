=== label_object_type.html === TITLE: 标签模板中的对象
标签模板中可以排入文字、条码、RFID标记、线条、图形、图片和表格等，这些称为模板上的对象。在一个模板中可以排入多个对象，每个对象的属性（如条码的码制、文字的字体、内容等）都可以修改，对象可以旋转、组合、放大缩小，还可以方便地进行对齐位置等操作。
文字对象
条码和二维码对象
RFID标记
图片对象
直线和斜线对象
矩形和圆形对象
表格对象
