=== datasource_type_time.html === TITLE: 时间类型
“时间”数据类型将系统当前的时间作变量内容，可以选择多种输出格式，并可设置时间偏移。
时间数据类型支持如下属性：
时间格式： 用于指定时间变量的输出格式
区域： 用于指定国家和地区时区
偏移： 用于指定要显示的时间与当前时间的偏移。正值指定未来的（推迟）时间，而负值指定过去的（提前）时间
特别说明：
在 数据查重 时，不可以选择日期和时间对象。因为日期和时间是递增的，软件逻辑上不会重复。
