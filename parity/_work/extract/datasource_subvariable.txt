=== datasource_subvariable.html === TITLE: 子串连接
多数情况下，标签中的对象使用单一的数据源。然而有时会希望将两个或多个数据源的信息放置到单一的条码或文字对象中。可以通过将多个数据子串结合在一起，并使它们作为一个字符串输出（这种情况叫字符串的“串联”）。
使用子串列表可以达到两个目的：
显示数据源结构
子串列表提供清晰、直观的描述，从中可以看到标签对象中有多少个子串，每个子串使用的数据源类型。每行列表左边的小图标代表子串使用的数据类型。而每行列表的右边是子串当前指定的样本数据。
编辑数据源结构
子串列表也可以对每个子串的内容进行编辑，并更改子串的顺序。每次用鼠标单击列表中的一个子串，或用上下箭头更改选定子串的位置，新近选定的子串的重要信息也显示在列表中。这样可以快速查看并编辑每个子串的现有配置。
数据源的子串连接功能实现类似以下需求：
当我们引入了数据库字段后，需要在一个数据前面加入标题，同时为了编辑方便，我们希望标题和内容位于同一个文字对象中。这些我们可以使用 签赋LabelShop
的子串连接功能，为文字对象建立多个子串。
例如我们有如下数据：
希望编辑如下格式的标签模板：
我们注意到：书名和出版社前面都加了标题文字，在签赋LabelShop中，使用多子串功能就可以实现这个需求。
另外，多子串功能还为数据共享提供了更多的便利。
