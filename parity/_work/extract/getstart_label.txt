=== getstart_label.html === TITLE: 关于标签打印
签赋LabelShop是一款标签编辑打印软件，它与普通的软件最大的差异是：签赋LabelShop需要预先设计好标签的格式，在签赋LabelShop中定义好标签在页面上的布局，包括单个标签的宽度、高度；标签的行数、列数、间隔等参数。然后针对一个标签进行模板设计，在打印输出时，签赋LabelShop会自动将标签按设计好的格式排列在页面上。
设计好的标签格式：
对一个标签的内容模板进行编辑：
打印时设置好需要输出的标签的数量：
签赋LabelShop
会自动按预先设计好的格式，将标签打印在整个页面上。
当然，还可以打印内容变化的标签，例如序列号标签等。但需要注意的是，签赋LabelShop
支持每个标签的文字和条码的内容可以变化，甚至于图片的内容也可以不同，但是每个标签上的布局必须是一致的。
一个按顺序变化的标签（序列号）样例：
