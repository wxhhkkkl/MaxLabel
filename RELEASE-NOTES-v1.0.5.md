# MaxLabel v1.0.5 发布说明（打印机属性「工具」页 + 串行端口 5 项参数）

**基线**：v1.0.4（2026-09-20）
**本次类型**：真机对照缺陷修复（打印机链路第三批）
**日期**：2026-09-20

---

## 一、打印机属性对话框补齐第 4 个页签「工具」（DIFF-40）

真机 `Gprinter GPL-N (203 dpi) 属性` 的页签是 **首选项 / 端口 / 自定义命令 / 工具**；复刻版此前只有前三个。本轮补齐：

- 分组「常用」
- `操作：` 下拉 2 项：**发送打印机命令** / **发送文件到打印机**
- `执行` 按钮 + 下方输出区
- 「发送打印机命令」把输入的命令按当前端口直接发给打印机（走既有 `print:command`）；
- 「发送文件到打印机」把磁盘文件原样发给打印机（新增 IPC `command:send-file`，单文件上限 16 MB）

真机行为对照：真机点「执行」在发送文件模式下会弹 Windows「打开」对话框；复刻版提供「文件」输入框 + 「选择文件…」按钮（同样走系统对话框），便于在无法注入鼠标的回归环境里验证。

## 二、串行端口(COM) 的 5 项参数（DIFF-40）

真机「端口」页类型=串行端口(COM) 的参数量为 5 项，复刻版此前只有「波特率」（且默认 115200、串口发送固定 None/8/One）：

| 参数 | 选项 | 真机默认 |
| --- | --- | --- |
| 速率(B) | 1200 … 2000000（15 档） | **9600** |
| 数据位(D) | 7 / 8 | 8 |
| 奇偶检验(P) | 无 / 奇 / 偶 / 标志 / 空格 | 无 |
| 停止位(S) | 1 / 1.5 / 2 | 1 |
| 流控制(F) | 无 / 硬件（RTS/CTS）/ 软件（XON/XOFF） | 无 |

`writeSerialWindows()` 现在按这 5 项构造 `SerialPort(波特率, 奇偶, 数据位, 停止位)` 与 `Handshake`；`PortConfig` 增加对应字段并在 IPC 校验层同步。

## 三、验证

| 门禁 | 结果 |
| --- | --- |
| 全量 UI 回归 | **72/72 脚本全过**（ui-v48 … ui-v121） |
| `ui-v121.cjs`（新增） | **10/10**：工具页存在、操作 2 项、执行、空命令提示、发送命令输出字节数与结果、端口显示、发送文件入口、空路径提示、不存在文件被拒绝 |
| `ui-v120.cjs` | **13/13**（新增 COM 5 项参数的选项与默认值断言） |
| `print-engine.test.ts` | 串行参数往返与非法值（数据位/停止位/流控制）断言通过 |
| 其余单元门禁 | typecheck / architecture / color / barcode / editor / geometry / history / printer 全绿 |
| `Check-Matrix.ps1` / `test:evidence` | 605/605；证据 599 行通过 |
| 未收口差异 | **0 条**（DIFF-1…40） |
| 打包版启动冒烟 | `ui-smoke.cjs` 4/4 |

## 四、产物

| 产物 | 路径 |
| --- | --- |
| 安装包 | `app/release/MaxLabel-Setup-1.0.5.exe`（129.26 MB，未签名内部测试包） |
| 免安装目录 | `app/release/win-unpacked/` |
| 校验和 | `RELEASE-SHA256.txt` |
| 真机取证 | `parity/reference/labelshop/PROBE-round107.md`（probe-16/17/18） |

## 五、下一轮待办

1. 蜂打打云盒的参数区（真机 = 云盒下拉 + 设置按钮；本机扫不到云盒）；
2. 标准 TCP/IP 的 `SysIPAddress32` 四段 IP 控件（复刻版用「主机名/IP + 端口号」，功能等价）；
3. 卷筒格式在**打印预览**里的纸张呈现；
4. 真机同时安装多台 LabelShop 打印机时的下拉排列顺序；
5. 真机「工具」页命令为空时无提示（复刻版给了提示，属易用性增强）。
