# MaxLabel v1.0.16 发布说明（需求清单 194/194 全部取证完毕 + 取证暴露的缺口收口）

**基线**：v1.0.15（2026-09-21）
**本次类型**：需求清单收尾（数据源 55 + 数据库 19 + 授权 1）+ 缺口修复
**日期**：2026-09-21

---

## 一、需求清单 **194/194 全部填完**

| 批次 | 条数 | 轮次 |
| --- | --- | --- |
| 标签格式 | 13 | round-112 |
| 打印机属性 | 6 | round-113 |
| 系统选项 | 6 | round-113 |
| 对象编辑 | 15 | round-114 |
| 打印和预览 | 15 | round-114 |
| 其它 | 8 | round-115 |
| 对象属性 | 56 | round-57 / round-58 |
| **数据源** | **55** | **round-59** |
| **数据库** | **19** | **round-59** |
| **授权** | **1** | **round-59** |

本批结论都写了「原版有 / 原版无 / 原版有但受限」三态，并附可复现证据（原版自带帮助页名 / 真机控件 dump / 复刻版回归脚本）。
其中：**数据源** 55 条以帮助 `label_object_page_data*.html` + `datasource_*.html` 各类型专页为原版判据，
配合真机数据源页控件（7 种数据源类型、子串列表 + 工具栏、显示数据、变量共享名称、高级选项）；
**数据库** 19 条以 `database_import_text/excel.html`、`database_print_search.html`、`database_main.html` 为判据；
**授权** 1 条（本地授权/加密狗）按真机 6.39 只走在线激活取证，与 E 区「硬件锁激活」边界项同一处置。

## 二、本轮修掉的缺口（取证时发现）

| 项 | 修法 |
| --- | --- |
| 序列号「序列」只读显示（帮助明文有，复刻版缺） | 新增 `serial-sequence`：随「类型」显示 0-9 / A-Z / 0-9A-Z / 0-9A-F |
| 汉信码「版本」（真机 85 项，复刻版只有 4 项） | 补到 85 项（自动 + 1…84）并透传 bwip `version` |
| 条码「缩减量」（.lsdx 有 `reduction`，复刻版只有导出对话框有） | `BarcodeObj.reductionMm` + EAN/UPC 条码页「缩减量（毫米）」+ 画布/打印透传（DIFF-57 收口） |
| 序列号「归位」 | 复刻版**一直有**（`resetEachRecord` + 按记录基准复位）——本轮纠正结论，不再记为缺口 |

**新登记（DIFF-62，未收口）**：① 共享变量名称的「下拉选已有名」（真机是组合框，复刻版只有输入框）；
② 截短「保留」的整数/小数部分；③ 序列号「边界值」（帮助未记载、真机本轮未读到，待再取证）。

## 三、验证

| 门禁 | 结果 |
| --- | --- |
| `ui-v127.cjs`（新增，本轮四项修复） | **5/5** |
| 全量 UI 回归（78 个脚本） | **ALL SCRIPTS PASSED (78/78)**，exit 0 |
| `test:barcode` / `test:render` / `test:print` / `test:editor` / `test:geometry` / `test:history` / `test:color` / `test:printer` / `test:workspace` / `typecheck` / `test:evidence` | 全绿 |
| `Check-Matrix.ps1` | 605/605 校验通过 |
| 需求清单 | **194/194（待填 0 条）** |

## 四、产物

| 产物 | 路径 |
| --- | --- |
| 安装包 | `app/release/MaxLabel-Setup-1.0.16.exe` |
| 校验和 | `RELEASE-SHA256.txt` |
| 清单队列 | `parity/需求清单-待验证队列.md`（194/194） |

## 五、之后还剩什么

1. **DIFF-62 的两处半**（共享名下拉、截短整数/小数部分）；
2. 对象编辑三处真机待确认（CTRL+拖动是否复制、拖动有无对齐参考线、点空心矩形内部是否选中）；
3. 打印机链路两处待用户配合（装佳博官方驱动验 USB「有队列」发送、移除真机残留的 `TSC TSPL-N`）；
4. E 区边界 5 条（硬件锁/专业版演示/三版本分层/起始页服务端图文/内置驱动不支持预览）。
