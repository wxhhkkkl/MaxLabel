# MaxLabel v1.0.6 发布说明（内置驱动禁用打印预览 + 云盒参数区）

**基线**：v1.0.5（2026-09-20）
**本次类型**：真机对照缺陷修复（打印机链路第四批）
**日期**：2026-09-20

---

## 一、内置驱动文档下「打印预览」应当禁用（DIFF-41）

真机上只差一个变量——文档绑定的打印机：

| 文档 | 打印机 | 文件菜单 `打印预览(V)` |
| --- | --- | --- |
| 卷筒 `[602001] 100mm x 150mm 单列 320签/卷` | `Gprinter GPL-N (203 dpi)`（签赋LabelShop 打印机 = 内置驱动） | **灰（禁用）** |
| 平张 `[608053]` | `Microsoft Print to PDF`（Windows 驱动端口） | **可用** |

这与帮助 `print_preview.html`「LabelShop 打印机内置驱动不支持打印预览」一致。此前台账（DIFF-23）把这条判为「复刻版没有内置驱动端口，限制客观不存在」，但 v1.0.3 引入「安装 LabelShop 打印机」之后复刻版也有了内置驱动打印机，限制必须补上。

**修复**：`isLabelShopBuiltInPrinter()`（文档 `printerName` 命中已安装的 LabelShop 打印机）命中时——

- 文件菜单 `打印预览(V)` 禁用；
- 工具栏「打印预览」按钮禁用，title 显示「打印预览（LabelShop 打印机内置驱动不支持打印预览）」；
- 打印对话框「预览」按钮禁用，title 显示原因；
- `handlePreview()` 兜底：状态栏提示「LabelShop 打印机内置驱动不支持打印预览，请改用「打印」或选择 Windows 打印机驱动端口」。

## 二、蜂打打云盒参数区形态对齐

真机「类型 = 蜂打打云盒」的参数区是 `云盒：` 下拉 + `设置` 按钮（本机扫不到云盒，下拉为空）。复刻版改为同样形态：下拉恒为「未检测到云盒」（没有云盒发现协议），点「设置」展开「云盒地址 + 端口」两个输入框作为补充能力。

## 三、验证

| 门禁 | 结果 |
| --- | --- |
| 全量 UI 回归 | **72/72 脚本全过** |
| `ui-v119.cjs` | **32/32**（新增 3 条：内置驱动文档文件菜单预览禁用、打印对话框预览按钮禁用且 title 含原因、Windows 驱动端口文档预览可用） |
| `ui-v120.cjs` | **14/14**（云盒参数区形态 + 点「设置」展开地址/端口） |
| 其余单元门禁 | typecheck / architecture / color / barcode / editor / geometry / history / printer / print 全绿 |
| `Check-Matrix.ps1` / `test:evidence` | 605/605；证据 599 行通过 |
| 未收口差异 | **0 条**（DIFF-1…41） |
| 打包版启动冒烟 | `ui-smoke.cjs` 4/4 |

## 四、产物

| 产物 | 路径 |
| --- | --- |
| 安装包 | `app/release/MaxLabel-Setup-1.0.6.exe`（129.26 MB，未签名内部测试包） |
| 免安装目录 | `app/release/win-unpacked/` |
| 校验和 | `RELEASE-SHA256.txt` |
| 真机取证 | `parity/reference/labelshop/PROBE-round108.md`（probe-19/20） |

## 五、下一轮待办

1. **USB 端口原生指令输出**：Windows 不把 `USB001` 暴露成可写设备路径（实测 `\\.\USB001`、`\\.\USBPRINT\…` 都打不开），真机靠内置驱动写 USBPRINT；复刻版目前 USB 端口返回「请使用系统打印驱动或映射为串口」，已安装打印机的默认端口仍是「指令文件」（可另存 .prn）。若要真正支持，需要走 Windows 打印后台（spooler）的 raw 写入，且要求存在打印队列。
2. 标准 TCP/IP 的 `SysIPAddress32` 四段 IP 控件（复刻版用「主机名/IP + 端口号」，功能等价）。
3. 真机同时安装多台 LabelShop 打印机时的下拉排列顺序。
4. 卷筒格式在**打印预览**里的纸张呈现（须先把文档打印机换成 Windows 驱动端口，内置驱动下预览本就不可用）。
