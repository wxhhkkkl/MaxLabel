# MaxLabel v1.0.9 发布说明（TCP/IP 四段 IP 控件 + 打印机链路对照文档）

**基线**：v1.0.8（2026-09-21）
**本次类型**：真机形态对齐（打印机属性端口页最后一项差异）
**日期**：2026-09-21

---

## 一、TCP/IP 端口改为真机的四段 IP 控件（DIFF-44）

真机「端口」页类型 = `标准 TCP/IP 打印机端口` 时，参数区是 **`SysIPAddress32` 四段 IP 输入 + 端口号 + `设置` 按钮**；
复刻版此前是单个「主机名/IP」文本框（功能等价，形态不同，台账 D-25 记为等价替代）。

现在复刻版：

- 四段 IP 输入（`printer-port-ip-1..4`，每段限 3 位并夹到 0–255，合成 `tcpHost`）+ 端口号（默认 9100）；
- 追加「按主机名填写 / 按 IP 填写」切换：既有配置若是主机名（`printer.local` 之类）仍走主机名输入框，
  **保留主机名兼容**（真机控件只收 IP，复刻版是超集）。

## 二、新增《打印机链路复刻对照》文档

`parity/打印机链路-复刻对照.md`：把用户实测的三点反馈（① 安装逻辑完全不同 ② 装完删不掉 ③ 卷筒标签展示不同）
逐条映射到「真机行为 / 修复前 / 现在 / 证据 / 回归脚本」，并列出：

- 打印机「属性」对话框四个页签（首选项 / 端口 / 自定义命令 / 工具）的对齐情况；
- 已知能力边界（USB 原生输出需打印队列、导出指令文件恒禁用、工具页空命令提示差异、多台打印机的卸载需鼠标操作）；
- 待用户在真机上配合的两件事（装厂商驱动验证「有队列」分支；需求清单 194 条待验证队列）。

## 三、验证

| 门禁 | 结果 |
| --- | --- |
| 全量 UI 回归 | **72/72 脚本全过** |
| `ui-v69.cjs` | **10/10**（四段 IP 存在、端口默认 9100、段值 999 夹到 255、四段填满后可保存） |
| `ui-v121.cjs` | **12/12**（用四段 IP 填 127.0.0.1 走通「工具」页发送链路） |
| 其余单元门禁 | typecheck / architecture / color / barcode / editor / geometry / history / printer / print 全绿 |
| `Check-Matrix.ps1` / `test:evidence` | 605/605；证据 599 行通过 |
| 未收口差异 | **0 条**（DIFF-1…44） |

## 四、产物

| 产物 | 路径 |
| --- | --- |
| 安装包 | `app/release/MaxLabel-Setup-1.0.9.exe` |
| 免安装目录 | `app/release/win-unpacked/` |
| 校验和 | `RELEASE-SHA256.txt` |
| 打印机链路对照 | `parity/打印机链路-复刻对照.md` |

## 五、下一步

打印机链路与用户三点反馈已全部收口（未收口差异 0）。后续两项需要用户配合：

1. 装上佳博 GP-1324D 的官方 Windows 驱动（建出打印队列）→ 实测复刻版 USB 端口的 raw 发送；
2. 需求清单 194 条待验证队列——先与清单作者对齐「×」的语义，再逐条真机取证。
