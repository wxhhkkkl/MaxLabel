# MaxLabel v1.0.19 发布说明

发布日期：2026-09-21
产物：`app/release/MaxLabel-Setup-1.0.19.exe`（129.27 MB）
SHA256：`B2BB1E014D982E7C2CB19D2DDE9E5060E4D3C0D1413BD0A351E5A5F0D4A48271`

## 用户可见修复

### 1. 新建条码显示为一整块黑（已修）

**现象**：新建一个条码对象后，画布上是一整块黑色矩形，看不到条码。

**根因**：生成条码图片时用 `backgroundcolor: 'FFFFFF00'` 表达"透明底"，而 **bwip-js 不接受 8 位带 alpha 的颜色**，
会把整张图渲染成 100% 黑；新建条码默认 `backgroundTransparent: true`，因此**每个新建条码都全黑**。

**修复**：透明底时**不再传** `backgroundcolor`（bwip-js 会保留画布透明底）。实测：

| `backgroundcolor` | 透明像素 | 暗像素 | 结果 |
| --- | --- | --- | --- |
| `FFFFFF00`（修复前） | 0.0% | 100.0% | 全黑 |
| 不传（修复后） | 53.2% | 46.8% | 透明底 + 条码正常 |

**防复发**：`render-regression` 新增 2 条断言（暗像素占比 20%–80% + 透明 >20%，已验证"改回旧写法即红"，
渲染检查总数 64 → 66）；另有界面层工装 `tools/parity/Verify-BarcodeRender.cjs`（按画布实际缩放裁对象区域、
数水平中线黑↔白跳变，≥10 才算条码）。证据：`parity/reference/maxlabel/PROBE-verifier-round63-barcode-black.md`。

## 本次一并包含（本批 round-104…112 的成果）

- **对象属性对话框页签按真机 LabelShop V6.39 对齐（DIFF-64 收口）**：文字=`数据源/字体/文本/常规`、
  条码=`数据源/条码/字体/常规`（**删掉自造的「码制专页」**，码制专属项并入条码页的「条码特殊选项」分组）、
  矩形/椭圆/直线=`图形/常规`、表格=`表格/常规`、图片=`图片/常规`；打开属性页默认停在对象专属页。
- **「选择标签格式」自定义入口与圆角规则（DIFF-66 / 用户反馈）**：`标签名称` 下拉不再有「自定义」项，
  `自定义(N)` 打开独立「标签格式设置」（四页签）；**圆角半径固定 1mm**（真机实测 0.9–1.1mm），
  真机没有「圆角半径」字段故移除；`形状` 首项改真机原文 **`方角矩形`**；`孔洞` 三项 `无/圆洞/矩形`；
  `应用(A)` 按真机**隐藏**；预览行按真机 `100.00 x 70.00 毫米 [4行 2列]`。
- **「孔洞 = 矩形」贯通修复（DIFF-69）**：此前只有对话框预览认它；现在孔形经 `paperHoleFields` 单一来源
  贯通到文档归一化、打印场景、编辑器裁剪，重开对话框也能回填。
- **「系统设置」按真机对齐（DIFF-71 第一步）**：窗口标题改 `系统设置`（菜单项仍叫「系统选项(C)...」，与真机一致）、
  常规页四个分组框（语言/单位/非打印对象/其它）、字段按真机原文（`界面语言(L):`、`标尺单位(U):`、
  `输出非打印对象(P)`、`不选中非打印对象(N)`、`允许运行脚本(S)` …）、底排 `确定/取消/帮助`
  （真机的 `应用(&A)` 是隐藏控件）。

## 未包含（仍待取证，未凭猜测改动）

- 真机「工具」菜单没有 RFID 创建入口，复刻版 RFID 对象与页签维持现状（DIFF-65 未收口）。
- 「系统设置」的「编辑」「系统」两页尚未实现；复刻版多出的「标签」页处置方案待定（DIFF-71 剩余部分）。
- **打印输出里有没有孔**仍未取证：真机打印到 PDF 的产出是**空白页**（A4、无绘制算子），需先让输出非空（DIFF-70）。
- 停靠式属性面板（`PropertyPanel.tsx`）页面名未改 —— 真机该界面尚未取证。
- 「条码」页字段名（`码  高(&H):` 等加速键与命名）待按 `probe-45-barcode-props-p3.txt` 对齐。

## 验证

- `npm run typecheck` / `npm test`（typecheck · architecture 7 · color · barcode · editor 40 · geometry · history 9 ·
  printer · print 110 · **render 66** · workspace）**全部 exit 0**；`npm run build` exit 0。
- 驱动器独立门禁 **round-112：全部通过**，`test:ui` **82/82**（1780s），`parity:matrix` 605/605。
- 验收方独立工装：`Verify-BarcodeRender` PASS（中线跳变 50）、`Verify-LabelFormatDialog` **17/17**、
  `Verify-ObjectTabs` 4/4（文字/条码/矩形/表格）。
