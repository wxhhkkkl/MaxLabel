# 夜间连续作业计划（用户指令：继续不要停，做到 2026-09-23 08:00）

开始：2026-09-22 18:55。当前基线：round 129 / `lastGoodSha=8457c67` / `consecutiveFail=0` / P4 行级四件套齐 39、界面级 **15/30** / v1.0.19 已发布、**v1.0.20 待打包**。

## 一、两条线并行

**A. 复刻循环（codex 优先，额度耗尽自动切 claude）** —— 由 `tools/loop/Start-Loop-Auto.ps1` 后台驱动，我负责盯：
- 额度事件 → 切换/冷却（**claude 永不冷却**；codex 首次只冷却 20 分钟，秒退式再触限才翻倍 ✓）；
- 门禁红 → 取失败清单、**替跑**单脚本、把失败断言原文写进 `round-focus.md`；
- 秒退/停机/回滚 → 立刻处理或上报（回滚前要求打备份分支 ✓）。

**B. 验收方自己的工作**（每轮挑能做的做，不空转）：
1. **P4**：按 `parity/P4-界面级覆盖表.md` 补"只缺并排图"的界面 → 出图（`New-ParityShot.ps1`）→ 补引用（`cite-parity-shots.cjs`）→ 复核 `Check-Matrix` + `check-evidence-files`；
2. **发布**：窗口出现（无 electron + 产品源码干净）就跑 `Release-Clone.ps1 -Version 1.0.20`，自算 SHA256 填进 `RELEASE-NOTES-v1.0.20.md`，提交推送；
3. **独立验证**：每轮复核循环的声明（断言是否实跑 ✓、台账是否诚实 ✓、并排图是否同态 ✓）；
4. **工装维护**：场景/引用脚本/覆盖表的修补与校准（含我自己的踩坑记录 ✓）。

## 二、硬性纪律（今天踩过的坑换来的）

| 纪律 | 原因 |
| --- | --- |
| 门禁跑 UI 期间**绝不起 electron**（出图/验证前先看 `Get-Process electron`） | 曾因抢资源把 ui-v116 搞成"抖动失败" ✗ |
| 打包前查**产品源码**是否干净（不要求整个工作区干净） | 台账在途不该阻塞发布，但也**绝不能**打出半成品产品树 |
| 并排图必须**同态**（两边同一状态） | 两次"假差异"（菜单缺 6 项、端口页缺字段）都是状态不同造成的 |
| 引用证据只写"图里确实看得到什么"，允许标"部分覆盖" | 否则覆盖率是假的 |
| 引用前校验行 id 是否真实存在 | 我今天拍脑袋写的一批 id 全错 ✗ |
| 真机路径必须带 `labelshop/` 前缀；复刻路径带 `reference/maxlabel/` | 普查按前缀判定 |
| 每个结论要么有实测证据，要么登记"待取证" | 不许用帮助文字反推实现 |

## 三、08:00 时应该交付什么

1. **v1.0.20 已打包发布**（含 RELEASE-NOTES + 自算 SHA256 登记）；
2. **P4 界面级四件套齐**尽可能高（当前 15/30，目标 ≥20）；
3. **未收口差异**仍 ≤6 条且每条都有明确状态（已修/待取证/待决策）；
4. 每轮门禁结论可查（`tools/loop/logs/round-*-gates.md`），红线不积压；
5. 一份**晨间摘要**：这一夜发生了什么、验证了什么、还剩什么。
