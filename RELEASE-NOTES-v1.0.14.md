# MaxLabel v1.0.14 发布说明（需求清单「对象属性」首批 18 条取证 + 四处对象属性形态对齐）

**基线**：v1.0.13（2026-09-21）
**本次类型**：需求清单逐条取证 + 属性页形态对齐
**日期**：2026-09-21

---

## 一、需求清单新增 18 条结论（累计 **81/194**）

| 行 | 条目 | 原版结论（真机读值） | 复刻版 |
| --- | --- | --- | --- |
| 63 | 常规 旋转 | 4 项 0°/90°/180°/270° | 对齐 |
| 64 | 常规 镜像 | **3 项**（无/水平镜像/垂直镜像） | 本轮到齐（DIFF-56） |
| 65 | 常规 背景 | 2 项，**默认透明** | 默认值对齐 |
| 66 | 常规 位置锁定 | 有（不可移动/属性页禁用/组合后失去） | 对齐（round-114 DIFF-48/49） |
| 67 | 常规 不打印输出 | 有 | 对齐 |
| 68 | 常规 对象附加说明 | 有 | 对齐 |
| 72 | 字体 字体大小 | **31 项**（8…72 + 初号(42)…七号(5)） | 本轮到齐（DIFF-58） |
| 76 | 字体 宽度缩放倍数 | 有（默认 1.00） | 对齐 |
| 77 | 字体 字间距 | 有（默认 0.00） | 对齐 |
| 79 | 字体 示例 | 有「示例」预览组 | 本轮补上（DIFF-58） |
| 115 | 条码 X 尺寸 | **61 档 mil 下拉**（1.67 步长） | 等价数值框（单位/默认值一致） |
| 116 | 条码 码高 | 数值框（默认 10.00 毫米） | 本轮补上（DIFF-59） |
| 117 | 条码 条宽比 | **7 档** 2.00–3.00 | 本轮到齐（DIFF-59） |
| 118 | 条码 缩减量 | EAN/UPC 下有 | **缺口**，记为 DIFF-57 待补 |
| 119 | 供人识读字符 位置 | 4 项（默认/无/条码上方/条码下方） | 本轮补上（DIFF-59） |
| 120 | 供人识读字符 垂直偏移 | 有（默认 0.00） | 本轮补上（DIFF-59） |
| 121 | 供人识读字符 对齐方式 | 4 项（左齐/右齐/居中/撑满） | 本轮补上（DIFF-59） |
| 122 | 条码 字符模板 | 有 | 对齐 |

## 二、对照修复

| 编号 | 问题 | 修法 |
| --- | --- | --- |
| DIFF-50（收口） | 「水平/垂直位置（相对标签边对齐）」在真机**按对象类型**启用：条码 3 项（左齐/居中/右齐、顶部/居中/底部），文字 **0 项且禁用** | 文字对象下不渲染选项并禁用；选项文案改成真机的「左齐/居中/右齐」 |
| DIFF-55 | 码制下拉只有 18 项、名称无空格、缺 **Pharmacode** 与 **Micro QR**、顺序错 | 按真机 20 项重建（名称/顺序逐项一致），补两个码制的特性条目 |
| DIFF-56 | 镜像多一项「水平+垂直镜像」（真机 3 项） | 删掉该选项 |
| DIFF-58 | 字号下拉是纯数字（缺中文号数，真机 31 项）、字体页缺「示例」预览 | 31 项按真机重建；补 `text-font-preview`；去掉与「字体样式」重复的粗体/斜体按钮 |
| DIFF-59 | 条码页缺「码 高」「供人识读字符（位置/垂直偏移/对齐方式）」；条宽比只有 3 档 | 补齐四组字段（含模型联合类型 `humanPosition`/`humanAlign` 扩展）；条宽比 7 档 |
| DIFF-57 | ⏳ 未收口：条码页缺「缩减量」（导出对话框有，属性页没有，画布/打印未透传） | 下一轮补 |

## 三、验证

| 门禁 | 结果 |
| --- | --- |
| `ui-v125.cjs`（新增，对象属性首批 18 条） | **17/17** |
| 全量 UI 回归（76 个脚本） | **ALL SCRIPTS PASSED (76/76)**，exit 0 |
| `barcode-spec.test.ts` | 20 项码制名称/顺序与真机逐项一致 |
| `test:render` / `test:print` | 54 / 110 项通过（背景默认改「透明」后无回归） |
| 其余单元门禁 | typecheck / architecture / editor(36) / geometry / history / color / printer / workspace |
| `Check-Matrix.ps1` / `test:evidence` | 605/605；证据校验通过 |
| 未收口差异 | **1 条**：DIFF-57（缩减量字段），已登记待补 |

## 四、产物

| 产物 | 路径 |
| --- | --- |
| 安装包 | `app/release/MaxLabel-Setup-1.0.14.exe` |
| 校验和 | `RELEASE-SHA256.txt` |
| 清单队列 | `parity/需求清单-待验证队列.md`（81/194 已填） |

## 五、清单进度与下一步

- 已填 **81/194**：标签格式 13 + 打印机属性 6 + 系统选项 6 + 对象编辑 15 + 打印和预览 15 + 其它 8 + 对象属性 18；
- 余 **113 条**：对象属性 38 / 数据源 55 / 数据库 19 / 授权 1。
- 下一轮：**对象属性余下 38 条**（图片属性 6、文本属性 11、条码码制专属 20、汉信码 3 等）——真机链路已通
  （选工具 → `postdrag` 拖出对象 → `Alt+Enter` → `Ctrl+Tab` 翻页 → `Probe-LabelShopCombos`/`Read-LabelShopDialogValues` 读值），
  顺带补 DIFF-57（缩减量）。
