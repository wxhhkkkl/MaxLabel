# MaxLabel v1.0.20 — 发布说明（草稿，打包后补 SHA256）

> 本版把 v1.0.19 之后的三处**用户可见界面差异**按真机实拍对齐，并清掉一处重复控件。
> 全部改动都有真机证据与自动断言；门禁：`typecheck` + 7 套单元门禁 + 全量 `test:ui`（见下方"门禁"一节）。

## 一、系统设置（原「系统选项」）—— 按真机 4 页结构重排（DIFF-71）

| 项 | 变化 |
| --- | --- |
| 页签 | `常规 / 打印和数据库 / 编辑 / 系统`（真机 4 页，顺序与文案逐字） |
| 「标签」页 | 复刻版自造页**已删除**；其 6 项设置（默认标签尺寸/排列/行列间隔/外观形状/显示标尺/显示网格）与「云服务器地址」一起移入各页底部的 **「复刻版扩展」** 区（虚线框 + 图例「复刻版扩展（原版系统设置中无此项）」）——**没有静默删功能** |
| 「编辑」页（新增） | `表格操作` 分组 + `增删行列时，保持表格尺寸`，并接真实行为（作为新建表格对象的全局默认） |
| 「系统」页（新增） | `系统操作` 分组 + `恢复默认窗体布局`（重置工具栏分组与逐按钮布局，确定后生效） |
| 底排按钮 | 按真机重排为 **`确定 / 取消 / 帮助`**（`帮助` 打开帮助主题） |
| `标尺单位(U):` | 选项文案改回真机原文 **`毫米` / `英寸`**（原先误写成「毫米（公制）/英寸（英制）」） |
| 未渲染项 | 真机另有 `应用(&A)`（隐藏控件）与 6 组复刻版暂无对应行为的选项（`禁用鼠标拖动复制功能` 等）——按"不留 TODO 占位/假实现"的口径**不渲染**，逐条登记在 `parity/diffs.md` DIFF-71「仍未收口」 |

## 二、条码属性「条码」页 —— 字段原文与颜色控件（DIFF-72）

| 项 | 变化 |
| --- | --- |
| 字段原文 | `条码符号类型(码制)(&B):` / `X 尺寸(&X):` / `码  高(&H):`（两个空格）/ `垂直偏移(&O):` / `对齐方式(&A):` 逐字对齐真机 |
| 颜色控件 | **放回「条码」页页尾**（`颜色:` + 色块 + 下拉）——真机实拍该页页尾确有颜色控件；v1.0.19 曾据控件树文本 dump 判"本页无颜色"，属**误判**（owner-drawn 色块枚举不到），本版按实拍更正 |
| 「常规」页 | `颜色(&C):` 保留为**颜色模式**下拉（真机本机值 `固定颜色`），与条码页色块不是同一个控件；并清掉「变色设置」里重复渲染的同一模式行（同页只剩一处模式入口） |

## 三、「标签格式设置」预览 —— 改为整张拼版网格（DIFF-73）

- 预览原先 `viewBox` 只有一个标签 → 只画一格；现按 `列数 × 行数` 画**整张网格**：
  每格正中序号 1..N（先行后列）、`列距/行距` 参与间距、孔洞画在**每格中心**、尺寸标注只在**第一个**格子；
  公式与页面尺寸计算同源（不再写第二份）。
- 预览底色改为透明（真机预览区就是对话框底色，与「选择标签格式」一致）。

## 四、门禁与验证

- 单元门禁：`typecheck` / `architecture` / `editor` / `geometry` / `history` / `print` / `render`（66 项）/ `workspace` / `build` 全通过；
- 全量 `test:ui`：见 `tools/loop/logs/round-116-gates.md`（本版对应的那一轮）；
- 验收方独立工装（不占循环门禁）：`Verify-LabelFormatDialog`（P0 ①–⑪）、`Verify-ObjectTabs`（对象属性页签 5/5）、`Verify-BarcodePage`（DIFF-72 4/4）、`Verify-SystemOptions`（DIFF-71 6/6）、`Verify-BarcodeRender`（条码非黑块）；
- 当轮构建并排图：`parity/review/cmp-*.png`（真机 × 复刻版同界面左右对照）。

## 五、已知未收口（与 v1.0.19 相同口径，如实列出）

- **RFID 对象入口**（真机工具菜单与数据源下拉均无）→ 待决定"删工具 or 登记为复刻版扩展"；
- **打印机页开关形态 / 打印机端口页**：部分需真机补拍（`D-22/23/28/34/35`）；
- **DIFF-70**：打印输出里"孔在不在"仍未取到（现有链路产出为空白 A4 PDF，无绘制算子）；
- **DIFF-50 / DIFF-60**：两条观察项，收口前需明确"已记录边界"口径；
- **DIFF-71 未渲染的 6 组真机选项**（见第一节末行）；
- **界面语言**：真机 3 项（简体中文/繁体中文/English），复刻版仅实现简体中文 → 记为「原版有但受限」。

---

SHA256：<!-- 打包后由验收方核算并填入 -->
文件：`app/release/MaxLabel-Setup-1.0.20.exe`
