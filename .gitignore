# Dependencies
node_modules/
**/node_modules/

# Build outputs
app/out/
app/release/
app/dist/
server/dist/
server/build/
server/static/
server/frontend/dist/
server/__pycache__/
**/__pycache__/
*.pyc
*.pyo

# Environment / secrets
.env
.env.local
.env.*.local
server/.env
server/data/*.db
server/data/*.db-*

# Logs
*.log
npm-debug.log*

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Original software reference materials (copyright, not code)
software/
_chm_zh/

# CHM extracted text dumps
_chm_clean_*.txt

# UI observation screenshots (keep .md docs)
_ui_observation/*.png
_ui_observation/*.jpg
app/_v_*.png

# Temp scripts
_montage.ps1
app/scripts/_*.cjs

# Python venv
.venv/
venv/
env/

# Electron builder cache
app/release/

# Parity 循环的临时/中间产物（证据与清单保留）
parity/_rows/
parity/_extract/
parity/_work/
parity/reference/*/_*
# 并排对照图（`cmp-<界面>-<轮次>.png`）是「四件套」证据的一部分，**必须入库**（round-62 验收方发现：
# 原先整目录被忽略 → 证据只在本地、换台机器就没了）。其余历史/临时图仍忽略。
parity/review/*
!parity/review/cmp-*.png
app/.release-staging-current/
tools/loop/logs/
tools/loop/state.json
tools/loop/HALT

# ����˱������ļ�
server/data/*.lock

# 循环运行期标记（验收方/工装用，不应入库）
tools/loop/STOP
tools/loop/PAUSE
tools/loop/FORCE-UI
tools/loop/.last-report
