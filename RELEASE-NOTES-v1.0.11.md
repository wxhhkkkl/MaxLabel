# MaxLabel v1.0.11 发布说明（需求清单：打印机属性 6 条 + 系统选项 6 条 + 系统选项两处修复）

**基线**：v1.0.10（2026-09-21）
**本次类型**：需求清单逐条取证 + 对照修复
**日期**：2026-09-21

---

## 一、需求清单新增 12 条结论（累计 **25/194**）

| 分类 | 条数 | 结论要点 |
| --- | --- | --- |
| 打印机属性相关（行 6–11） | 6 | 原版**全部有**：标签格式设置 →「打印机」页有「输出方式:」（可切指令方式）；「打印机属性 → 首选项」含 打印速度(1–6) / 打印浓度 / 打印方式（打印机默认·热敏·热转印）/ 标签类型（默认·连续纸·间隔定位·标记定位）/ 介质处理（撕纸·剥离·切纸）/ 出纸回退 / 顶部偏移 / 打印后处理命令。复刻版对应字段齐全，并在指令侧生效（TSPL/ZPL/CPCL 的 SPEED/DENSITY 等，`print-engine` 测试覆盖）。 |
| 系统选项（行 262–267） | 6 | 原版**全部有**：系统设置 → 常规 的 界面语言 / 标尺单位 / 输出非打印对象 / 不选中非打印对象 / 允许运行脚本 / 标签工作区背景颜色。复刻版「系统选项 → 常规」逐项对应。 |

## 二、对照修复（DIFF-46）

1. **补「新建对象后自动打开属性页」**（真机常规页有、复刻版缺）：
   `AppOptions.autoOpenObjectProps`（默认关闭，与真机一致）+ 常规页复选框；
   勾选后，**单击创建**与**拖拽创建**两条路径都会在新建对象后自动弹出属性对话框。
2. **「系统选项」保存后关闭对话框**：原来点「保存」只写偏好、对话框不关，用户体感像没生效；
   现在保存即应用并关闭（与真机 `确定` 行为一致），按钮加 `data-testid=options-save`。

> 顺带把 `ui-v68` 里那条「偏好持久化」断言从「读还开着的对话框」改成**读 localStorage**（原来即使没保存也会通过），并补了「重开系统选项后仍为勾选」。

## 三、验证

| 门禁 | 结果 |
| --- | --- |
| 全量 UI 回归 | **72/72 脚本全过** |
| `ui-v116.cjs` | **12/12**（新增 2 条：选项存在且默认未勾选；勾选后新建对象自动打开属性对话框） |
| `ui-v68.cjs` | **14/14**（保存后关闭；持久化断言改为 localStorage + 重开核对） |
| 其余单元门禁 | typecheck / architecture / color / barcode / editor / geometry / history / printer / print 全绿 |
| `Check-Matrix.ps1` / `test:evidence` | 605/605；证据 599 行通过 |
| 未收口差异 | **0 条**（DIFF-1…46） |

## 四、产物

| 产物 | 路径 |
| --- | --- |
| 安装包 | `app/release/MaxLabel-Setup-1.0.11.exe` |
| 免安装目录 | `app/release/win-unpacked/` |
| 校验和 | `RELEASE-SHA256.txt` |
| 清单队列 | `parity/需求清单-待验证队列.md`（25/194 已填） |

## 五、清单进度与下一步

- 已填：标签格式 13 + 打印机属性 6 + 系统选项 6 = **25/194**；
- 余 **169 条**：对象属性 56 / 数据源 55 / 数据库 19 / 对象编辑 15 / 打印和预览 15 / 其它 8 / 软件与登录授权 1。
- 下一轮计划：**对象编辑 15 → 打印和预览 15 → 其它 8**（这三类有真机截图与既有回归打底，取证快），再进入量最大的 对象属性 56 与 数据源 55。
