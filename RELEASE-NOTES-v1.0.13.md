# MaxLabel v1.0.13 发布说明（需求清单「其它」8 条取证 + 「关于」对话框按真机重做）

**基线**：v1.0.12（2026-09-21）
**本次类型**：需求清单逐条取证 + 界面结构对齐
**日期**：2026-09-21

---

## 一、需求清单新增 8 条结论（累计 **63/194**）

| 行 | 条目 | 原版结论 | 复刻版 |
| --- | --- | --- | --- |
| 270 | 文件操作 分享 | 原版有（文件菜单「分享(I)...」，未登录置灰；位置紧随「另存为」） | 对齐（`ui-v124`） |
| 271 | UNDO/REDO | 原版有（Ctrl+Z 撤消 / Ctrl+Y 重做） | 对齐（`ui-v124` + `document-history.test.ts`） |
| 272 | 显示 标签旋转 | 原版有（正常显示/左旋90度/右旋90度/旋转180度） | 对齐（`ui-v124` + `ui-v91`/`ui-v79`） |
| 273 | 显示 放大显示 | 原版有（查看菜单「放大(I) Ctrl+=」） | 对齐（`ui-v124` + `ui-v86`） |
| 274 | 显示 缩小显示 | 原版有（「缩小(O) Ctrl+-」） | 对齐 |
| 275 | 显示 适合窗口 | 原版有（「撑满窗口(W) Ctrl+Alt+0」） | 对齐（`ui-v124` + `ui-v86`/`ui-v79`/`ui-v93`） |
| 280 | 关于 激活/更改设置 | 原版有（「关于」含版本行/产品ID/激活/公司/官网/版权；账户菜单有授权管理） | **本轮重做**（DIFF-54） |
| 282 | 关于 表格功能 | 原版有（表格行高/列宽/边框/合并单元格/不可直排提示） | 对齐（`ui-v124` + `ui-v102`/`ui-v76`/`ui-v116`） |

> 清单里 271 那一行的「单元/功能」两栏疑似错位（单元写 UNDO/REDO、功能写 分享），本轮按**撤销/恢复**取证，并在结论列注明。

## 二、对照修复

### DIFF-54 「关于」对话框按真机 66-dlg-about.png 重做

修复前只是一个居中的品牌弹窗，**版本号还硬编码成 0.1.0**（与实际包版本脱节），没有真机的字段结构。
现在按真机骨架重做：

- 图标 + `MaxLabel [ 标准版 - 未激活 ]  (真实版本) 64位` + `产品ID: …`
- 「激活」按钮 → 切到「授权与激活」对话框
- 二维码（用软件内置的 `qrcode` 码制生成，指向项目主页）+ 项目组行 + 项目地址
- 分隔线 + 版权敬告 + 「确定」
- 版本号取自 `window.maxlabel.appVersion()`，激活状态取自授权接口

## 三、验证

| 门禁 | 结果 |
| --- | --- |
| `ui-v124.cjs`（新增，其它 8 条） | **10/10** |
| 全量 UI 回归（75 个脚本） | 见 `tools/loop/logs/round-115-gates.md` |
| 其余单元门禁 | typecheck / architecture / editor(36) / geometry / history(9) / color / barcode / printer / print(110) / render(54) / workspace |
| `Check-Matrix.ps1` / `test:evidence` | 605/605；证据校验通过 |
| 未收口差异 | **0 条**（DIFF-1…49、51…54；DIFF-50 为观察项） |

## 四、产物

| 产物 | 路径 |
| --- | --- |
| 安装包 | `app/release/MaxLabel-Setup-1.0.13.exe` |
| 校验和 | `RELEASE-SHA256.txt` |
| 清单队列 | `parity/需求清单-待验证队列.md`（63/194 已填） |

## 五、清单进度与下一步

- 已填 **63/194**：标签格式 13 + 打印机属性 6 + 系统选项 6 + 对象编辑 15 + 打印和预览 15 + 其它 8；
- 余 **131 条**：对象属性 56 / 数据源 55 / 数据库 19 / 授权 1。
- 下一轮计划：**对象属性 56**（真机属性页链路已打通：工具菜单选工具 → `postdrag` 拖出对象 → `Alt+Enter` →
  `Ctrl+Tab` 翻页 → `Read-LabelShopDialogValues.ps1` 读值），随后 数据源 55 / 数据库 19 / 授权 1。
