# MaxLabel v1.0.18 发布说明

发布日期：2026-09-21
产物：`app/release/MaxLabel-Setup-1.0.18.exe`（129.27 MB）
SHA256：`DD6E423A5793971A33018848240E12F2719991FAC9C86DE83DFA8258D48DB8DD`

## 本次改动（用户可见）

**对象属性对话框的页签全部按真机 LabelShop V6.39 对齐**（对应 `parity/diffs.md` 的 DIFF-64）。

此前复刻版的页签是自造的：公共页叫「通用」且被放在最前，矩形/椭圆/直线的页签叫「方框和圆形」「直线和斜线」
（这只是帮助页标题，真机 UI 上并不存在），条码对象还多出一个「Code128 / PDF417」之类的**码制专页**。
真机取证（`parity/reference/labelshop/PROBE-verifier-object-tabs.md` 与 5 张属性页实拍）显示，
真机的规律是**对象专属页在最前、公共页在最后，且最后一页叫「常规」**。

改后的页签（左 → 右）：

| 对象 | 改前 | 改后（＝真机） |
| --- | --- | --- |
| 文字 | 通用 / 文字 / 字体 / 数据 | **数据源 / 字体 / 文本 / 常规** |
| 条码 | 通用 / 条码 / 字体 / 数据 / `<码制>`（5 页） | **数据源 / 条码 / 字体 / 常规**（4 页） |
| 矩形 · 椭圆 · 直线 | 通用 / 方框和圆形（直线为 直线和斜线） | **图形 / 常规** |
| 表格 | 通用 / 表格 | **表格 / 常规** |
| 图片 | 通用 / 图片 / 数据 | **图片 / 常规** |

### 条码的「码制专页」并回「条码」页

真机没有第 5 个页签：码制选择器就在「条码」页页首，随码制变化的专属字段（Code 128 的 `GS1/EAN 128` + `字符集`、
QR 的纠错级别/字符编码、ITF 14 的保护框、汉信码的版本、PDF 417 的层高…）统一放在该页的
**「条码特殊选项」分组**里，页尾是颜色。复刻版按同一结构重排，并把这个分组做成一个带
`barcodeSpecial` 锚点的分组框，后续脚本仍可按锚点定位。

### 顺带

- 「条码」页的码制下拉标签由「码制」改为真机原文 **`条码符号类型(码制)`**。
- 打开属性对话框时默认停在**对象专属页**（真机的行为），不再默认停在「通用」。

## 未包含（仍待取证，本轮未动）

- RFID 对象的页签仍是复刻版既有形态（真机「工具」菜单里没有 RFID 创建入口，DIFF-65 未收口）。
- 停靠式属性面板的页面名（`PropertyPanel.tsx`）未改 —— 真机该界面尚未取证，不凭猜测改。

## 验证

- `npm run typecheck` / `npm test`（typecheck · architecture 7 · color · barcode · editor 40 · geometry · history 9 ·
  printer · print 110 · render 64 · workspace）**全部 exit 0**；`npm run build` exit 0。
- 受影响的 CDP 回归脚本全部单跑通过：`ui-v56` 11/11、`ui-v57` 8/8、`ui-v71` 20/20、`ui-v77` 7/7、
  `ui-v109` 21/21、`ui-v117` 11/11、`ui-v124` 11/11、`ui-v125` 17/17、`ui-v126` 10/10、`ui-v127` 5/5、`ui-v102` 27/27；
  另对 26 个用到 `object-props-tab` 的脚本做了全量单跑清扫（`ui-v100/101/103/105/107/108/116/122/128/58/59/60/66/68/73/74/76/78/81/85/89/92/95`），全部通过。
- `powershell -File tools/parity/Check-Matrix.ps1` exit 0（605/605）。
