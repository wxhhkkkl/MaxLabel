# MaxLabel v1.0.12 发布说明（需求清单 30 条取证 + 七处缺陷修复）

**基线**：v1.0.11（2026-09-21）
**本次类型**：需求清单逐条取证（对象编辑 15 + 打印和预览 15）+ 缺陷修复
**日期**：2026-09-21

---

## 一、需求清单新增 30 条结论（累计 **55/194**）

### 对象编辑（15 条）

| 行 | 条目 | 原版结论 | 复刻版 |
| --- | --- | --- | --- |
| 38 | CTRL+单击多选 | 原版有（帮助 `label_object_select_mouse.html`） | 对齐（`ui-v122` + `ui-v88`） |
| 39 | SHIFT+单击 选择/取消 | 原版有（同页，含空白处圈选） | 对齐 |
| 40 | 拷贝粘贴（含跨模板） | 原版有（`shortcut_main.html`） | 对齐（同模板 + 跨标签模板） |
| 41 | 剪切粘贴 | 原版有（CTRL+X，另标 SHIFT+DELETE） | 对齐（两种键位都可粘贴还原） |
| 42 | CTRL+拖动复制 | **原版未见**（帮助快捷键页无此组合） | 复刻版「不复制、只移动」 |
| 45 | 鼠标拖动移动 | 原版有（`label_object_move_mouse.html`） | 对齐（并修掉拖动改尺寸） |
| 46 | 键盘方向键 | 原版有（0.5 毫米 / SHIFT 5 毫米） | 对齐 |
| 47 | 鼠标拖动调整尺寸 | 原版有（步长离散、SHIFT 等尺寸、角/中间把柄差异） | 对齐（`ui-v76`） |
| 54 | 菜单调整层次 | 原版有（`label_object_align_order.html`） | 对齐 |
| 55 | CTRL+B 移到最后 | 原版有 | 对齐 |
| 56 | 单个对象旋转 | 原版有（绕自身中心） | 对齐 |
| 57 | 多个对象旋转 | 原版有（绕多选组合中心） | 对齐（`ui-v99`） |
| 58 | 组合（含多级） | 原版有（单击组内对象选中整组） | 对齐 |
| 59 | 解组 | 原版有 | 对齐 |
| 60 | 位置锁定与解锁 | 原版有（锁定后不可移动；属性页位置被禁止；被组合后失去锁定） | **本轮补齐**（DIFF-48/49） |

### 打印和预览（15 条）

| 行 | 条目 | 原版结论 | 复刻版 |
| --- | --- | --- | --- |
| 245 | 打印机 名称 | 原版有（换打印机在「标签格式设置」里） | 对齐 |
| 246 | 打印机 位置 | 原版有（端口位置 / 网络路径） | 对齐 |
| 247 | 打印到文件 | 原版有（帮助：需专业版及以上；指令输出方式支持） | 对齐 |
| 250 | 单签拷贝 | 原版有（输出 = 打印数量 × 单签拷贝） | 对齐 |
| 251 | 作业份数 | **原版未见独立字段**（真机字段清单只有 打印数量/单签拷贝/启始记录） | 同样不设 |
| 252 | 记录数量 | 原版有（有数据库时打印数量即记录个数） | 对齐 |
| 253 | 起始记录 | 原版有 | 对齐 |
| 254 | 打印后更新变量数据 | 原版有，**默认不勾选** | **本轮修默认值**（DIFF-53） |
| 255 | 打印标签边框 | 原版有，命令输出方式下无效（真机置灰） | 对齐 |
| 256 | 打印时自动设置数据库记录数量 | 原版有（`print_dlg_dbs.html`） | 对齐 |
| 257 | 拷贝数量从数据库字段引入 | 原版有 | 对齐 |
| 258 | 字段名称 | 原版有 | 对齐 |
| 259 | 允许打印时输入第一个标签的拷贝数量 | 原版有 | 对齐 |
| 260 | 选择起始标签位置 | 原版有（之前置灰、之后重新排号；命令输出方式不可选） | **本轮补齐**（DIFF-51） |
| 261 | 自动跟踪起始标签位置 | 原版有 | 对齐 |

## 二、对照修复（DIFF-47…53）

| 编号 | 缺陷 | 修法 |
| --- | --- | --- |
| DIFF-47 | 一次纯鼠标拖动就把对象框改成「渲染内容尺寸」（条码 8mm→5.08mm、文字 11mm→16mm，还带 0.5mm 位移） | 创建时记下缩放比；缩放比没变就保留对象框尺寸；位置统一按「模型框」换算（顶层与组内子对象同一套） |
| DIFF-48 | 位置锁定没拦住方向键 / 对齐命令 / 属性页位置字段 | 三处分别收口，全被锁时状态栏给提示 |
| DIFF-49 | 被组合的锁定对象没失去位置锁定；组合框用 x/y/w/h 最小最大算（旋转对象与组合子对象会算错） | 组合时清锁定；组合框改为旋转后的视觉包围盒并集 |
| DIFF-50 | **观察项（未收口）**：真机对象属性「常规」页的水平/垂直（相对标签边对齐）下拉是灰的且 0 项，复刻版可用 | 先记录，待拿条码/图片/表格各开一次属性页确认启用条件 |
| DIFF-51 | 打印对话框「选取起始标签」没把起始标签之前的格子置灰、也没重新排号 | 之前置灰禁用、编号从起始标签起重排 |
| DIFF-52 | 对象工具激活时拖到已有对象上会把它拖走而不是新建 | `findTarget` 覆写在对象工具下直接返回空（形状与 fabric 一致）；「数据」工具除外 |
| DIFF-53 | 打印对话框「打印后更新变量数据」默认被勾选（真机默认不勾选） | 默认值改为 `false` |

## 三、验证

| 门禁 | 结果 |
| --- | --- |
| `ui-v122.cjs`（新增，对象编辑 15 条） | **20/20** |
| `ui-v123.cjs`（新增，打印和预览 15 条） | **17/17** |
| 全量 UI 回归（74 个脚本） | 见 `tools/loop/logs/round-114-gates.md` |
| `editor-operations.test.ts` | 36 条（新增组合框并集 / 清锁定 / `isPositionLocked`） |
| 其余单元门禁 | typecheck / architecture / color / barcode / geometry / history / printer / print / render(54) / workspace |
| `Check-Matrix.ps1` / `test:evidence` | 605/605；证据校验通过 |
| 未收口差异 | **0 条**（DIFF-1…49、51…53；DIFF-50 为观察项） |

## 四、产物

| 产物 | 路径 |
| --- | --- |
| 安装包 | `app/release/MaxLabel-Setup-1.0.12.exe` |
| 校验和 | `RELEASE-SHA256.txt` |
| 清单队列 | `parity/需求清单-待验证队列.md`（55/194 已填） |

## 五、清单进度与下一步

- 已填 **55/194**：标签格式 13 + 打印机属性 6 + 系统选项 6 + 对象编辑 15 + 打印和预览 15；
- 余 **139 条**：对象属性 56 / 数据源 55 / 数据库 19 / 其它 8 / 授权 1。
- 下一轮计划：**其它 8 → 对象属性 56 → 数据源 55 → 数据库 19 → 授权 1**。
- 遗留待真机确认（`parity/reference/labelshop/PROBE-round114.md`）：
  ① 原版拖动是否有「对齐参考线」吸附（复刻版有 5px 吸附，帮助无记载）；
  ② CTRL+拖动在原版到底是复制还是移动；
  ③ 点空心矩形内部在原版算不算选中（复刻版当前算选中，`findTarget` 里的「点边框才选中」是死代码）。
