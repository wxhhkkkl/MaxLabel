# MaxLabel v1.0.10 发布说明（需求清单取证启动：标签格式 13 条 + 页面左空/上空 + 卷筒行数）

**基线**：v1.0.9（2026-09-21）
**本次类型**：需求清单逐条取证 + 对照修复
**日期**：2026-09-21

---

## 一、需求清单「标签格式」13 条：全部取证并填写结论

用新工装 `tools/parity/Read-LabelShopDialogTree.ps1`（递归 dump 对话框控件树，能进页签页这种嵌套 `#32770`）
把真机「标签格式设置」四个页签逐页枚举（`PROBE-round112.md`，probe-23~27）：

| 页签 | 关键控件 |
| --- | --- |
| 打印机 | `名称(N)` 下拉 + `设置(S)` / `安装(I)`；`输出方式:` 下拉；`整页反相打印`、`单页任务模式`（`镜像输出` 仅标签打印机可见） |
| 页面 | `纸张尺寸(Z)` + `更改标签格式(S)` + `手动设置` + `纸型(P)`；尺寸：宽/高/**左空/上空**；`纸张颜色` |
| 标签 | 标签（宽/高）、形状、间距（列距/行距）、孔洞（下拉 + 灰禁毫米框）、行列（列数/行数）；`多行标签` |
| 其它 | `起始位置(A)`、`首选方向(W)`、`打印位置微调`（左侧/顶部）、`保存自定义格式`（系统格式下灰禁） |

13 条结论已写入 `parity/需求清单-待验证队列.md`（含一条**与清单描述相反**的澄清）：
清单说「卷筒模式下，列数不可设置」，真机实测是 **行数(R) 灰禁、列数(C) 可设置**（摘要显示 `100.00 x 150.00 毫米 [1 列]`）。

## 二、对照修复（DIFF-45）

1. **页面页补「左空(L)/上空(T)」**：`PageLayout` 增 `pageLeftMm/pageTopMm`（文档归一化 + `pageCells()` 用它作为标签阵列起点），
   「模板属性设置 → 页面」新增两个输入框（系统预定义格式下只读）。
2. **卷筒格式的行数禁用**：新增 `isRollPrinter()`（LabelShop 内置打印机→卷筒；Windows 驱动按名称识别），
   卷筒文档的「标签」页里行数只读、保存按 1 行落库——与真机「行数灰、列数可」一致。

## 三、验证

| 门禁 | 结果 |
| --- | --- |
| 全量 UI 回归 | **72/72 脚本全过** |
| `ui-v70.cjs` | **17/17**（新增 左空/上空 字段 + 系统格式只读 + 自定义可输入 7.5/3） |
| `ui-v119.cjs` | **36/36**（新增 卷筒自定义文档：行数只读、列数可编辑） |
| `test:print` / `test:printer` | 新增「页面左空/上空把整组标签格平移到指定起点」「isRollPrinter 判定」 |
| 其余单元门禁 | typecheck / architecture / color / barcode / editor / geometry / history 全绿 |
| `Check-Matrix.ps1` / `test:evidence` | 605/605；证据 599 行通过 |
| 未收口差异 | **0 条**（DIFF-1…45） |

## 四、产物

| 产物 | 路径 |
| --- | --- |
| 安装包 | `app/release/MaxLabel-Setup-1.0.10.exe` |
| 免安装目录 | `app/release/win-unpacked/` |
| 校验和 | `RELEASE-SHA256.txt` |
| 真机取证 | `parity/reference/labelshop/PROBE-round112.md` |
| 清单队列 | `parity/需求清单-待验证队列.md`（标签格式 13/13 已填） |

## 五、清单进展与下一步

- 已填结论：**标签格式 13/13**；
- 余 **181 条**：对象属性 56 / 数据源 55 / 数据库 19 / 对象编辑 15 / 打印和预览 15 / 其它 8 / 打印机属性 6 / 系统选项 6 / 授权 1。
  下一轮按「打印机属性 6 → 系统选项 6 → 对象编辑 15 → …」推进（优先把已有真机截图的分类先收掉）。
- 另有两处待用户配合：装佳博官方驱动后验证 USB「有队列」发送；移除真机上残留的测试打印机 `TSC TSPL-N (203 dpi)`。
