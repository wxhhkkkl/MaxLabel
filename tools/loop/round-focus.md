本轮只做 **P0-A 模块** 的这几条（不要跨模块）：

- **A1** 编辑态菜单栏 12 项与顺序，以及"无文档 / 有文档"两种上下文下的菜单收窄行为
- **A5** 状态栏字段顺序与文案
- **A6** 左侧对象面板 / 右侧打印面板（参数设置 · 打印服务器 · 帮助 三页签 + 输入数据 + 打印数量/单签拷贝/打印）结构
- **DIFF-1** 复刻版菜单文案「云服务(C)」应改为原版「云马通(C)」
- **DIFF-4** 状态栏文案对齐（原版含标签规格串与数据库状态，不是「未打开标签模板 / 未使用数据库」）

## 视觉对照附件（已作为图片附件给出）

1. `40-editor.png` —— **真机编辑态主窗口**（物理像素 2582x1550）。从上到下依次是：标题栏 → 菜单栏 12 项 → 第 1 行主工具栏 → 格式栏 → 对齐栏 → 标签页行（起始页 / 新标签模板1）→ 中间工作区（左侧对象面板、带双轴标尺的画布、右侧打印面板）→ 底部状态栏。
2. `00-main.png` —— 真机**起始页**（无文档上下文）：菜单栏只有 `文件(F) 查看(V) 账户(A) 云马通(C) 选项(O) 帮助(H) 建议与反馈`。
3. 复刻版当前起始页截图（`parity/reference/maxlabel/00-main.png`）。

## 要求

1. **先取证再改**：用现成工装截复刻版当前状态，确认"我们改之前长什么样"：
   ```powershell
   powershell -File tools/parity/MaxLabelCtl.ps1 -Action run -Scenario tools/parity/scenarios/menus.json
   ```
   截图落在 `parity/reference/maxlabel/`，用于汇报里贴证据。
2. 菜单栏在有文档时必须是 12 项且顺序与真机一致；无文档时按原版收窄。改完要能用 CDP 断言这一点（新增/扩展 `app/scripts/` 下的冒烟脚本并说明命令）。
3. 菜单**项**文案与加速键字母必须逐条对标：`新建(N)`、`打开(O)...`、`关闭(C)`、`打印设置(R)...`、`退出(X)`、`最近的文件`（无最近文件时应禁用）等。真机菜单弹窗截图在 `parity/reference/labelshop/`（`menu-file.png`、`50-editor-menu-*.png`，若某些还没采集到，以 `app/docs/labelshop-help-zh/menu_*.html` 为准，并在汇报里注明依据来源）。
4. 状态栏字段顺序、分隔符、单位与原版一致；不确定时以 `app/docs/labelshop-help-zh/toolbar_status.html` 求证，汇报里列依据。
5. 除 P0-A 外不要顺手改打印引擎、数据源、服务端；不要改 `parity/reference/` 下的证据文件；不要改 `tools/parity/LabelShopCtl.ps1`。
6. 完成自测（门禁命令见上文）后提交，并把 `parity/matrix.md` 对应行状态改为 `已实现`（证据列写测试名/截图名），`parity/backlog.md` 勾掉完成项。
