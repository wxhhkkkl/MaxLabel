# 本轮起（循环 round-104 起）指定任务与硬性口径

> ⚠️ **round-114 门禁的 `ui-v116` 失败（7/12）是抖动，不是真回归** —— 别为它改测试、更别改产品。
> 验收方 round-95 复跑：`$env:MAXLABEL_UI_SCRIPT='ui-v116.cjs'; npm run test:ui` → **12/12 PASS**（指针/表格拖拽/斜线/系统设置自动打开属性页逐条全绿）。
> 大概率原因：门禁跑 UI 的同时（16:46–16:52）我起了自己的验收实例（`maxlabel-verifier-profile`）跑并排图与工装，抢了资源；
> ui-v116 对指针状态与拖拽几何是时序敏感的。
> **处置：重跑门禁即可**；若要更稳，可在该脚本里给指针/拖拽类断言加"失败重试一次"，但**不许降低断言强度**。
> （教训：门禁跑 UI 时，验收方一律不得起任何 electron 实例——这条我已写进自己的检查清单。）

> 本文件由验收方维护（2026-09-21 重写；上一版写于 09-17、基线 33%，已全部过期——**不要再按旧内容选活**）。

## 一、当前真实基线（验收方实测，2026-09-21）

| 项 | 值 |
| --- | --- |
| 矩阵 `parity/matrix.md` | 605 条 → **已实现 605 / 部分 0 / 未实现 0 / 待核 0（100%）**；`Check-Matrix.ps1` exit 0 |
| 需求清单 `parity/需求清单-待验证队列.md` | **194/194 全部有结论（待填 0 条）** |
| 全量 UI 回归 | **79 个脚本**（`ui-v48 … ui-v128`）全绿（`tools/loop/logs/round-60g-ui.log`） |
| 单元门禁 | `npm test` 全绿（typecheck / architecture / color / barcode / editor 40 / geometry / history / printer / print / render 54 / workspace）；另 label-spec / label-formats / update / title / license / close / installer / evidence 全 exit 0 |
| 已发布 | **v1.0.17**（`app/release/MaxLabel-Setup-1.0.17.exe`，SHA256 `76A01BCE…6A1E`，冒烟 4/4）；`main` 与 tag `v1.0.0…v1.0.17` 已推送 |
| 未收口差异 | `parity/diffs.md`：**DIFF-63**（待真机点按）、**DIFF-50 / DIFF-60**（观察项） |

**`parity/backlog.md` 是历史档案（900+ 行，最新在最上），里面很多"待办"早已做完**——只按本文件选活；backlog 只在收尾时追加本轮记录。

## 二、做活的硬性口径（用户要求，违反即返工）

1. **结论三态写清**：原版**有** / 原版**无** / 原版**有但受限**，并给可复现证据（真机截图或控件 dump 文件名 + 帮助页名 + 复刻版断言名/命令）。
2. **不许按猜测造功能**。语义不确定 → 先取证（真机 > 帮助）；取不到就在 `parity/diffs.md` 登记为「待取证」并写清**已试过的手法与失败现象**，不许实现猜测版本、不许留 TODO/假实现。
3. **不许为过测试而改弱断言**；测试随行为变更失效时，改测试可以，但断言强度不能降，并在提交信息里说明理由。
4. 每收口 1-2 条就 `git add -A && git commit`（含 matrix/diffs/清单更新），提交信息写清条目编号（DIFF-xx / 清单第 N 条 / 矩阵条目号）。

## 三、优先级清单（按顺序取第一项尚未完成的）

**⚠️ 用户可见缺陷（round-63 验收方已定位并修复，勿回退）**：**新建条码全黑**。
> 根因：`editor/barcode.ts` 的 `barcodeToDataURL()` 原来用 `backgroundcolor: 'FFFFFF00'` 表达"透明底"，
> 但 **bwip-js 不接受 8 位带 alpha 的颜色 → 整张图渲染成 100% 黑块**（实测 透明 0% / 暗 100%）；
> 而新建条码默认 `backgroundTransparent: true` → 每个新建条码都全黑。
> 修法：**透明底时不传 `backgroundcolor`**（实测 透明 53.2% / 暗 46.8%，条码正常）。
> 防复发断言已加进 `app/scripts/render-regression.ts`（暗像素 20%~80% + 透明 >20%，已验证"改回旧写法即红"）。
> 证据：`parity/reference/maxlabel/PROBE-verifier-round63-barcode-black.md`。
> **下一版发布说明（v1.0.19）请把这条列为"用户可见修复"**；`test:barcode` 只验编码规则、`test:render` 原来不渲染条码图像，
> 所以此前的门禁全绿也没拦住它——这类"图像渲染结果"的断言以后要覆盖。

**⚠️ DIFF-72 需要更正（验收方 round-79 用实拍复核，方向要改回来）**：真机「条码属性 → 条码」页**页尾是有 `颜色:` 控件的**
（`verifier-20c-barcode-page.png` 实拍：`颜色:` + 黑色色块 + 下拉；并排图 `cmp-props-r113.png` 左边一眼可见）。
round-113 依据**文本 dump** 判"该页没有颜色"、并把复刻版「条码颜色」**迁到了「常规」页** —— **那是误判**：
该控件是 **owner-drawn 色块**，控件树 dump 枚举不到它，所以 dump 里整行消失。
→ 请把 `条码颜色`（testid `barcode-color` 可不变）**放回「条码」页页尾**（在「供人识读字符」组之后、底排按钮之前），
`常规` 页的 `颜色(&C):`（真机值 `固定颜色`，是**颜色模式**）**保留**，两者不是同一个东西。
证据与教训：`parity/reference/labelshop/PROBE-verifier-round79-barcode-color.md`（含"图片证据优先于文本 dump"的结论）。
验收方工装 `Verify-BarcodePage.cjs` 的断言已同步改成"条码页**必须**有颜色控件"。

> **验收方 2026-09-21 17:32 刷新后的当前队列（以本表为准；下面各段保留作证据与施工依据）**
>
> | # | 事项 | 状态 / 下一步 |
> | --- | --- | --- |
> | 1 | **DIFF-63** 序列号「重置」按钮 | ✅ 证据齐（真机**单一 `重置` 且恒 DISABLED**，无「立即重置」）→ **改代码**：复刻版若有两个按钮合并成一个并默认禁用；启用条件登记待取证。见 `PROBE-verifier-round88-DIFF63-serial.md` |
> | 2 | **DIFF-72 方向更正**：`条码颜色` 放回「条码」页页尾 | ⏳ 待改代码（验收方工装 `Verify-BarcodePage` **3/4** 红灯盯着）；同时把 `diffs.md` 里 DIFF-72 的「已修」与"按无颜色处理"改过来。见 `PROBE-verifier-round79-barcode-color.md` |
> | 3 | **DIFF-71 收尾**：底排补 **`帮助`** 按钮 | ⏳ 待改（工装 `Verify-SystemOptions` **5/6**，实测按钮 `[常规,打印和数据库,编辑,系统,恢复默认,取消,确定]`）；页签已=真机 4 页 ✓、复刻版扩展区已合规 ✓ |
> | 4 | **新差异**（round-80 并排图发现）：自定义对话框预览**只画一个标签**，真机画 4×2 拼版网格（带编号、孔洞在每格中心） | ⏳ 待改 + 补断言（格子数=行列乘积）；**与「选择标签格式」共用同一套预览逻辑**。证据 `parity/review/cmp-custom-r114.png` |

**round-108 验收方复核 `3d4db08`（claude round-116 提交）：两个红灯都转绿了 ✓，另发现一处新问题**

> **round-116 断言强度抽验（验收方，`git diff 55b2e55 fec35d6`）——"强度不降"属实 ✓**
> | 脚本 | 断言增删 | 结论 |
> | --- | --- | --- |
> | `ui-v103.cjs` | +5 / −1（净 +4，与自述 15→19 一致） | ✓ 加严 |
> | `ui-v125.cjs` | +2 / −2（**两条 DIFF-72 断言按更正方向重写**）：`DIFF-72 条码页无颜色控件` ✗ → **`DIFF-72 条码页页尾有颜色色块（真机实拍 颜色: + 色块 + 下拉）`** ✓；`常规页有「颜色(&C):」色块` → **`常规页有「颜色(&C):」颜色模式下拉且选中「固定颜色」`** ✓ | ✓ 方向对了、且更具体（不是删断言换宽松） |
> | `ui-v130.cjs` | +3 / −0 | ✓ 纯新增（拼版网格） |

| 复核项 | 结果 |
| --- | --- |
| `Verify-SystemOptions`（DIFF-71） | ✅ **6/6 PASS** —— 页签 4 页整数组全等 ✓、底排 `确定/取消/帮助` 齐备 ✓、无可见「应用」✓、常规页四分组框与字段原文 ✓ |
| `Verify-BarcodePage`（DIFF-72） | ✅ **4/4 PASS** —— 「条码」页**有**颜色控件 ✓（方向已按实拍更正）、常规页颜色模式仍在 ✓ |
| `标尺单位(U):` 文案 | ✅ 已是真机原文 **`毫米` / `英寸`**（源码 L256/257，注释还引了 `PROBE-verifier-round100-sysset-controls.md` ✓） |
| DIFF-72 台账标题 | ✅ 已改为「**字段原文已修，颜色控件方向已按实拍更正**」✓ |

⚠️ **新问题（round-108 读代码发现，请改）**：`ObjectPropsDialog.tsx` 的**「常规」页现在有两个下拉绑同一个状态**：
- L1705 `data-testid="obj-color-mode"`，label `颜色(&C):`
- L1775 `data-testid="color-change-mode"`，label `颜色变化模式`
两者 `value` 都是 `ccMode`、选项都来自 `COLOR_CHANGE_MODES` → **同一页重复渲染同一个选择器**。
真机「常规」页的 `颜色(&C):` 只有一个（本机值 `固定颜色`），其下的「变色设置」组是**依赖该模式展开的字段**。
→ 处置：**保留 `颜色(&C):` 这一个模式下拉**，把「变色设置」组内那个重复的模式下拉去掉（组内的索引表/输入/粒度等字段保留）；
若「变色设置」整体其实是**复刻版扩展**（真机无此组），那就按扩展区口径加图例标注，但**仍只保留一个模式下拉**。
改完请补一条断言：**同一页 `颜色(&C):` 类模式下拉恰好 1 个**。

⚠️ **小事**：`界面语言(L):` 复刻版只有 `简体中文`（真机 3 项）——请在 `diffs.md` 的 DIFF-71 段补一条「**原版有但受限：仅实现简体中文**」，不要列假选项。

- 现状：`CustomLabelFormatDialog.tsx` 的预览是一个 `<svg>`，`viewBox={-2 -2 ${width+4} ${height+4}}` —— **viewBox 就是一个标签**，
  所以只画得出一个格子（`rows`/`cols` 虽然解析了、但只用于算页面尺寸与底部那行文字）。
- 真机形态（证据 `parity/review/cmp-custom-r114.png` 左半 + `verifier-r44-hole-circle-20b.png`）：
  1. 预览区画**整张拼版**：`cols × rows` 个格子（本机 4行×2列 = 8 格）；
  2. 每格**正中**写格子序号，**先行后列**（1、2 在第一行，3、4 第二行 …）；
  3. 格子之间留 `列距(P)` / `行距(L)` 的间距；每格形状按 `形状` 选择（圆角矩形要用**共享半径** `roundRectRadiusMm`）、孔洞画在**每格中心**；
  4. 只在**第一个格子**上标尺寸（`100mm` 上方 / `70mm` 右侧），与现在那两处标注保持一致；
  5. 底部那行文字**不变**（`100.00 x 70.00 毫米 [4行 2列]`，已有断言钉住）。
- 目标 viewBox = 整个网格的范围（`width*cols + colGap*(cols-1)` × `height*rows + rowGap*(rows-1)`，再留少量边距），
  与 `pageWidth/pageHeight` 的计算（L71-72）保持同一套公式，**别再写第二份**。
- 补断言：**格子数 = rows × cols**，且序号文本是 `1..N`（照 `ui-v130/131` 的加严先例）。
> | 5 | **P4 四件套**：先做**零成本**的一步 —— 把已入库的并排图写进矩阵证据列（见下表 6 行） | ⏳ 改完四件套齐 **0 → 6**；再按 `parity/P4-四件套工单.md` 推进真机证据引用 |
> | 6 | **DIFF-70** 打印输出里有没有孔 | ⏳ 改走「打印对话框 → 预览(V)」；或先放一个满标签大矩形确认输出非空（现有产出是空白 A4 PDF，无绘制算子） |> | 7 | **DIFF-65 / DIFF-68** | ⏳ DIFF-65：真机工具菜单与数据源下拉都无 RFID（后者由 round-88 实测）→ 决定"删工具 or 登记为复刻版扩展"；DIFF-68：打印机页三个开关的控件形态待取证 |
> | 8 | **P2 对象编辑三处** | ⏳ 第 1 条（拖动复制语义）**验收方已收敛**：证据＝真机「系统设置→编辑」有 `☐禁用鼠标拖动复制功能`（**选项名=强证据**，⇒ 默认拖动=复制）；**行为对照未取到**（真机画布工具脆弱：docview 尺寸每次启动都变，坐标映射不能跨会话复用）。→ **复刻版暂不改行为**（这是"少一个真机有的可选行为"＝功能缺口，不是行为做错）；台账按证据强度分级记录。见 `PROBE-verifier-round92-ctrldrag.md` 第八节 |
> | 9 | **DIFF-50 / DIFF-60** 两条观察项 | ⏳ 边界类，收口前需明确"已记录边界"口径 |
>
> **已完成、别再当待办**：P1.5 属性页签（DIFF-64 收口，`ui-v129/130/131` 断言齐）；P0 选择标签格式/圆角（DIFF-66 + 单圆角 1mm，`Verify-LabelFormatDialog` 17/17）；`孔洞=矩形` 贯通（DIFF-69）；**新建条码全黑**（v1.0.19 已发布修复）；帮助 vs 真机用词（DIFF-67）。

**并排图新发现（round-99，`parity/review/cmp-sysset-r115.png`：左真机 round-112 / 右复刻版 round-115）——「系统设置 · 常规」页还有 3 处可见差异，请逐条核实后处理**

> **round-100 已用真机实测把 a–d 全部定案**（证据：`parity/reference/labelshop/PROBE-verifier-round100-sysset-controls.md`，含 tree dump 原文）：
> | # | 定案 | 要做什么 |
> | --- | --- | --- |
> | a | 真机底排 = `确定 / 取消 / **帮助**`，`应用(&A)` 是隐藏控件 | 复刻版**补「帮助」按钮**（与工装 5/6 那条同源） |
> | b | 真机 `标尺单位(U):` 下拉 **2 项：`毫米` / `英寸`**（无"（公制）/（英制）"后缀）；`界面语言(L):` 下拉 **3 项：简体中文/繁体中文/English** | **改选项文本为 `毫米`/`英寸`**，并同步改 `ui-v103` 里引用「英寸（英制）」的断言；界面语言若只支持简体，**不要**列假的另两项，改记 `diffs.md`「**原版有但受限**」 |
> | c | 真机「非打印对象」组两个复选，round-112 现场为 **☐/☐（均未勾选）** | 复刻版默认若是勾选 → 改成**未勾选**并同步改 `ui-v103` 的"默认勾选"断言（**注意**：真机那张是现场态，若你更保守，可先在台账写明"现场观测为未勾选"） |
> | d | 真机「其它」组 = **4 个复选**（`允许运行脚本(&S)`/`启动时运行模板向导`/`自动旋转输出页面`/`新建对象后自动打开属性页`）+ `标签工作区背景颜色：` + 色块按钮 + `恢复默认` | 复刻版并排图里看不全 → 先排除"对话框太矮/需滚动"，确实缺项则照上表补齐 |

> **round-101 复核结论（验收方读 `parity/diffs.md` 的 DIFF-71 段后）**：
> - **c 已由循环自己正确处置** ✓：台账 L1330 明确写着"真机四个复选框全为勾选态＝**本机已保存的配置，不能据此改复刻版默认值**（默认值按帮助 `config_general.html`）"
>   —— 与我"现场态不等于默认值"的顾虑一致，**无需再改**。
> - **d 属于我误读** ✓：台账显示复刻版常规页已按真机补齐四个分组框与 9 个字段（`ui-v103` 15/15）；并排图里"看不全"是**对话框高度/滚动**造成的，**不是缺项**。
> - **b 是唯一的新增项，且台账里还没记** ✗：`标尺单位` 下拉复刻版写「毫米（公制）/英寸（英制）」，真机实测是 **`毫米` / `英寸`**（2 项，无后缀）；
>   `界面语言` 真机 3 项（简体中文/繁体中文/English）而复刻版仅 1 项 → 请**①** 改文案并同步 `ui-v103` 断言；**②** 把这两条补进 `diffs.md` 的 DIFF-71 段（界面语言按「**原版有但受限**」记）。
>   **双证据**：除真机实测外，**复刻版自己的注释**（`ui-v103.cjs` L5）就写着"标尺单位…可选公制「毫米」和英制「英寸」"
>   —— 也就是说真机与帮助两处都是 `毫米`/`英寸`，只有 UI 文案被写成了「（公制）/（英制）」✗，属于**纯文案偏差**，改起来零风险。

| # | 真机 | 复刻版 | 处置 |
| --- | --- | --- | --- |
| a | 底排 `确定 / 取消 / **帮助**` | `取消 / 确定`（**无「帮助」**） | ✅ 与工装 5/6 的结论一致 → **补「帮助」按钮**（§3 项） |
| b | `标尺单位(U):` 值显示 **`毫米`** | 显示 **`毫米（公制）`** | ⚠️ 文案不一致：先核实真机下拉的**选项原文**（可能真机选项就叫「毫米」，复刻版自己加了"（公制）"），再对齐 |
| c | `输出非打印对象(P)` 默认 **未勾选** ☐ | 默认 **已勾选** ☑ | ⚠️ 需核实：真机这张是 round-112 现场状态，我没动过它 → 很可能就是真机默认值；若确认，复刻版默认应改成**未勾选**（并同步改 `ui-v103` 里那条"默认勾选"的断言） |
| d | 「其它」组有 4 个复选框（`允许运行脚本(S)`/`启动时运行模板向导`/`自动旋转输出页面`/`新建对象后自动打开属性页`）+ `标签工作区背景颜色:` + `恢复默认` | 截图中只看到部分项 | ⚠️ 先确认是否只是**对话框高度不足/需要滚动**；若确实缺项，按真机补齐（不许留占位） |

**注**：b/c/d 三条都是"并排图一眼能看出来"的差异 —— 这正是第 ③ 件（并排图）不可省的原因。

**🔴 round-116 门禁失败已定位：`ui-v108.cjs` 7/8 是**真回归**（不是抖动）—— 我单跑复现了，请优先修**

```
PASS A-85 …（4 条）
PASS A-201 驱动端口下「变色设置」可选且提供六种颜色变化模式
FAIL A-201 USB 直连的条码标签打印机不可选择彩色打印（变色设置不提供）   ← 就是这条
PASS A-201 提示写明原版的自动判定规则
PASS A-201 换回驱动端口后「变色设置」恢复可选
```

**根因判断（读代码得出）**：round-116 为了消除"同页两个模式下拉"，把模式下拉**挪到「常规」页顶部**并复用了
`data-testid="color-change-mode"`、删掉了「变色设置」组里那一行 —— 结果这个模式控件变成**无条件渲染**，
于是丢掉了原本"**USB 直连的条码标签打印机不提供彩色（变色设置不可选）**"的**按打印机类型门控**语义 ✗。

**正确修法（不要回退"删重复"这个决定，只补回门控）**：
1. 保留**唯一**一个模式入口（"同一页只留一个"这条要求继续成立）；
2. 把这个入口（连同"变色设置"组的字段）按既有的判定（`imageColorAllowed` / `VARIABLE_COLOR_UNSUPPORTED_NOTE` 那一套）
   **重新门控**：USB 直连的条码标签打印机 → **不提供**彩色/变色（渲染提示文案，`ui-v108` 那条断言即恢复）；
3. 若"常规页顶部的 `颜色(&C):`"与"变色设置组"在真机上是**两个不同控件**（前者是颜色模式、后者是随模式展开的字段区），
   那就让顶部保留 `颜色(&C):`、把组内字段整体门控，**断言按 testid 精确到组**，不要再出现"全页任找模式下拉"这种宽断言。

**风险提示**：`state.json` 的 `consecutiveFail` 已是 **2**，再失败一轮会触发三连败回滚 ✗ —— 所以这条**优先于队列其它项**。

**🔵 运维记录（验收方 round-125，知悉即可、无需处理）**

- **已切回 codex**：18:46:20 起 `agent=codex` 驱动 round 118/119（用户明确要求优先用 codex）✓。
- **round-117 的门禁被验收方主动中止**（18:45）：为尽快完成 agent 切换，中止了那一轮正在跑的全量 UI 门禁（已跑约 10 分钟）。
  → **round-117 没有门禁结论** ✗；但它修的内容（`d2543d9` 真回归修复）与 `d70f269`（DIFF-63）都已在代码里 ✓，下一次全量门禁会一并覆盖 ✓。`state.json` 的 `consecutiveFail` 已归 **0** ✓。
- **顺带修掉切换器一个 bug**：看护写的 `STOP` 原来不删 → 切回首选 agent 后监管器自己也因该 STOP 退出 → **codex 永远等不到接管**（正是"还是没用 codex"的机制）。现已在监管器退出后自行删除 ✓。

**💡 DIFF-70 取证路径提示（验收方 round-132，省你时间）**

我看到你在真机上截了「登录 LabelShop」（`parity/reference/labelshop/round119-print-dialog.png`）—— 那多半是 **`Ctrl+P` 撞上了登录墙** ✓。
这条我早有实测：**快捷键 `Ctrl+P` 会弹登录框（未登录不可达）**，但**编辑器右侧面板的「打印」按钮不需要登录** ✓
（round-47 与 round-110 两次实测；见 `PROBE-verifier-round47*.md` 与 round-110 的 DIFF-70 记录）。所以请**别在 `Ctrl+P` 上花时间** ✓。

**DIFF-70 现有的真实困难**（也不是路径问题）：round-110 走右侧面板打印到 `Microsoft Print to PDF`，产出是
**单页 A4、无 /Resources、内容流只有 1 个 `cm`、零绘制算子的空白 PDF** ✗ —— 所以"孔在不在"无从判断 ✓。

> **round-137 更正我自己的说法**：你 round-119 的复核实测显示 —— **右侧面板的「打印」也会先弹「登录 LabelShop」** ✗，
> 但**点「取消」之后仍会进入「将打印输出另存为」**并产出 PDF ✓（`PROBE-round119-DIFF70.md`）。
> 所以我 round-47 记的"右侧面板打印**不需要登录**"应更准确地表述为：**不需要登录也能走到"另存为 PDF"，但中间会先弹一次登录框（可取消）** ✓。
> 这条更正已同步到本清单，供后续取证引用。
建议按此顺序试：① **打印对话框里的「预览(V)」**（先看预览里画不画孔，比 PDF 快）✓；
② 若预览正常，再打一份并**先放一个满标签的大矩形**确认输出非空（排除"什么都没画"）✓；
③ 仍为空就换 `XPS Document Writer` 或另一台打印机 ✓。
**取证不到就继续登记"待取证"** ✓，不要用帮助文字反推实现 ✓。

**⚠️ 待复核候选（round-130，`cmp-start-r119.png`）：启始页「最新文章」列表看着是空的**

- 并排图 `parity/review/cmp-start-r119.png`（左＝真机启始页，右＝复刻版 round-119 构建）：真机右下方「最新文章」有 **3 条**（带标题与日期），
  复刻版只看到**标题**、下面一片空白 ✗。
- **先别改**：可能只是"列表在折叠线以下/需要滚动"（我在 round-121 就吃过一次"状态不同→假差异"的亏，这次同样先复核）。
- 复核方法：把启始页**整页**（含滚动到底）截一张复刻版图，和真机那张对比；确实为空再依真机补内容（真机那 3 条是**联网抓的文章**，
  若复刻版拿不到同样内容，就按「**原版有但受限**」登记，不要编造文章）。

**🟠 流程风险（验收方 round-129，请循环纳入自己的规程）：三连败回滚会"销毁"回滚点之后的全部提交**

- **事实**：`git reflog` 里能看到 `HEAD@{4}: reset: moving to 944625d…` —— 三连败回滚把工作树 reset 回了 `lastGoodSha`，
  于是**回滚点之后的 54 个提交全被扔掉**（含 round-114~117 的产品改动、台账、**以及验收方的证据与工装**）。
  本次是 codex 自己发现并用 `3a4d502`（"救回被回滚工装销毁的 54 个提交"）**救回来了** ✓ —— 我逐项复核过：
  `options-help` / `标尺单位 毫米|英寸` / `barcode-color` / `serial-reset` / 拼版网格断言 / `imageColorAllowed` 门控**全部还在** ✓，
  且 `origin/main` 与本地一致（ahead/behind = 0/0）✓，没有丢失 ✓。
- **要求（防下次真丢）**：执行回滚**之前**
  ① 先给当前 HEAD 打一个备份分支或 tag（例如 `git branch wip-rollback-r<轮次> HEAD`）；
  ② 把被丢弃的 SHA 列表写进 `parity/FAILURES.md`（这样即使工作树回退，也能像这次一样按 SHA 找回）；
  ③ **不要把 `tools/parity/**` 与 `parity/reference/**`（验收方证据与工装）纳入回滚范围** —— 它们不是循环的产品改动，
  被一起销毁会让"证据链"凭空断裂，严重影响验收。

**✅ 同态复核完成（round-128，`cmp-menu-r119.png`）：round-121 那条"菜单项数对不上"是**状态造成的假差异** ✓ —— 真差异只有一处**

- 复核图：`parity/review/cmp-menu-r119.png`（左＝真机 round-43，右＝复刻版 **round-119 构建**，**两侧都是"有文档态"**）。
- 结论：14 项**全部存在、顺序完全一致** ✓（`新建(N) Ctrl+N` / `新建条幅飘带` / `打开(O)... Ctrl+O` / `关闭(C) Ctrl+W` / `保存(S) Ctrl+S` / `另存为(A)...` / `分享` / `打印(P)... Ctrl+P` / `打印预览(V)` / `导出打印机指令文件(E)` / `标签格式设置(L)...` / `模板属性设置(M)...` / `最近的文件 ▸` / `退出(X)`）。
  → **说明"缺 6 项"是无文档态把相关项隐藏/禁用所致** ✓，**不要**按 round-121 的初判去改代码 ✓。
- **唯一真差异**：`分享` 的**加速键**不同 —— 真机 `分享(T)...` ✗ vs 复刻版 `分享(I)...` → 请改成 **`分享(T)...`** 并补断言。
  **改动点（验收方已定位到行）**：`app/src/renderer/src/features/commands/labelShopMenus.ts` 里
  `{ label: '分享(I)...', action: () => deps.setModal('cloud'), … }` → 把标签改成 `分享(T)...`（真机里 `T` 没被别的项占用：`打印(P)`/`模板属性设置(M)`/`退出(X)` ✓），并补一条断言钉住加速键。
  （`A-34/A-35/A-37/A-38/A-39` 五行现已引用这张同态图作并排证据 ✓，四件套齐 11 → **14** ✓。）

`parity/review/cmp-menu-r115.png`（左＝真机 round-43 **有文档态**；右＝复刻版 round-115 构建，**无文档态**）：

| 真机（有文档） | 复刻版（无文档，本轮这张图） |
| --- | --- |
| `新建(N)` / `新建条幅飘带` / `打开(O)...` / `关闭(C)` / **`保存(S)`** / **`另存为(A)...`** / **`分享(T)...`** / **`打印(P)...`** / **`打印预览(V)`** / **`导出打印机指令文件(E)`** / **`标签格式设置(L)...`** / **`模板属性设置(M)...`** / `最近的文件` / `退出(X)` | `新建(N)` / `新建条幅飘带` / `打开(O)...` / `关闭(C)` / **`打印设置(R)...`** / `最近的文件` / `退出(X)` |

**但我先不判它是缺陷**：两边**状态不同**（无文档态很可能把保存/打印/模板相关项隐藏或禁用），所以这可能是假差异。
→ **复核方法**（我已把出图工装改好）：`menu` 场景现在会**先进编辑器**再展开菜单，用当前构建重出一张**同态**并排图；
若同态下仍缺项/多项，那就是真差异，按真机补齐/删除并补断言。**在同态图出来之前不要动代码。**

> **验收方 round-114 新增两个自检工具，建议循环每次改证据列后都跑一遍**：
> - `node tools/parity/check-evidence-files.cjs` —— 审计 `parity/matrix.md` 里**引用的证据文件是否真的存在**
>   （当前：引用 97 个文件，**存在 97 / 缺失 0** ✓）。存在性不查的话，覆盖率会被"指向不存在文件"的引用灌水。
> - `node tools/parity/survey-evidence-coverage.cjs` —— 四件套覆盖度普查（当前：真机 64 / 复刻 255 / 并排 21 / 断言 485，**四件套齐 16**）。

**🔴 round-149 验收方替跑结果（最高优先）：round-126 的门禁红了 4 个脚本，`consecutiveFail` 已 = 1**

我替跑了你那条"未实跑"的 `ui-v134.cjs` ✓，并把 round-126 门禁的失败清单取了出来：

```
round-126 门禁结论：失败 1 项: test:ui
ui-v134.cjs : 16/19 : 16/19 PASS
FAILED SCRIPTS: ui-v56.cjs, ui-v77.cjs, ui-v106.cjs, ui-v134.cjs
```

**`ui-v134` 里失败的三条（原文）**：
1. `「条码」页分组框整数组全等 = 真机 [尺寸, 条码特殊选项, 供人识读字符]` → **false**
2. `分组框按真机坐标次序出现（尺寸 在 条码特殊选项 之前、供人识读字符 在最后）` → **false**
3. `「尺寸」组含真机原文 X 尺寸(&X): 与 码  高(&H):（「码」「高」间两个空格）` → **false**

**我的独立结论（重要）**：**页面本身没坏** ✓ —— 我的两个工装在**同一份代码**上全绿：
`Verify-BarcodePage` **4/4 PASS**（四个页签 ✓、条码页 5 个字段原文逐字命中 ✓、条码页有颜色色块 ✓、常规页颜色模式 ✓）、`Verify-LabelFormatDialog`（P0）**18/18 PASS** ✓。
→ 所以这 4 个脚本的红，**不是"功能坏了"，而是断言与新结构对不上** ✗：
- `ui-v134` 那三条：你新加的**分组框**与断言里期望的"分组框集合/顺序/组内文本"不一致（多半是 legend 文案或容器 testid 与断言取的不是同一处 ✓ 请自查 `results['「条码」页分组框整数组全等…']` 上面那段的选择器）；
- `ui-v56` / `ui-v77` / `ui-v106`：这三条**没跟着这次重构迁移** ✗（你把 `供人识读` 四项从数据源页挪到了条码页，之前只迁移了 `ui-v71`/`ui-v78` ✓）→ 按新结构改断言 ✓（**只许加严，不许放宽** ✓）。

**风险**：`consecutiveFail=1`，**下一次门禁再红就会触发三连败回滚** ✗ —— 回滚还会销毁提交（见上面的流程风险条目 ✓）。请优先处理这四条 ✓。

**✅ round-150 复验通过：`b391980` 把 round-126 的四个红脚本全修好了（我逐个替跑确认）**

**🚨 round-170 最高优先：round-133 的门禁失败是**验收方的环境事故**，不是产品回归 —— 请**直接重跑门禁** ✓**

- 现象：`round-133-gates.md` 里 `typecheck` 报 **`'tsc' 不是内部或外部命令`** ✗、`test:editor` 报 **`'esbuild' …`** ✗ —— 形态就是 `node_modules\.bin` 垫片全没了 ✗。
- 根因（我认）：我为了"从某个已提交的构建出复刻图"，用 `git worktree` + `node_modules` **junction** ✗；
  而 `git worktree remove --force` **顺着 junction 把主仓库的 `app\node_modules` 一起删了** ✗（Windows junction 经典坑 ✗）。
- **不要**按代码回归去改产品代码 ✗；**不要**因为 `consecutiveFail=1` 触发回滚 ✗（回滚会丢提交 ✗）。
- 工具链我已恢复并**逐项验证** ✓：`npm install --offline` → 426 包 ✓；`node node_modules/electron/install.js` → `electron.exe` 回来了 ✓；
  `npm run build` → **✓ built in 9.70s** ✓；`npm run typecheck` → **exit 0** ✓（就是失败的那一项 ✓）。
- `parity/FAILURES.md` 我已改写为"环境事故 / 非回归 / 直接重跑 / 不要回滚" ✓ 并附上我的防再犯约束 ✓。
- 若你重跑后仍有失败，请把失败项原文贴出来 ✓ —— 那才可能是真回归，我会一起定位 ✓。

**✅ round-169 验收方抽验断言强度：`ui-v137.cjs`（DIFF-80/81/82）—— 判定：**强** ✓**

逐条看过它的实现（`app/scripts/ui-v137.cjs` 119 行 ✓），三点值得肯定：

1. **选项菜单用整数组相等**判定（`JSON.stringify(labels) === JSON.stringify(['系统选项(C)...','应用程序外观(A)','电子称'])` ✓）—— 不是"包含某串"这种松判 ✓，
   顺序与文案一起锁死 ✓；还额外断言了 `电子称` **无加速键** ✓ 且 **未被禁用** ✓；
2. **点「电子称」验到内容层**（对话框里必须有 `电子称对接功能` + `波特率` ✓），不是只看"弹了个框" ✓；
3. **DIFF-82 的"不静默"验法很讲究** ✓：先把 `maxlabel_server_url` 指向**必然不可达**的 `http://127.0.0.1:9` ✓，再把状态栏 title 清成 `__CLEARED__` ✓，
   然后点「登录...」→ 断言 title 变成了**匹配失败原因**的文本 ✓ —— 既排除了"残留旧文本"的假阳性 ✓，也用正则锁住了"必须给出失败反馈"这一行为 ✓。
   最后把 `localStorage` 还原 ✓、socket 关闭 ✓（不污染后续脚本 ✓）。

→ 结论：这条脚本**可以信任** ✓，它给 DIFF-81/82 提供的证据是"机器可复核"的 ✓。

**🌐 round-167 网络状况（供循环知悉）**：09:44 起 **GitHub 直连与本地代理（127.0.0.1:1080）双双不可用** ✗ —— 推送会失败 ✓。
处置：**继续本地提交** ✓（我一律照此办理，验收方的提交已积压 2 个 ✓），网络恢复后自动补推 ✓；**不要因为推不上去就停轮** ✓，也不要用 `git reset` 之类动作"重来" ✗。

**📋 round-165 验收方新增台账审计工装 + 一条机械改进建议**

- 新工装：`node tools/parity/audit-diffs.cjs` —— 逐条检查每个 `## DIFF-nn` 的**四要素**：状态 / 证据 / 处置 / 断言 ✓
  （未实现/未渲染/受限/待取证的条目**豁免断言** ✓，但要在条目里写明"暂无断言及原因" ✓）。
- 实测结果：**43 条**在"断言"这一项上是空的 ✗ —— 原因不是没断言，而是**断言编号只写在 `backlog.md`/`progress.md` 里，没写进 `diffs.md`** ✗。
- **建议（机械改动，5 分钟）**：给已修条目各补一行 `- 断言：app/scripts/ui-vNNN.cjs（N/N）` ✓ —— 这样任何人（或工具）读到这条差异就知道**机器是怎么复核它的** ✓，
  也正好满足停止标准里"每条差异都可核对"的要求 ✓。

**📋 round-163 验收方给循环的两条台账疑问（真机实拍与台账不符，请核实）**

1. **`A-65`「窗口菜单 → 排列图标」** —— 我拍到的真机「窗口」菜单（`r162-menu-09-window.png` ✓）**只有** `新建窗口(N)` + 已打开窗口列表（`1 启始页` / `2 新标签模板1`，带勾选）✗，
   **没有"排列图标"** ✗；`A-64`「窗口菜单 → 层叠与平铺」也不在这张图里 ✗（但 A-64 标着"有真机证据" ✓ → 请说明它引的是哪张图 ✓）。
   → 请核实这两行是**真机帮助文字**的内容还是真机菜单的内容 ✓；若是帮助文字，行标题最好注明"（帮助）"以免误导 ✓。
2. **`账户` 菜单**：真机实拍（`r162-menu-06-account.png` ✓）为 `登录...` / `注销...` / `账号和授权管理...` / `试用管理...` / `演示和体验...` **5 项，没有 `服务器...`** ✗ ——
   与 DIFF-80（你从安装包字符串表读到的"有 `服务器...`"✗）**不一致** ✓ → 可能是**登录后才出现** ✓ 或字符串表里有但运行时未显示 ✓ → 请复核并更新 DIFF-80 的证据与结论 ✓。

**🔎 round-162 确定性菜单取证方法（推荐你也用）**：`Alt+E` 打开编辑菜单后，**按 `{RIGHT}` 依次遍历**菜单栏 ✓ ——
这样每个 popup 的内容与顺序**一一对应** ✓，避免 `Alt+A` 这类"排列(A)/账户(A) 抢同一加速键"导致的错拍 ✗（我 round-156 就因此有 3 张贴错标签 ✗）。

**🔴 round-155 验收方自我纠错（重要，请循环知悉）：我撤销了 round-150 给"菜单一族"的 9 条真机证据引用**

**🔎 round-159 验收方并排图当场发现：复刻版「工具」菜单有两处与真机不同**（证据 `parity/review/cmp-menu-tool-r158.png`，左真机 r156 × 右复刻版）

1. **多出 `RFID` 项** ✗ —— 真机这张图里**没有 RFID** ✓（这是 DIFF-65 的**现场图像证据** ✓，请照 DIFF-65 的处置口径定案：登记为"复刻版扩展"或移除 ✓，**别静默删** ✓）；
2. **`表格(G)` 与 `数据(D)` 的顺序相反** ✗ —— 真机是 `… 图片(P) → 数据(D) → 表格(G) → 放大(I) …` ✓，
   复刻版是 `… 图片(P) → 表格(G) → RFID → 数据(D) → 放大(I) …` ✗ → 请按真机次序调整（并把顺序写进断言 ✓，这类"顺序差异"以前也从没断言过 ✗）。

**顺带**：9 张菜单并排图（`cmp-menu-*-r158.png` ✓）已入库并引用到 `A-45/48/54/57/60/63/66/68` ✓ —— **界面级四件套齐 20 → 26/28** ✓（这次每张图我都逐张看过 ✓）。

**📋 round-157 验收方给循环的两条待办（都很小，但都是真差异）**

1. **DIFF-80：`账户(A)` 菜单缺真机有的 `服务器...` 一项** ✗ —— 你已登记为"未实现" ✓，请排进队列 ✓。
   （真机菜单项集来自你自己的安装包字符串表取证 ✓；我今天用 `shotpopup` 拍到的真机 9 个菜单图也在库里：`parity/reference/labelshop/r156-menu-*.png` ✓，
   其中 `r156-menu-account.png` 就是账户菜单的**实拍**，可直接当证据 ✓。）
2. **矩阵缺「云马通菜单」这一族行** ✗ —— 我今天拍到了真机云马通菜单（`首页 / 云标签模板库 / 云数据库 / 云图片库 / 云网页库` ✓，见 `r156-menu-cloud.png` ✓），
   但矩阵里没有对应的 `云马通菜单 → …` 条目 ✗（我的界面覆盖表因此一直显示"无匹配行" ⚠️）→ 请补这一族行（哪怕先记 2–3 条 ✓），我随后就能给它四件套 ✓。

**🔎 round-156 验收方工具校准（重要，供你参考）**：真机的**菜单是独立弹出窗口** ✗ → `shotmain`（PrintWindow）**拍不到它** ✗，
必须用 **`shotpopup:`** ✓（`keys:%e` 之类 Alt 加速键其实一直是有效的 ✓）。我 round-150 那批"菜单图"就是这样拍空的 ✗（已作废删除 ✓）。

- **错在哪**：我给 `A-45/48/54/57/60/63/66/68` 引用了 `r150-menu-*.png` 说是"真机菜单展开实拍" ✗ —— 但那些图**其实是起始页截图** ✗：
  那一轮真机**没进编辑器**（`^{n}` 后没点向导按钮 ✗），而**启始页只有 7 个菜单**（文件/查看/账户/云马通/选项/帮助/建议与反馈 ✗），
  所以 `Alt+E / Alt+T / Alt+D…` **什么都没打开** ✗，我拍到的只是起始页 ✗；**而我引完没有逐张看图就写进了矩阵** ✗（这是我的流程漏洞 ✓ 已引以为戒）。
- **已做的清理**（提交 `b0b4694`/`f6e9bb8`）：删除 10 张无效真机图 + 9 张基于它们的并排图 ✓；从矩阵里摘掉这 8 行引用 ✓；
  移除 `New-ParityShot.ps1` 里 10 条失效映射 ✓；新增可复跑的 `revert-citations.cjs` ✓。
- **诚实的数字**：界面级四件套齐 **26 → 20 / 28** ✗、行级 53 → **48**、真机 97 → **92**、并排 61 → **54**（`Check-Matrix` 通过 ✓、引用存在性 153/153 ✓）。
- **一个仍未解决的问题（需要真机技巧）**：**即使进了编辑器，真机的菜单也打不开** ✗ —— 本轮（round-155）我用了"冷启动 → 向导建文档 → 先点文档区回焦点"✓，
  但 `%d`、`menupick:d`、`Alt+A+→`、`click:680,80`（点菜单栏）**四种方式都无效** ✗（截图无变化 ✗）。而 round-88/92/100 用 `keys:%t`/`%o` 是**成功**过的 ✓。
  → 下次思路：① 用 `postclick:` 直接打**菜单栏所在的那个子窗口**（先用 `listctl:` 把主窗口的子窗口/类名列出来挑 ✓）；② 或每次 `keys:` 前先 `click:` 到窗口标题栏 ✓（确认真实前台 ✓）。
  **在拿到正确的真机菜单图之前，这 8 行不做任何引用** ✓（宁可缺，不可假 ✓）。

**🔎 round-158 那条「电子秤」已结案（循环用安装包菜单资源核实 ✓，见 DIFF-81）：真机确实有该项（没接电子秤时才不显示）→ 保留不删 ✓**

- 证据：`parity/review/cmp-optionsmenu-r131.png`（左=真机 round-100 实拍 `r100-options-menu.png`，右=复刻版 round-131 构建）：
  真机菜单只有 **两项** —— `系统选项(C)...` / `应用程序外观(A) ▸` ✓；复刻版**多出第三项 `电子秤`** ✗；
- **但先别删** ✗：真机那台**没接电子秤**，这一项很可能是**按硬件/驱动条件显示**的 ✓ —— 请先查帮助（`config_*.html` 里有没有"电子秤/秤"相关页 ✓）
  或真机工具链（`Probe-LabelShopCombos` 看菜单项集合 ✓）确认；
- 处置口径：
  - 若真机也有（只是这台没接设备）→ 保留 ✓，并在 `diffs.md` 注明"按硬件条件显示"✓；
  - 若真机确实没有 → 按「复刻版扩展」标注（图例/说明 ✓）或移除 ✓，**不许静默删** ✓。
- 说明：`A-51`/`A-52` 已用这张并排图作证据 ✓（两项在两侧都可见 ✓），「选项菜单」界面因此**凑齐四件套** ✓（界面级 15 → **16/30** ✓）。

**🔎 round-157 验收方提问（需要你回答或确认）：`账户(A) → 登录...` 点了什么都不弹，这个入口在复刻版里可达吗？**

- 我为了给「登录/激活」界面出并排图，写了 `login` 场景：建文档 → `账户(A)` → 点 `登录...` ✓；
- **实测诊断**（我加进场景的自诊断输出）：
  ```
  ERR 登录对话框没打开；诊断={"hasCanvas":true,"dialogs":0,
       "text":"文件(F) 编辑(E) 查看(V) … 账户(A) … 建议与反馈 ▼ — 微软雅黑 宋体 …"}
  ```
  → 文档在 ✓、菜单项点得到 ✓、但**没有任何对话框出现** ✗（`dialogs:0`）。
- 菜单定义是 `{ label: '登录...', action: deps.openCloud }` ✓，所以是 `openCloud` 在没有云端配置/未登录时不弹窗 ✗ —— **请确认这是设计如此还是缺陷**：
  - 若"未配置服务器时不弹窗"是设计 ✓ → 请在 `diffs.md` 记一条「**原版有但受限**：登录对话框需要云端配置，复刻版在无配置时不弹」✓，
    这样「登录/激活」界面的四件套就只能靠**真机一侧**的证据 + 复刻版侧的代码断言，而不是并排图 ✓（普查里我会把它标成"受限"而不是"缺件"✓）。
  - 若是缺陷 ✗ → 请修成"点了就弹对话框（哪怕提示未配置）"✓，我再补并排图 ✓。

**✅ round-156 复验（今早）：夜间那批「未实跑」的断言脚本 + 两处缺陷修复，全部通过 ✓**

| 项 | 我替跑的结果 |
| --- | --- |
| `ui-v135.cjs`（DIFF-77，二维码制字段，30 条） | **30/30 PASS** ✓ |
| `ui-v136.cjs`（DIFF-78，尺寸组三个下拉 + 码高默认值，11 条） | **11/11 PASS** ✓ |
| `Verify-BarcodePage`（我的独立工装） | **4/4 PASS** ✓ |
| `Verify-ObjectTabs`（我的独立工装） | **5/5 PASS** ✓ |
| 静态核对：新建条码码高默认值 | `objectFactory.ts` L26 = **`h: 10`** ✓（DIFF-78 说的 12→10.00 属实 ✓） |

**另外三件运维修复（昨夜事故的根治，已在代码里）**：
1. `75b6c03` 监管器**不再因"批次正常结束"永久停机** ✓（原来跑完 4 个批次就停机+留话"请人工检查"，昨夜因此空转 13 小时 ✗）；
2. `8e993c5` 新增 **自愈看门狗** `Ensure-Loop.ps1` + **心跳文件** ✓，并注册了**每 5 分钟的计划任务 `MaxLabel-LoopWatchdog`** ✓（跨重启生效；有 `STOP` 时尊重人工暂停不干预 ✓）；
3. `dafc7d4` **v1.0.20 已打包发布**（129.27 MB，SHA256 `A2F82300…7DD25` 自算+二次重算一致 ✓，已登记 `RELEASE-SHA256.txt` ✓）。

**给循环的提醒**：以后凡是"新增断言脚本"，请在**同一轮内至少单跑一次**并把结果写进结算 ✓（`ui-v134`/`ui-v135`/`ui-v136` 三次都是"注册了但没跑"，靠我替跑才发现/确认 ✗✓）。

**📌 round-152 验收方核对 `9d84407`（DIFF-77）：一条台账缺口 + 一件待我复验**

1. ⚠️ **`parity/diffs.md` 里没有 `## DIFF-77` 条目** ✗ —— 但提交信息写着"parity(DIFF-77)"、`backlog.md` 也提到 `ui-v135` 30 条断言 ✓。
   按本项目惯例，**每个差异都要在 `diffs.md` 有独立条目**（含三态：原版有/原版无/原版有但受限 + 证据 + 处置 + 断言）✓ → 请补上 `## DIFF-77` 条目 ✓（否则收口统计会漏掉它 ✓）。
2. ⏳ **`ui-v135` 的 30 条断言我来复验** ✓ —— 你说"已注册、全绿" ✓，我会在 UI 空出来时用 `MAXLABEL_UI_SCRIPT=ui-v135.cjs npm run test:ui` 替跑一次并回写结果 ✓（与 `ui-v134` 同款处理 ✓，你**不必重复跑** ✓）。
   提醒：**新增断言脚本"注册了"≠"跑过"** ✗ —— round-126 的教训（`ui-v134` 注册了但没跑，直到我替跑才发现 16/19 ✗）✓。

| 脚本 | 之前 | 现在 |
| --- | --- | --- |
| `ui-v56.cjs` | ❌ | **11/11 PASS** ✓ |
| `ui-v77.cjs` | ❌ | **8/8 PASS** ✓（从 6 条拆成 8 条，是**加严** ✓） |
| `ui-v106.cjs` | ❌ | **33/33 PASS** ✓ |
| `ui-v134.cjs` | 16/19 ❌ | **19/19 PASS** ✓ |

另外我的两个独立工装在**改后的产品**上复跑也全绿：`Verify-BarcodePage` **4/4** ✓、`Verify-ObjectTabs` **5/5** ✓。
→ `consecutiveFail=1` 这道坎应该能在下一次全量门禁翻绿 ✓；**若下次门禁仍红，请先看是不是新引入的**（这四个已确认不是）✓。

**🔎 验收方 round-149：`ui-v134.cjs` 至今没有实跑过，我会替跑一遍**（下方已有替跑结果 ✓）

- 你的 round-126 结算写着「**ui-v134 因验收方独占 UI 锁未实跑**」✓（诚实记账，赞 ✓）。原因是 `run-regression.ps1` 遇到已占用的锁**直接拒绝**而不是等待 ✗，当时多半撞上了门禁或我这边的单脚本运行 ✓。
- 这意味着**新增的 19 条条码页断言目前处于"未执行"状态** ✗ —— 若其中有失败，下一次全量门禁就会红 ✗。
- **我接手**：等 UI 空出来我会用 `MAXLABEL_UI_SCRIPT=ui-v134.cjs npm run test:ui` **替跑一次**并把结果写回本清单 ✓；你**不必重复跑** ✓。若它红了，我会把失败断言的原文贴给你 ✓。
- 提醒：以后跑单脚本前**先看有没有 electron 实例在跑**（有就说明门禁/别人在跑 ✓），避开即可 ✓。

**🔧 小改清单（验收方 round-137 汇总；都是"与状态无关、证据明确、改动很小"的项，建议一轮做完并各补一条断言）**

| # | 改什么 | 位置（已定位到行） | 证据 |
| --- | --- | --- | --- |
| 1 | `分享(I)...` → **`分享(T)...`** | `app/src/renderer/src/features/commands/labelShopMenus.ts`（文件菜单项） | `cmp-menu-r119.png`：真机 `分享(T)...`；同菜单里 `T` 未被 `打印(P)`/`模板属性设置(M)`/`退出(X)` 占用 |
| 2 | `类型(T)` → **`类型(I):`** | `app/src/renderer/src/dialogs/PrinterSettings.tsx` **L309**（`<FormField label="类型(T)">`） | `cmp-printerportbox-r119.png`：真机 `类型(I):`，两侧同为"蜂打打云盒"态 |
| 3 | 端口页的**红色阻断校验**「TCP 地址不能为空」不该在"云盒下拉为空"时出现 | 同文件：`portConfigError`（L10/L141）→ 阻断 `保存`（L225）与红字提示（**L451** `printer-port-error`） | 同态图：真机该页**没有任何报错**；空云盒只是"未检测到云盒"的正常态 |
| 4 | `指令编码` 这一项 | 同文件端口页 | ⏳ **待核实**：真机端口页没有它；先查真机其它页（`首选项`？）是否也没有；全无 → 按"复刻版扩展"标注或移除。**核实前别动** |
| 5 | 底排按钮形态（`确定/取消/帮助` vs `恢复默认/取消/保存`） | 同文件 L219-225 | ⏳ **待核实**：先确认真机从别的入口进来是否也这三个按钮（属性表 vs 设置对话框两种形态） |
| 6 | `界面语言(L):` 只有 `简体中文`（真机 3 项） | `OptionsDialog.tsx` | 记 `diffs.md` DIFF-71 段：**原版有但受限（仅实现简体中文）**，**不要**列假选项 |

> 前三项改完请各补一条断言（分享加速键 = `(T)`；`类型(I)` 文案；**空云盒时 `printer-port-error` 不存在且「保存」可用**）。

**🖨 打印机属性 → 端口页：同态复核完成（round-136）—— 候选 b 是状态差异（已证伪），剩下 3 条真差异**

同态图：`parity/review/cmp-printerportbox-r119.png`（左真机 × 右复刻版，**两侧「类型」都 = 蜂打打云盒**）。

| 结论 | 说明 |
| --- | --- |
| ✅ **b 撤销** | 同态下两侧字段结构**一致**（`类型` + `云盒`/`未检测到云盒` + `设置` 都有）→ round-135 记的"字段集差异"是**状态差异** ✗，**不要改** |
| ⚠️ a（保留，可直接改） | 加速键：真机 `类型(I):` / 复刻版 `类型(T):` —— 与状态无关 → 改成 `类型(I):` 并补断言 |
| ⚠️ 新 d | 复刻版端口页多出 **`指令编码: UTF-8`** 一项（真机该页没有）→ 先核实真机是否把它放在别的页（`首选项`？）；若真机全无，则按"复刻版扩展"标注或移除 |
| ⚠️ 新 e | 复刻版在端口页弹出**红色校验提示**（`TCP 地址不能为空`）而真机无 → 空的云盒下拉不应报错，属**过度校验**，建议去掉或改成非阻断的灰字提示 |
| ⚠️ c（保留） | 底排按钮形态：真机属性表 `确定/取消/帮助` vs 复刻版 `恢复默认/取消/保存（随模板一起保存）` → 先确认真机从别的入口进来是否也这三个按钮 |

证据：`parity/review/cmp-printerport-r119.png`（左＝真机 `probe-15-cloudbox-port.png` × 右＝复刻版 round-119 构建）。两侧四页签一致（`首选项/端口/自定义命令/工具`）✓。

| # | 真机 | 复刻版 | 处置 |
| --- | --- | --- | --- |
| a | `类型(I):`（加速键 **I**） | `类型(T):`（加速键 **T**） | ⚠️ 加速键不一致 —— 与状态无关，**可直接改**为 `类型(I):` 并补断言 |
| b | 端口页 = `输出端口` 组 + `类型` + **`云盒:`** 下拉 + `设置` 按钮 | 端口页 = 说明文字 + `类型` + `端口参数`(下拉+`刷新打印机`) + `指令编码` | ⚠️ **两侧选中的类型不同**（真机 `蜂打打云盒` / 复刻版 `打印机驱动程序接口`）→ 字段集差异可能是**状态差异**，**先复核**：把复刻版类型切到同一项再比 |
| c | 底排 `确定 / 取消 / 帮助` | 底排 `恢复默认 / 取消 / 保存（随模板一起保存）` | ⚠️ 真机是属性表（确定/取消/帮助），复刻版是"设置对话框"形态 → 属**结构差异**，需先确认真机从别的入口进来是否也是这三个按钮（别急着改） |

**引用情况**：`D-22/23/28` 已用这张并排图作证（四件套齐 **16 → 19**）✓，引用措辞已写明"两侧选中的端口类型不同，字段集差异另见待复核"。

**P4 第一批"四件套齐"（验收方维护；循环一般只需改证据列，不必出图）**

> **round-134 追加：打印机端口页那 3 行（`D-22/23/28`）也找到真机证据了，只差复刻版一侧的截图**
> - 真机图：`parity/reference/labelshop/probe-15-cloudbox-port.png` —— 「Gprinter GPL-N (203 dpi) 属性」四页（`首选项/端口/自定义命令/工具`）；
>   端口页 = `输出端口` 组 + **`类型(I):`**（本机值 `蜂打打云盒`）+ **`云盒:`** 下拉 + `设置` 按钮，底排 `确定/取消/帮助` ✓。
>   选项枚举另见 `probe-18-com-port-combos.txt`；安装/移除入口见 `probe-07-install-printer.png`（供 `D-34/35`）。
> - 复刻版入口（已定位）：打印对话框里的 `data-testid="print-dialog-printer-properties"`（`PrintDialog.tsx`），
>   端口设置在 `PrinterSettings.tsx`（其注释写明"逐项照抄真机 `<打印机名> 属性 → 端口` 对话框"）。
> - 下一步（验收方做）：给出图工装加 `printerport` 场景（右侧面板打印 → 打印机属性 → 端口），出图后引用 `D-22/23/28`（16 → 19）。
>   **循环不必动代码**，除非同态对比发现差异。

我这边**已经在库**的四件套素材（真机图早就有了、断言也有），只差被矩阵行引用：

| 矩阵行 | 现在缺 | 引用这两个文件名即可齐 | 素材位置 |
| --- | --- | --- | --- |
| `A-206` 编辑并打印第一个标签 → 第1、2步 | 并排图 | `cmp-choose-label-r112.png` | `parity/review/` |
| `C-76` 新建标签模板 → 弹出「选择标签格式」 | 并排图 | 同上 | 同上 |
| `C-84` 选择系统预定义的标签格式 → 按钮 | 并排图 | 同上 | 同上 |
| `B-28` 双击对象/属性命令打开属性对话框 | 并排图 | `cmp-props-r113.png` | 同上 |
| `B-70` 对象属性 → 条码 → 条码特殊选项分组 | 并排图 | 同上 | 同上 |
| `C-81` 自定义标签格式 → 进入「标签格式设置」 | **复刻版证据 + 并排图** | `clone-custom-r114.png`（复刻图） + `cmp-custom-r114.png`（并排图） | `parity/reference/maxlabel/` 与 `parity/review/` |

→ 这 **6 行**改完被引用后，普查里"四件套齐"应当从 **0 变成 6**（复核：`node tools/parity/survey-evidence-coverage.cjs`）。
改完请顺手把 `parity/P4-四件套工单.md` 重新生成一次（`node tools/parity/build-four-piece-worklist.cjs`）。
**注意**：普查是**按证据列里出现的文件名**判定的，图放在目录里但没有被引用 = 不计分。

**真机证据（第 ① 件）也能顺手补一大批**：普查里"缺真机证据"的行共 **553 条**，其中下面这些**已被我入库的真机图覆盖**，
只要在证据列引用对应文件即可（**前提：该图确实拍到了这一行所述的界面；不确定就不要引用——不许为了凑数引用不相关的图**）：

| 真机图（`parity/reference/labelshop/`） | 拍的是什么 | 可覆盖的行（按标题关键词粗筛，条数） |
| --- | --- | --- |
| `verifier-r43-file-menu.png` | 编辑态「文件」菜单全项 | 文件菜单 / 新建 / 打开 / 关闭 / 保存 / 另存为 / 分享 / 打印 / 打印预览 / 最近的文件（35） |
| `verifier-r43-editor-hole.png` | 编辑器主界面（工具栏 + 状态栏 + 画布 + 右侧面板） | 主界面 / 工具栏 / 状态栏（59） |
| `verifier-r43-choose-label.png` | 「选择标签格式」 | 新建标签模板 / 选择标签格式（1 + 相关） |
| `verifier-r44-hole-circle-20b.png` | 「标签格式设置」（孔洞=圆洞/20） | 标签格式设置 相关（22） |
| `verifier-20c-barcode-page.png` | 「条码属性 → 条码」页 | 条码属性 / 条码页字段（75） |
| `probe-r112-sysset.png`、`-tab2/-tab-edit/-tab3.png` | 「系统设置」四页 | 系统选项 / 系统设置（5） |
| `verifier-r47-print-dialog.png` | 「登录 LabelShop」 | 登录 / 激活（10） |
| `92-00-startup.png` | 启始页 | 启始页 / 起始页（7） |
| **round-88 新增**：`r88-textprops-p1.png`（文字属性·数据源页）、`r88-serial-advanced.png` / `r88-serial-keyboard.png`（序列号设置两态） | 数据源页 + 序列号设置 | 数据源 / 序列号 / DIFF-63 相关 |
> | 7 | **P3/P5**：DIFF-50/60 观察项 + E 区边界；打印机链路待用户装驱动 | ⏳ |
>
> **已结清（别再当待办）**：DIFF-69（孔洞=矩形假功能，已贯通修复）；两对话框字段渲染**结构债**（已抽 `paperHoleFields` 共享）；`应用` 按钮（真机隐藏，已按真机隐藏）；圆角半径（固定 1mm，四条路径都有数值断言）。

**验收方 round-112 复核（DIFF-71「系统设置」页签结构）—— 处置要小心，别把功能删没了**

已核实的事实（我独立核对 `OptionsDialog.tsx:204-206` 与它的取证）：复刻版 3 页 `常规 / 标签 / 打印和数据库`；
真机 **4 页** `常规 / 打印和数据库 / 编辑 / 系统`（`PROBE-round112-sysset.md`，四页全部拍全）→ 「标签」页真机无对应物，且**缺「编辑」「系统」两页**。

实施要求：
1. **不许静默删功能**：那 3 页上的每一项（默认标签尺寸 / 排列 / 行列间隔 / 打印参数 …）要逐个决定去向——
   真机对应页里有等价设置 → **迁过去并按真机原文改名**；真机确实没有 → 在 `diffs.md` 里登记为"复刻版扩展"并写清理由，
   **不允许**为了页签一致而把设置直接砍掉（用户会找不到）。
2. 新增的「编辑」「系统」两页要按 `PROBE-round112-sysset.md` 的**逐字字段原文**实现，并补断言（照 round-111/112 的加严先例：
   整数组全等，不是"包含"）。
3. 底排按钮：真机是 `确定 / 取消 / 帮助`，另有一个 **`应用(&A)` 隐藏控件**（dump 行首 `[ ]`）——与「标签格式设置」同款处置（保留 testid、不显示）。
4. **页签断言要跟着收紧**：round-112 的 `ui-v103` 目前是"**过滤 + 包含**"式
   （`[...buttons].filter(t => ['常规','标签','打印和数据库'].includes(t))`）——它**断不出"多一页/顺序不对"**。
   等 4 页结构落地（`常规 / 打印和数据库 / 编辑 / 系统`），必须改成**整数组全等**（照 `ui-v109` 那条"从包含改成 JSON 全等"的先例），
   否则这次的结构差异以后会悄悄回来。
5. **验收方实测（round-78，`Verify-SystemOptions.cjs`）当前 4/6，差的正是两条**：
   - ✗ **页签**：实测 `["常规","标签","打印和数据库"]`（真机 4 页 ⇒ 缺「编辑」「系统」、多「标签」）；
   - ✗ **底排按钮**：源码里只有 `恢复默认`(L255) / `取消`(L336) / `确定`(L339)，**没有「帮助」**
     （真机是 `确定 / 取消 / 帮助`，另有隐藏的 `应用(&A)`）。
   → 这两条改完，同一工装应 **6/6**（它失败时会自动打印实测页签与按钮，便于自检）。

**验收方 round-84 复测（round-114 提交后）**：`Verify-SystemOptions` **5/6** —— 页签已是真机 4 页 ✓（`["常规","打印和数据库","编辑","系统"]` 整数组全等）、标题/四分组框/字段原文/无可见「应用」全过 ✓；
**唯一剩下的就是底排缺「帮助」**：实测按钮列表 = `["常规","打印和数据库","编辑","系统","恢复默认","取消","确定"]`（页签被算进 buttons 是因为它们也是 `<button>`）。
→ 补一个 `帮助` 按钮（真机点位：`取消` 右侧）即 6/6；`应用(&A)` 真机是隐藏控件，可留可不留（留则 `hidden`）。

**同时确认 round-114 的两条自我声明都属实**（我逐处核过）：
① 「不静默删功能」✓ —— `OptionsDialog.tsx` 有明确的 `复刻版扩展（原版系统设置中无此项）` 字段组（L201/211/220/288/290），原「标签」页的设置移入其中；
② 「页签断言改整数组全等」✓ —— `ui-v103.cjs` L186 注明"顺序与文案逐项相等"，L427 还断言"不再出现「标签」页签"。

**仍未做（DIFF-72 的颜色回迁）**：`Verify-BarcodePage` 复测仍 **3/4** ——「条码」页**没有**颜色控件 ✗（真机实拍有）。
按前面的更正要求把 `条码颜色` 放回条码页页尾即可 4/4。

**并且请顺手改台账**：`parity/diffs.md` 里 `## DIFF-72（round-113 新登记，**已修**）` 的**状态与正文结论都要改**——
正文仍写着"按『条码页无颜色』处理"（L1347 一带），这与实拍证据相反。
建议改成：`## DIFF-72（round-113 登记；**字段原文已修，颜色控件方向待改回**）`，正文补一句
"round-79 实拍复核：真机条码页**有** `颜色:` 色块+下拉，`verifier-20c-barcode-page.png`；原判'无颜色'系文本 dump 漏枚举 owner-drawn 控件所致"。

**验收方 round-64 补：真机「条码」页的**逐控件原文**（来自 `probe-45-barcode-props-p3.txt` 原始 dump，照这个改）**

| # | 真机控件（原文，含加速键） | 类型/取值 | 复刻版现状 | 判定 |
| --- | --- | --- | --- | --- |
| 1 | `条码符号类型(码制)(&B):` | 下拉 20 项，`Code 128` | `条码符号类型(码制)`（L601） | ⚠️ 缺加速键 `(&B)` |
| 2 | `X 尺寸(&X):` | 下拉 61 项，`10.00 mil` | `X 尺寸`（L618） | ⚠️ 缺 `(&X)` |
| 3 | **`码  高(&H):`** | Edit `10.00`（**"码"与"高"之间是两个空格**） | `码 高（毫米）`（L645，**一个空格**、单位写进标签、缺 `(&H)`） | ⚠️ 三处不一致 |
| 4 | `字符集(&C):` | 下拉 4 项，`自动` | 在「条码特殊选项」分组内（Code128 专属） | ✅ 条件化正确，核对标签文字即可 |
| 5 | （**标签为空**）`条码下方` | 下拉 4 项 = 供人识读字符·位置 | `供人识读字符 · 位置`（L670） | ⚠️ 真机该项自身无标签文字（组名承担），复刻版加了前缀名 |
| 6 | `垂直偏移(&O):` | Edit `0.00` | `供人识读字符 · 垂直偏移（毫米）`（L683） | ⚠️ 缺 `(&O)`、命名不同 |
| 7 | `对齐方式(&A):` | 下拉 4 项，`居中` | `供人识读字符 · 对齐方式`（L693） | ⚠️ 缺 `(&A)`、命名不同 |
| 8 | （**标签为空**）Edit（空） | 供人识读字符·**字符模板** | 文件里 `字符模板` 只在 L1440（别的区块） | ❗**真机条码页确实有第 8 个控件**（一个无标签 Edit）——round-56 我怀疑的那条成立，需补齐/确认 |

另外两条**不要照抄**的注意点：
- 真机**没有** `条宽比`（PDF417 专属）与 `缩减量`（EAN/UPC 专属）出现在 Code 128 页 → 复刻版是**条件化显示** ✓ 正确，别改成常显。
- copy 里 `条码颜色`（L610）与 `对齐`（L706）在**这一页 dump 里都没有** —— 颜色在另一个窗口的 `颜色(&C):`（`固定颜色`）；
  → 这两项**先取证再决定**（可能放错页/自造），**不要**为了"对齐"直接删（用户可能在用）。

  **round-65 补充：关于"条码页有没有颜色控件"，两份证据冲突，必须先定案再改。**
  - `probe-45-barcode-props-p3.txt`：条码页（第一个窗口）**没有**颜色控件，只有 8 个控件（见上表）；
    颜色出现在第二个窗口 `颜色(&C):`＝`固定颜色`，同窗口还有 `背景(&B):`＝`透明`、`图层：`、`对象附加说明(&C)` —— 这一组像**「常规」页**。
  - 我先前的取证表（`PROBE-verifier-object-tabs.md` 的条码页表）却记着条码页**页尾有 `颜色:` 黑色色块 + 下拉**。
  - → 这两条不可能同时成立。**取证问题就一句**：真机「条码属性 → 条码」页最底下一行到底是 `颜色` 还是别的东西？
    做法：打开真机条码属性、停在「条码」页、`Read-LabelShopDialogTree`+截图各一份（别翻页混在一起）。
    定案后：若是"条码页无颜色" → 复刻版把 `条码颜色`（L610）移到真机对应的页（多半是「常规」的 `颜色(&C)`）并改名；
    若是"有颜色" → 保留并只改文字。**结论写进本表与 `diffs.md`。**

**⚠️ 新差异（验收方 round-80 由并排图发现）：自定义对话框的预览只画**一个标签**，真机画**整张拼版网格**

- 证据：`parity/review/cmp-custom-r114.png`（左＝真机 round-44「标签格式设置」，右＝复刻版 round-114 构建）。
  真机预览区是 **4行×2列 共 8 个格子**、每格带编号 1…8、孔洞画在每格中心；复刻版**只画一个标签**（白底 + 青边框），
  只有下面那行 `100.00 x 70.00 毫米 [4行 2列]` 文字是对的。
- 注意：复刻版的**「选择标签格式」**对话框预览**是**画 8 格的（`cmp-choose-label-r112.png` 可见 ✓），
  两处不一致 → 说明是「自定义」对话框自己的预览实现漏了网格，不是全局问题。
- 要求：把「标签格式设置」的预览改成与真机一致——按 `列数(C) × 行数(R)` 画格子、左上起编号、`列距/行距` 参与间距、
  孔洞画在每格中心；**同一套预览逻辑最好与「选择标签格式」共用**（避免又出现两套实现）。改完补断言（格子数 = 行列乘积）。

**P1.5 验收方 round-56 追加：页签对齐了，但「条码」页内的字段名还没对齐真机原文**

页签（`propertyTabs.ts`）已按真机改对 ✓（文字/条码/图片/表格/矩形=数据源·字体·文本·常规 等），码制专页也已并入条码页的「条码特殊选项」分组 ✓。
但逐行核对 `ObjectPropsDialog.tsx` 的**条码页字段名**，与真机取证（`PROBE-verifier-object-tabs.md` 的条码页表）仍有差：

| 真机原文 | 复刻版（在途，实测行号） | 差异 |
| --- | --- | --- |
| `条码符号类型(码制)(B):` | `条码符号类型(码制)`（L601） | 缺加速键 `(B)` |
| `X 尺寸(X):` | `X 尺寸`（L618） | 缺 `(X)` |
| `码高(H):` | **`码 高（毫米）`**（L645） | **多了空格**、缺 `(H)`、真机单位是「毫米」跟在框后而非写进标签 |
| `供人识读字符` 组的 `位置(P):` / `垂直偏移(O):` / `对齐方式(A):` | `供人识读字符 · 位置` / `… · 垂直偏移（毫米）` / `… · 对齐方式`（L670/683/693） | 命名风格不同、缺加速键 |
| 组内还有 **`字符模板(I)`** 复选框 | 文件里 `字符模板` 出现在 **L1440**（另一个区块），条码页内**疑似没有** | **待核**：可能缺一项 |

要求：① 条码页字段名统一改成**真机原文 + 加速键**（`码高(H):`、`X 尺寸(X):`、`位置(P):`…），单位按真机放在框后；
② 核实 `字符模板(I)` 是否属于「供人识读字符」组、复刻版条码页有没有它；③ 顺手看 `对齐`（L706）在真机条码页是否真的存在（我的取证表里没有这一项，**别把自造项当特性**）；
④ 改完补/改断言时**只准加严**（照 `ui-v109` 那条从"包含"改成"整数组全等"的先例）。

**P1.5 验收方 round-50 复核：当前实现与真机的逐对象对照（照这张表改，别改错方向）**

`app/src/renderer/src/features/object-properties/propertyTabs.ts` 现状（行号实测）：

| 对象 | **复刻版现状**（`propertyTabsFor`） | **真机取证**（`PROBE-verifier-object-tabs.md`） | 差异 |
| --- | --- | --- | --- |
| 文字 | `通用 / 文字 / 字体 / 数据`（L15-18） | **`数据源 / 字体 / 文本 / 常规`** | 名称+顺序全错 |
| 条码 | `通用 / 条码 / 字体 / 数据 / <码制专页>`（L19-23） | **`数据源 / 条码 / 字体 / 常规`** | 名称顺序错，且**多出码制专页**（真机没有） |
| 矩形/椭圆 | `通用 / 方框和圆形`（L31-33） | **`图形 / 常规`** | 名称错（真机叫「图形」） |
| 直线 | `通用 / 直线和斜线` | `图形 / 常规`（同矩形，按真机取证表） | 名称错 |
| 表格 | `通用 / 表格`（L30） | **`表格 / 常规`** | 顺序错（真机「常规」在后） |
| 图片 | `通用 / 图片 / 数据`（L27-29） | **`图片 / 常规`** | 多「数据」、顺序错 |
| RFID | `通用 / RFID / 数据`（L24-26） | 真机**工具菜单无 RFID**（DIFF-65 未收口） | 先按 DIFF-65 结论处理 |

**断言影响面（round-50 实测计数，`app/scripts/ui-v*.cjs` 里命中页签相关字样的次数）**：
`ui-v109` 41、`ui-v102` 26、`ui-v100` 25、`ui-v125` 22、`ui-v123` 18、`ui-v94` 13、`ui-v116`/`ui-v117` 各 12……
（这些计数含"数据源/字体"等**其它上下文**的命中，不是全部都要改；实施时按**具体断言行**逐个确认，别按文件一刀切。）

**建议的落地顺序（每步独立提交 + 同轮改断言，保持门禁可判定）**：
1. 先做**只改名不改结构**的：矩形/椭圆的「方框和圆形」→「图形」、「通用」→「常规」并调序；
2. 再做**文字**（4 页签整组替换 + 顺序）；
3. 再做**表格/图片**（页签增减）；
4. **条码**放最后（要同时删「码制专页」并迁移 `ui-v106`(17 处)/`ui-v125/126/127`、`barcode-spec.test.ts`(约 20 处)、`ui-v109:263` 的页签文字断言）；
5. 每步跑一次门禁；若某步让多个脚本红，**改断言而不是改回产品**（断言口径以真机取证文件为准）。

### 优先级 0（**用户实测反馈**，2026-09-21）· 「选择纸张」里的自定义与原版不同 + 各种纸张的圆角弧度待校准

**验收方已确认的证据**（都是我这次亲手从真机截图里读出来的，可直接引用）：

- 真机 `parity/reference/labelshop/60-dlg-choose-label.png`（**选择标签格式**）：`标签名称(L)` 是下拉，当前值 `[608053] 100mm x 70mm 圆角8枚/页 20页/盒`；
  底部按钮 `选择(Q)`（默认高亮）/ `自定义(N)` / `取消(C)` / `帮助(H)`；提示行原文「如以上列表中没有尺寸适合的标签格式，请点击"自定义"，自行设置标签的尺寸。」
  → **「自定义」在原版是一个按钮，不在标签名称下拉里**。
- 真机 `61-dlg-custom-label.png`（**标签格式设置**，页签 打印机/页面/标签/其它）的 `标签` 页只有：`标签`组（宽度/高度/形状）、`间距`组（列距/行距）、
  `孔洞`组（孔洞下拉 + 毫米，禁用）、`行列`组（列数/行数）——**没有「圆角半径」输入框**。
- **圆角弧度量化**（对 `60-dlg-choose-label.png` 做 1:1 像素分析）：格宽 184px ↔ 100mm（1.84 px/mm），圆角矩形的角弧只有 **2–3px ≈ 1.1–1.6mm**
  （逐像素图：上边自 x=283 起、左边在 x=281、y=88 起稳定）。复刻版 `PaperFields.tsx` / `paper.ts` / `LabelEditor.tsx` 用 `min(w,h)*0.12`
  （100×70 → **8.4mm**），比原版大 5–6 倍 —— 这就是用户看到的「圆角不一样」。
- 复刻版现状（读代码所得）：`NewLabelDialog.tsx` 在 `标签名称(L)` 下拉末尾塞了 `自定义` 项并**内联**展开宽/高输入框；底部「自定义」按钮也只是
  `setCustom(true)` 展开同一对输入框；`PaperFields.tsx` 在形状=圆角矩形时多出「圆角半径（mm）」输入与「标签纸颜色」。

**本轮要求（先取证、再改，不许猜）**：

1. 真机点一次 `自定义(N)`（用 `tools/parity/LabelShopCtl.ps1` 的 `btn:`/`postclick:` 手法），截图 + `Read-LabelShopDialogTree.ps1` 控件树，
   确认它打开的到底是什么（是否就是「标签格式设置」四页签对话框、初始值、确定后回到哪里），结论写进 `parity/reference/labelshop/PROBE-round1xx.md`。
2. 枚举真机 `标签名称(L)` 下拉的**全部条目**（`Probe-LabelShopCombos.ps1`），确认 275 条格式之外**有没有**「自定义」项。
3. 量真机**编辑器画布**上的圆角半径：打开一个 `Corner=1` 的圆角格式（如 `[608053]`），把视图放大到 ≥200% 后截图，
   用状态栏缩放（或画布毫米标尺）做标尺 + 逐像素 ASCII 图数出圆角像素 → 换算成 mm；
   同时确认**预览 / 编辑器画布 / 打印输出**三处的圆角是否一致。
   **验收方已量好的标尺（round-104，可直接引用，别再重复量预览）**：见
   `parity/reference/labelshop/PROBE-verifier-corner-radius.md` —— 真机预览里 100×70 与 100×150 两种圆角标签的角弧
   **都只有约 2px（≈0.9–1.1mm，且与标签尺寸无关）**，而复刻版是按短边 12%（8.4mm / 12mm）画，**大约 7 倍**。
   量测工具：`node tools/parity/measure-corner-radius.cjs <png> <x0> <y0> <x1> <y1> [标签宽mm] [角区x 角区y 宽 高]`
   （打印边线清单 + 1:1 ASCII 角区图；**不要**用自动拟合，真机预览有纸张外框与格子两套近邻描边会误判）。
4. 按取证结论改实现：① `选择标签格式` 的标签名称下拉不再放「自定义」，`自定义(N)` 走真机行为；
   ② 圆角半径采用真机规则（固定值就写常量，随尺寸变化就写公式），并且**只有一个半径来源**，
   让 `NewLabelDialog` 预览 / `PaperFields` 预览 / `LabelEditor` 画布 / 打印场景与位图输出四处一致；
   ③ 真机没有的字段（如「圆角半径」输入框）删掉或改只读展示，依据写进 `parity/diffs.md`；
   ④ 直角矩形=0、圆形=直径、带孔=直径+孔 三种形状一并核对（用户就是要「各种纸张」都对）。

   **验收方静态排查：半径规则的全部消费点（别漏，共 3 处默认值 + 7 处绘制/裁剪）**

   | 位置 | 作用 | 现状 |
   | --- | --- | --- |
   | `app/src/shared/domain/paper.ts:26` | `paperPath()` 里 `cornerRadiusMm ?? min(w,h)*0.12` | **12% 默认值的源头**，应改成真机规则 |
   | `app/src/renderer/src/editor/LabelEditor.tsx:957` | 编辑器裁剪路径（再 `*10*zoom` 换算像素） | 又写了一遍 `*0.12` 默认值 → 应改为调用 paper.ts 的统一函数 |
   | `app/src/renderer/src/dialogs/PaperFields.tsx:24` | 「圆角半径（mm）」输入的显示默认值 | 又写了一遍 `*0.12`；该字段本身真机没有，见 ③ |
   | `NewLabelDialog.tsx:273 / 288 / 303` | 选择标签格式预览（平张 / 卷筒切段 / 多格） | 经 `paperFor()` 只设 `shape='roundRect'`，**不带半径** → 走 12% 默认 |
   | `PaperFields.tsx:39` | 标签格式设置「标签」页的 SVG 预览 | 同上 |
   | `LabelEditor.tsx:960 / 961` | 编辑器纸张大纲 + 裁剪路径 | 同上 |
   | `app/src/renderer/src/print/renderLabel.ts:77` | **打印/位图输出的裁剪**（`g.clip(Path2D(paperPath(...)))`） | 同上 → 打印出来的圆角也会跟着 12% |
   | `app/src/shared/print/scene.ts:231 / 258 / 298` | 把 `doc.layout.cornerRadiusMm` 塞进 `paperGeometry`（打印场景） | 透传点：改规则时确认这里传的仍是最终值 |
   | `app/src/shared/domain/document.ts:612` | 文档归一化时 clamp `cornerRadiusMm` | 若该字段被删/改为内部常量，这里要同步 |

   改法建议：在 `paper.ts` 里出一个**唯一**函数（如 `roundRectRadiusMm(width, height)`），上面 3 处默认值全部改成调它；
   7 处绘制/裁剪点保持只走 `paperPath`，不要在调用侧再算半径。改完**必须有断言钉住每一条路径**
   （预览 SVG 的 `A r r` 半径值、编辑器 clipPath、`renderLabel` 位图裁剪的半径），否则"四处一致"只是口头保证。

   **现有测试的连带影响（验收方查过）**：`app/scripts/render-regression.ts:157-166` 的纸张断言
   **显式传了** `cornerRadiusMm: 10`（并断言 `cornerRadiusMm === 10`、`innerDiameterMm === 20`），
   所以**只改默认值规则不会碰它们**；但如果按真机把「圆角半径」字段/模型字段**删掉**，
   这 4 条（rect/roundRect/ellipse/disc 各一条）+ `document.ts:612` 的归一化必须同步改，
   否则门禁会红——两种改法二选一，别做成"字段删了、测试还断言它"。
5. 断言：新增 `app/scripts/ui-v1NN.cjs` 并注册进 `run-regression.ps1` —— 标签名称下拉不含「自定义」项、`自定义(N)` 行为与真机一致、
   圆角矩形的弧度等于取证值、直角/圆形/带孔 三种形状的弧度分别为 0 / 直径 / 直径+孔。
6. 更新 `parity/diffs.md`（**新登记 DIFF-66**）与 matrix 里相关条目（`A-*` 新建标签 / `C-*` 标签格式设置）的证据。

**P0 追加（验收方读 codex round-105 的取证后整理，同一对话框的其余待办；证据 `probe-round105-custom-label-tree.txt`、`probe-round105-custom-label-values.txt`、`round105-custom-label.png`）**

- **已确认**：`选择标签格式 → 自定义(N)` 打开的就是 **`标签格式设置`**（＝工具栏那个，同页签），
  且 `形状=圆角矩形` 时**仍然没有「圆角半径」字段** —— P0 的 ①③ 到此有真机证据支撑。
- 真机 `标签` 页字段原文：`宽度(W)` / `高度(H)`（＋毫米）、`列距(P)` / `行距(L)`（＋毫米）、
  `列数(C)` / `行数(R)`（Edit+Spin）、`形状` 组 combo（**3 项，`圆角矩形` 是第 2 项**）、
  `孔洞` 组 combo（**3 项，`无` 是第 1 项**）+ 一个 **禁用** 的尺寸 Edit 与 `毫米`；
  预览行文字 = `100.00 x 70.00 毫米 [4行 2列]`；底部按钮 `确定` / `取消` / **`应用(A)`（禁用）** / `帮助`。
  → 待办：① 枚举 `孔洞` combo 的 3 项文本（复刻版现在只有 `无`/`圆洞` 2 项）；
  ② 核对复刻版有没有 `应用` 按钮及其禁用规则；③ 预览行文字格式对齐。
- 该 dump 里还有一个 **不可见** 的 `多行标签` 按钮 → 待办：取证它的显示条件与作用（复刻版未找到该文案）。
- 真机 `打印机` 页（同一对话框的页签，控件树里可见但隐藏）：`名称(N)` combo、按钮 `标准驱动(S)` / `设置(S)` /
  `高级设置(A)` / `安装(I)`、`输出方式:` combo、按钮 `整页反相打印` / `镜像输出` / `单页任务模式`。
  → 待办：逐项核对复刻版是**缺**还是**在别的对话框里**（复刻版 `反相`/`单页` 全库搜不到，
  `镜像` 只在对象属性里、`打印机属性` 在 `PrintDialog`/`PrinterSettings`）——若分散在别处，要在 `diffs.md` 写清
  「等价替代 or 入口不一致」，不要悄悄当成已有。

**P0 追加 2（验收方把真机 `标签` 页与复刻版 `TemplatePropsDialog.tsx` 逐字对完的差距清单，按真机改）**

| # | 真机（`probe-round105-custom-label-tree.txt` 原文） | 复刻版现状 | 待办 |
| --- | --- | --- | --- |
| 1 | **五个分组框**：`标签` / `间距` / `行列` / `形状` / `孔洞` | 全是平铺 `FormField`，**没有分组框** | 加分组框并按真机分组 |
| 2 | `宽度(&W):` / `高度(&H):` | `标签宽度（mm）` / `标签高度（mm）` | 字段名与加速键对齐真机 |
| 3 | `列距(&P):` / `行距(&L):` | `水平间距（mm）` / `垂直间距（mm）` | 同上 |
| 4 | `列数(&C):` / `行数(&R):`（Edit **+ Spin 微调**） | `列数` / `行数`（纯 number） | 改名 + 补上下微调形态 |
| 5 | `形状` 分组内 combo（3 项，`圆角矩形` 第 2 位） | combo 3 项 ✓（同一个 `PaperFields`） | 只需挪进分组框 |
| 6 | `孔洞` 分组内 combo（**3 项**）+ **禁用** 的尺寸 Edit 与 `毫米` | combo **2 项**（无/圆洞）+ 尺寸框（选中时启用） | **枚举真机 3 项文本**；补第 3 项；尺寸框的启用/禁用规则按真机（`无` 时为禁用态） |
| 7 | 页内预览行 `100.00 x 70.00 毫米 [4行 2列]` | 只有 `PaperFields` 右侧的 SVG 小图 | 补这行文字（格式逐字：两位小数 + ` 毫米 ` + `[N行 M列]`） |
| 8 | 底部按钮 `确定` / `取消` / `应用(A)`（**禁用**）/ `帮助` | 需核对（`TemplatePropsDialog` 里未见到；`应用` 文案只在别处出现） | 按真机补按钮与禁用规则 |
| 9 | `标签纸颜色` **不在** 标签页 | 复刻版把 `标签纸颜色` 放在 `PaperFields`（标签页可见） | 核对该字段真机归属页（帮助 `label_page_page.html` 指向 `页面` 页），按真机挪页 |

> 尚未比对：真机 `页面` / `其它` 两个页签的字段名与分组（codex 的 tree dump 只覆盖了 `标签` 与 `打印机` 两页）——
> 下一步取证时把这两页也 dump 出来再比，别凭空改。

**P0 验收要点（验收方会在轮末逐条查，按此自检）**

1. **半径规则已核**：`paper.ts` 新增 `roundRectRadiusMm()` + 常量 `LABELSHOP_ROUND_RECT_RADIUS_MM = 1`，
   验收方用 `node tools/parity/measure-paper-radius.cjs` 在**在途源码**上实测：100×70 / 100×150 / 60×60 / 40×30 **全部 = 1mm**，
   显式传 2mm 仍得 2mm，直角仍为 0 ✓。
2. **还没改的两处（轮末必查）**：
   - `app/src/renderer/src/editor/LabelEditor.tsx:957` 里还留着 `doc.layout?.cornerRadiusMm ?? Math.min(...)*0.12`
     的**第二份默认值** → 若不同步调 `roundRectRadiusMm()`，编辑器裁剪路径会仍按 8.4mm 裁，与 `paperPath` 的 1mm **不一致**（形状与裁剪对不上）；
   - `app/src/renderer/src/dialogs/PaperFields.tsx:24` 的「圆角半径（mm）」输入框（含第三份 `*0.12` 默认值）→ 真机没有该字段，应移除
     （移除后该处默认值自然消失；若保留字段则必须改默认值并说明理由）。
3. **新增的 `app/src/renderer/src/dialogs/CustomLabelFormatDialog.tsx` 要自检**：不要和 `TemplatePropsDialog` 出现两份实现漂移——
   两者要么共用同一组字段组件（`PaperFields` 等），要么明确分工（新建入口 vs 模板属性入口），并在 `diffs.md`/矩阵里写清入口关系。
4. 断言要求不变：预览 SVG 的 `A r r`、编辑器 `clipPath`、`renderLabel` 位图裁剪三条路径都要有**数值断言**钉住半径=1mm。
5. **接线必查（验收方 10:26 实测：新对话框目前还没有被任何地方引用）**：`NewLabelDialog` 的 `自定义(N)` 必须真的打开
   `CustomLabelFormatDialog`，并且**不再**在 `标签名称(L)` 下拉里放 `__custom__` 项、不再内联展开宽高——
   否则用户看到的行为没变（P0 的第一条诉求就没完成）。同时 `确定` 后要按真机**直接创建标签并回编辑器**（不回列表）。
6. **防漂移必查**：新对话框 import 的是 `paperPath` 而不是 `PaperFields`，即标签页字段被**重写了一份**——
   要么改成复用 `PaperFields`（推荐，孔洞/形状规则只留一处），要么在 `diffs.md`/矩阵里写清两个入口的分工与共享边界；
   不允许出现"两个入口各有一套孔洞/形状规则，改一处漏一处"。
7. **`ui-v129.cjs` 验收方审阅结论（断言强度合格，两处要清/补）**：核心断言已是**值级**（下拉恰好 42 项、
   预览 `A 1 1`、页签文本数组、初始值 100/70/2/4、`clipPath` 的 `/^M 10 0 H 990/`=10px=1mm、无圆角半径字段）✓；
   - 要清：第 88 行 `console.log('DEBUG clipPath', …)` 属调试残留，提交前删掉；
   - 要补：**「P0 追加 2」那 9 条还没有断言**——分组框（标签/间距/行列/形状/孔洞）、孔洞下拉 **3 项**、
     `应用` 按钮**禁用**、预览行 `100.00 x 70.00 毫米 [4行 2列]`；实现完必须补断言，不能"做了不钉"。
   - 健壮性提示：第 89 行的断言把文档宽度写死成 100mm，日后推广到其他尺寸需参数化。

**P0 验收方终验（round-105 提交 `68a2a93` 之后，2026-09-21 10:58）**

- ✅ **核心诉求已修并独立复核**：`paper.ts` 提交后复测半径 **各尺寸=1mm**（100×70 / 100×150 / 60×60 / 40×30）、显式 2mm 仍保留、直角=0；
  编辑器 `LabelEditor.tsx` 与 `PaperFields.tsx` 的两处旧默认值已清除；`ui-v129.cjs` + `render-regression.ts`（+6 条，
  含 `renderLabel clips the default roundRect corner with the shared radius`）把三条绘制路径都钉成了**数值断言**。
- ⏳ **「追加 2」的 9 条仍未做**（我在提交后的代码里逐项 grep 过）：
  `CustomLabelFormatDialog.tsx` 里 `孔洞` 下拉仍是 **2 项**（真机 3 项）；`应用` 按钮 **0 处**；`多行标签` **0 处**；
  `间距/行列/形状/孔洞` 只是普通 label（**不是真机的分组框**）；`打印机` 页还是占位（只有只读"名称(N)"+`输出方式`，
  真机是 `标准驱动(S)/设置(S)/高级设置(A)/安装(I)/整页反相打印/镜像输出/单页任务模式`）。
- ⚠️ **遗留**：`ui-v129.cjs` 里还有 **2 处 `DEBUG`** 输出未清（会污染门禁日志）。
- ⚠️ **记账口径**：`parity/diffs.md` 的 `DIFF-66` 已标 `✅ 已修` —— 就 P0 **核心**（自定义入口 + 圆角规则）而言成立，
  但「追加 2」的 9 条**不在其内**；请在 DIFF-66 里写明"核心已修，同对话框其余项另列"，或另开 DIFF 条目跟踪，
  不要把 9 条未做项一起算进"已修"。

**P0 验收方实测（2026-09-21 11:30，用 `tools/parity/Verify-LabelFormatDialog.cjs` 对**当前构建**跑，8/12 PASS）**

- 已通过：到达选择标签格式 ✓、**下拉不含「自定义」** ✓、能点「自定义(N)」✓、**打开的是四页签对话框** ✓、
  含 `标签/间距/行列/形状/孔洞` 文案 ✓、含真机字段名（宽度/高度/列距/行距/列数/行数）✓、形状下拉 3 项 ✓、
  **页内无「圆角半径」** ✓ —— 也就是 **P0 核心两条都过机器检查**。
- **确认为真缺口（必须补）**：
  1. `孔洞` 下拉 **2 项**（实测 `["无","圆洞"]`）→ 真机 3 项（第 3 项文本待真机枚举，见
     `PROBE-verifier-labelformat-strings.md` 第三节）；
  2. **没有「应用」按钮**（真机有且禁用）；
  3. **没有预览行** `100.00 x 70.00 毫米 [4行 2列]`。
- 待细化（可能是我这条检查的范围过宽）：`④b 不再用「标签宽度/水平间距/垂直间距」` 报 FAIL ——
  我扫的是"所有可见对话框的文本"，可能把别的对话框（如 `TemplatePropsDialog` 的字段名）算进来了；
  下一轮我会把它限定到 `custom-label-dialog` 子树内再判定，避免误报。

**P0 验收方终版实测（2026-09-21 11:45，工装已修正 → 10/13 PASS，**只剩 3 条真缺口**）**

修正了两个工装自身的问题（都已写进脚本注释，避免以后再踩）：
① 断言范围从"所有可见 `*-dialog` 的文本"收窄到 **`custom-label-dialog` 子树**（之前把 `TemplatePropsDialog` 的自造名算进来了）；
② `④b` 原用 `/标签宽度/` 判定会**误报**——`textContent` 会把分组框 legend「标签」与字段「宽度(W):」连成「标签宽度(W):」；
改成查带（mm）后缀的自造名（`标签宽度（mm）` 等，那才是 `TemplatePropsDialog` 用的形态）。

| 检查 | 结果 |
| --- | --- |
| 到达选择标签格式 / 下拉不含「自定义」/ 能点「自定义(N)」/ 出现 `custom-label-dialog` / 含四页签 | **PASS** |
| 五个分组框（标签/间距/行列/形状/孔洞） / 字段名 = 宽度(W)·高度(H)·列距(P)·行距(L)·列数(C)·行数(R) | **PASS** |
| ④b 不用自造名 / 形状下拉 3 项 / 页内无「圆角半径」 | **PASS** |
| **⑤ 孔洞下拉 3 项** | **FAIL**（实测 2 项：`无`、`圆洞`） |
| **⑥ 有「应用」按钮且禁用** | **FAIL**（不存在） |
| **⑦ 预览行 `100.00 x 70.00 毫米 [4行 2列]`** | **FAIL**（不存在） |

→ 下一轮就把这 **3 条**补掉（同对话框的其余 6 条：真机字段名/分组框已到位；打印机页 6 个控件与 `多行标签` 另算），
补完我用同一工装复跑，应当 13/13。

**P0 追加 3（round-106 真机枚举带来的两条新证据 + 工装升级，2026-09-21 11:45）**

codex 本轮新增真机枚举 `probe-round106-custom-label-combos.txt`（我已核对原文），**下拉逐项文本终于拿到**：

| 下拉 | 真机 3 项原文 | 复刻版现状 | 结论 |
| --- | --- | --- | --- |
| `形状` | **`方角矩形`** / `圆角矩形` / `圆形` | `直角矩形` / `圆角矩形` / `圆形` | ⚠️ **命名不一致：真机是「方角矩形」，复刻版写成「直角矩形」**（新发现，之前只数了项数没比文本） |
| `孔洞` | **`无` / `圆洞` / `矩形`** | `无` / `圆洞` | 缺第 3 项 **`矩形`**（文本已知，不必再猜） |

我已把验收工装升级为**逐项文本**断言（原来只断言"3 项"），复跑后现在报 **5 条缺口**：
`⑤ 孔洞 3 项`、`⑤b 形状文本`、`⑤b 孔洞文本`、`⑥ 应用按钮`、`⑦ 预览行`。
→ 这 5 条一起补（外加打印机页 6 控件与 `多行标签`），补完复跑应到 15/15。**这也说明"只断数量不断文本"会漏掉真机改名。**

**P0 途中复核（round-106 在途源码，11:39 验收方实测）**：`孔洞` 下拉已补齐 3 项（`无`/`圆洞`/`矩形`）✓；
`形状` 已改名 **`方角矩形`**（`CustomLabelFormatDialog` + `PaperFields`）✓；新增 `ui-v130.cjs` 并已注册 ✓。

- ⚠️ **遗留 1**：`OptionsDialog.tsx:266`（系统选项里的形状下拉）仍写 `直角矩形`。
  → **先取证真机「系统选项」里那个下拉的原文**再决定改不改；不要凭「标签格式设置」一处的证据去改另一个对话框。
- ⚠️ **遗留 2**：`NewLabelDialog.tsx:46`、`PaperFields.tsx:3` 的注释仍写「帮助 label_page_label.html 的形状只有直角矩形…」——
  这其实揭示了一条**帮助与真机 UI 用词不一致**：帮助写 `直角矩形`，真机 UI 是 `方角矩形`。
  → 在 `parity/diffs.md` 记一条「帮助 vs 真机用词差异（以真机 UI 为准）」，注释同步改成注明这一点。

**P0 追加 5（验收方更正自己上一条口径：`应用` 按钮在真机是**隐藏**控件，不能做成可见！2026-09-21 11:55）**

- 证据 1：`probe-round105-custom-label-tree.txt` 里那一行是 **`[ ] class=Button DISABLED text='应用(&A)'`** —— 行首 `[ ]`
  表示**不可见**（我先前只看了文本，把它当成"有且禁用"，这是我的误读）。
- 证据 2：真机截图 `r107-hole-rect-20.png`（claude round-107 抓的「标签格式设置」，孔洞=矩形/尺寸 20）底部**只有**
  `确定 / 取消 / 帮助` 三个按钮，**没有 `应用`**；同一张图也确认了：孔洞尺寸框在矩形下**点亮**、**只有一个尺寸框**、
  预览卡片里**不画孔**、预览行是 `100.00 x 70.00 毫米 [4行 2列]`、列数/行数带微调箭头、分组框与字段名与 dump 一致。
- 要求：
  1. 复刻版**不要显示 `应用` 按钮**（与真机一致）；`ui-v130.cjs` 里那条「应用按钮存在且禁用」的断言要改成真机口径
     （断言**不存在**，或断言它 `hidden`）。**不允许**为了保住断言而留一个真机没有的可见按钮。
  2. 若怀疑 `应用` 在某些条件下会出现（例如改了字段之后），**先取证**再决定；没证据就按"不显示"处理。
  3. 同理注意 `多行标签`（真机也是 `[ ]` 隐藏）——不要实现成可见按钮。
- 顺带确认（这三条**已对上**，无需再改）：孔洞=矩形时尺寸框**可用**、单值＝边长；预览不画孔；预览行文本格式与真机逐字一致。

**P0 追加 7（验收方对 round-107 门禁失败的独立诊断：这是**真阳性**，不是 flaky，也不是测试过时）**

- 现象：round-107 全量门禁 **`test:ui` 失败，`FAILED SCRIPTS: ui-v90.cjs`**；其余门禁全 PASS（含 round-107 新增的 `ui-v130` **14/14 PASS**）。
- 验收方独立复现：`$env:MAXLABEL_UI_SCRIPT='ui-v90.cjs'; npm run test:ui` → **13/14，确定性失败**：
  `FAIL A-42 模板属性包含四页签和关键字段`。
- 根因（代码级）：round-106/107 只把**「自定义」对话框**（`CustomLabelFormatDialog`）与共享的 `PaperFields`
  的字段名改成真机原文（`宽度(W):/高度(H):/列距(P):/行距(L):/列数(C):/行数(R):` + 五个分组框），
  但**工具栏那个「标签格式设置」对话框**（`TemplatePropsDialog.tsx`）**没同步**——它仍写着 `标签宽度（mm）`（`:259`）、
  `水平间距（mm）`（`:267`）、`垂直间距（mm）`（`:270`）。`ui-v90` 的 A-42 现在按真机口径断言，于是如实报错。
- 结论：**产品侧要改**（把 `TemplatePropsDialog` 的标签页字段名与分组框按真机对齐），而不是把断言改回去；
  这正是我在 P0 验收要点第 6 条点过的"两份实现漂移"风险——现在被测试抓住了，属于好消息。
- 要求：① `TemplatePropsDialog.tsx` 的标签页与 `CustomLabelFormatDialog` **共用同一套字段渲染**（推荐抽出共用组件，或至少共用字段名常量），
  确保以后只改一处；② 分组框 `标签/间距/行列/形状/孔洞` 同样补齐；③ 改完 `ui-v90` A-42 应通过，且**强度不降**；
  ④ 顺带清掉 `OptionsDialog.tsx:266` 的 `直角矩形`（**先取证**真机「系统选项」那处下拉的原文再决定）。

  **round-108 在途复核（验收方 12:40）**：`TemplatePropsDialog` 的标签页已经改对——分组框 `标签/间距/行列` 出现在
  `:275/:286/:297`，字段名改成真机原文 `宽度(W):`(:277) / `列距(P):`(:288) / `列数(C):`(:299)，自造名（标签宽度（mm）等）**已清零** ✓。
  但**"共用同一套字段渲染"只做了一半**：没有抽出共用组件/字段常量，两个对话框仍是**两份各自渲染**的实现
  → 建议下一步抽出共用字段组件（或至少共用字段名常量数组），否则同样的漂移以后还会发生；这条**不阻断门禁**，记为结构债。

  **结构债已还清（round-109 提交 `46fae82`，验收方 13:33 逐点核对）**：新增共享模块 `dialogs/paperHoleFields.ts`
  （`PAPER_HOLE_OPTIONS` / `PAPER_SHAPE_OPTIONS` / `maxHoleSizeMm` / `withHoleSelection` / `withHoleSize` / `holeSelectionOf`），
  `CustomLabelFormatDialog` 与 `PaperFields` 都改为从它取选项与规则 ✓；同时把孔形贯通到全链：
  `paper.ts`（几何）→ `shared/print/scene.ts` 三处（打印场景）→ `shared/domain/document.ts`（**持久化/归一化 `innerShape`**，
  注释明确写了"旧实现只留 innerDiameterMm 把 innerShape 丢掉"）→ `LabelEditor`（编辑器裁剪）→ `TemplatePropsDialog`（属性框）
  → `NewLabelDialog`（重开对话框时按 `innerShape` 回填，避免矩形被打回圆洞）✓。这条结构债可结。

**P0 追加 6（验收方读 round-107b 取证后新提的取证点：真机到底在哪些场景"画孔"？）**

`parity/reference/labelshop/PROBE-round107b-hole-rect.md` 已确证：真机在**对话框预览**和**编辑器画布**（点确定后、261% 缩放）
**都看不到孔**（两张截图逐像素一致；试过 `WM_SETTEXT`+`EN_CHANGE` 注入也无效），并据此把"矩形孔的精确画法"登记为**待取证**——
这个处置是对的。但由此浮出一个**新取证点**：真机是否**任何场景都不画孔**？

- 帮助 `label_page_label.html`：「标签的形状/孔洞**只在编辑标签时显示，并不会实际输出**」——即"输出不画"是明文的，
  但"编辑时显示"与实测的"编辑器画布也没孔"**互相矛盾**。
- 复刻版现状：`paperPath()` 的 `cutout` 在**对话框预览、编辑器裁剪、打印裁剪**三处都会画孔。
- 要求取证（不猜）：① 换一个**光盘类/圆洞**标签格式（`Corner=2`，如标签名含 `117mm/40mm` 的那两条）看真机是否画中心孔；
  ② 在真机上把 `孔洞` 从 `无` 切到 `圆洞`、尺寸给 20，看预览/编辑器是否出现孔；③ 打印到 PDF 看输出里有没有孔。
  结论落 `diffs.md`：若"从不画"→ 复刻版把孔只保留在**几何/命中/裁剪**里、不描出来（或在台账写明差异）；
  若"某些类型才画"→ 按类型分别对齐。

**P0 追加 6 的取证进展与下一步（验收方 round-43 上真机做过一轮，见 `PROBE-verifier-round43.md`）**

> ⚠️ **重大更正（round-44 验收方已取证，别再按旧结论干活）**：**真机会画孔**——
> 「标签格式设置」**对话框预览**里 8 个卡片中心各有一个小圆，**编辑器画布**中心也有一个 20mm 的圆
> （配置 `形状=圆角矩形 / 孔洞=圆洞 / 尺寸=20`，值由 `Read-LabelShopDialogValues` 回读确认）。
> 证据：`verifier-r44-hole-circle-20b.png`（预览画孔）、`verifier-r44-editor-hole20.png`（画布画孔）、
> 说明见 `parity/reference/labelshop/PROBE-verifier-round44.md`。
> round-107b 得出"真机不画孔"是**因为当时孔洞尺寸一直是 0.00**（不是真机不画）。
> → 所以**复刻版在预览与编辑器里画孔是对的，不要为了"对齐"把画孔去掉**；
> 仍待取证的只剩一条：**打印输出里有没有孔**（帮助说"不会实际输出"；做法：打印到 PDF 再渲染成位图看中心）。

已拿到：编辑态**文件菜单全项**（`verifier-r43-file-menu.png`，A-41 的直接证据）；`孔洞=圆洞` 时尺寸框**点亮**（黑字可编辑）；
**预览是随字段实时重绘的**——验收方误把 `20` 打进「高度(H)」后，预览立刻变成 `100.00 x 20.00 毫米 [4行 2列]`，
所以"改孔洞尺寸后预览仍无孔"**不能**用"预览不刷新"解释；点确定后的编辑器画布也**看不到孔**。

未拿到（下次照这个来，别重复踩）：
1. **别盲发 Tab 注入尺寸**：本轮 `keydlg:{TAB}` 落到了「高度」而不是「孔洞尺寸」。正确做法：先
   `Read-LabelShopDialogTree.ps1 -TitleLike '*标签格式设置*'` 取该 Edit 的**对话框内坐标**（先前实测在
   `xy=(1335,1087)`，对话框左上 `(762,871)` ⇒ 相对 `(573,216)`），用 `clickdlg:573,216` 点中后再 `keydlg:20`，
   并且**注入后立刻用 `Read-LabelShopDialogValues.ps1` 回读确认**再继续。
2. **从编辑态进「标签格式设置」不要猜项序号**：本轮 `{DOWN 8}{ENTER}` 没打开。先 `keys:%f` + `shotpopup:`
   抓菜单图数序号（菜单共 14 项，含 2 个禁用项：`分享(I)`、`导出打印机指令文件(E)`），或用 `menupick:` 步骤。
3. 冷启动进「选择标签格式」的**成功配方**（已复现两次）：
   `LabelShopCtl.ps1 -Action run -KeepOpen -StepsFile <含 click:900,60 → keys:^{n} → btn:新建标签模板 → btn:下一步>`
   —— 注意**不要先 `close`**（会把向导关掉），要么冷启动（向导自动出现），要么先 `click:900,60` 再 `^n`。
4. 三路问题的验收口径不变：① 光盘类（`Corner=2`）格式是否画中心孔；② `圆洞` + 尺寸 20 后预览/编辑器是否出现孔；
   ③ 打印到 PDF 的输出里有没有孔（PDF 需要渲染成图再判，别只看文本流）。

**P0 追加 4（验收方 11:45 在途复核发现的"半实现"，必须补齐，否则是假功能）**

`孔洞` 下拉现在有了第 3 项 `矩形`，但**几何侧没有实现**——逐行核对：

| 位置 | 现状 | 问题 |
| --- | --- | --- |
| `CustomLabelFormatDialog.tsx:73` | `...(draft.hole === 'circle' && holeSize > 0 ? { innerDiameterMm: holeSize } : {})` | **只有 `circle` 会写几何**；选 `矩形` 时**什么都不写** → 预览/新建出来的标签**没有孔** |
| `CustomLabelFormatDialog.tsx:129` | 尺寸框与「毫米」仅在 `draft.hole === 'circle'` 时点亮 | 选 `矩形` 时尺寸框始终灰 → 无法设尺寸 |
| `shared/domain/paper.ts:33` | `const cutout = hole > 0 ? ' ' + ellipse(...) : ''` | **只支持圆孔**（椭圆切孔），没有矩形切孔的实现；`PaperShape` 也只有 `rect/roundRect/ellipse/disc` |

要求（按"单一来源"原则，别在调用侧各写一份）：
1. 先**真机取证**：在真机「标签格式设置」里把 `孔洞` 切到 `矩形`，看**尺寸框是否点亮、是一个还是两个数值**（树里现在只有一个禁用 Edit + 毫米），
   以及预览里孔变成什么样（方形/圆角方？尺寸含义是边长还是对角？）——**不要猜**。
2. 按取证给 `PaperGeometry` 加"孔形"（如 `innerShape?: 'circle' | 'rectangle'`），在 `paperPath` 里实现矩形切孔；
   `PaperFields` 预览 / `CustomLabelFormatDialog` 预览 / `LabelEditor` 裁剪 / `renderLabel` 打印裁剪**都走同一个 `paperPath`**，
   不要各自实现。
3. 对话框里把 `rectangle` 正确映射成几何（含尺寸框的启用规则按真机）。
4. 补断言：选 `矩形` 后 preview 的 `d` 里出现**矩形切孔路径**（不是圆），以及打印裁剪一致；`ui-v130` 或新脚本里钉住。
5. 在 `diffs.md`/矩阵里写清 `孔洞=矩形` 的实现范围（编辑器预览是否也画孔、打印是否输出——帮助说形状与孔洞"只在编辑标签时显示，并不会实际输出"，请按帮助口径核实后写清）。

### 优先级 1 · DIFF-63：真机序列号数据源面板的「重置初始值: / 立即重置」

**验收方已独立复核 round-104 的取证（2026-09-21 09:2x）**：`PROBE-round104-DIFF63.md` 里 12 份证据文件全部存在且已入库；
`probe-63-serial-advanced-values.txt`（类型 4 项 / 序列 `0123456789` / 步长 1 / 重复 1 / 变化基准 2 项 / 初始值来源 2 项）
与 `probe-63-after-print-values.txt`（打印后显示数据 `12345679`、数据源=序列号 1/7）与文中结论一致，采信。

**版本受限提示（实现时逐字照抄，不要用转述）**——`probe-63-temp-basis-down.png` 标题为「功能提示」，正文与按钮原文：

```
这是一些只有专业版和企业版才可以使用的功能，目前的版本中无法使用。

目前您有两种方式可以使用这些功能：
    1、购买 签赋LabelShop 的专业版授权；
    2、购买云马科技的金牌会员服务。

您还可以进入专业版演示模式，体验和试用此项功能。程序演示模式下打印输出时，将在标签上随机添加一条提示信息！
```
按钮：`申请试用金牌服务` / `进入演示模式` / `了解更多信息` / `取消`（默认按钮＝`取消`）。
进入演示模式后再弹 `probe-63-18-label-mode-demo.png` 那条：标题 `LabelShop`，正文
「程序运行在演示模式下，您可以体验专业版功能。演示模式下打印输出时会在标签上随机添加一条提示信息！您可以关闭程序并重新启动，退出演示模式。」，按钮只有一个 `确定`。
（复刻版要断言这两处文案与按钮，必须与上图逐字一致；**不要**把「仅专业版和企业版可用」这类转述写进断言。）

**循环工装注意（验收方踩过）**：驱动器的卡死判定只看 `app\src`、`app\scripts`、`parity` 三个目录的**文件改动时间**。
codex 卡住时如果验收方往 `parity\` 下写文件（例如补证据），会把卡死计时器**重置**，让一个已经挂住的轮次多活 20 分钟。
所以：轮次在跑期间，验收方的笔记/工装只写到 `tools\loop\` 或 `tools\parity\`（不在 watchDirs 里），
往 `parity\` 的证据文件等轮次结束了再补。

- 已有证据：`parity/reference/labelshop/PROBE-round60.md`。`LabelShop.exe` 字符串资源（UTF-16LE）里确认了这两处字样，同面板的标题集也已拿全；但页面切源不重排（`tools/parity/Probe-LabelShopSerialPage.ps1` 已试过 `CB_SETCURSEL` + `CBN_SELCHANGE/CBN_SELENDOK` + 回车，序列号子面板连隐藏控件都不存在），复刻版**未实现**、也**没有按猜测实现**。
- 要求：① 用**真实鼠标路径**把真机数据源下拉切到「序列号」再 dump 数据源页：可用 `tools/parity/LabelShopCtl.ps1` 的 `postclick:` / `postdrag:` 步骤（本机 `SetCursorPos`+`mouse_event` 无效，务必用 PostMessage 手法），必要时先 `keys:` 聚焦下拉再用 `{DOWN}{ENTER}`；② 点一次「立即重置」，看它对「显示数据」的即时影响；③ 把子面板的完整控件/默认值写进 `parity/reference/labelshop/PROBE-round6x.md`；④ 结论落 `parity/diffs.md` DIFF-63 与 `parity/需求清单-待验证队列.md` 第 201 条；⑤ 语义确认后按真机实现并补 `app/scripts/ui-v1NN.cjs` 断言（含 `run-regression.ps1` 注册）；仍取不到就写清失败现象，保持「待取证」。
- 参考：真机序列号面板的资源标题集＝ 序列号 / 类型(&T): / 序列(&Q): / 步长(&S): / 增量(&A) / 减量(&D) / 重复(&E): / 序列号设置 / 重置 / 提示(&P): / 归位 / 初始值来源(&R): / 数据库连接(&D): / 重置初始值: / 立即重置。

### 优先级 1.5 · 验收方 round-104 实拍发现的两处疑点（**先独立复核，成立就登记 DIFF-64/65 并修**）

来源：`parity/reference/labelshop/probe-63-06-serial-page.png`（round-104 codex 抓的真机「文字属性 → 数据源」页实拍）。

1. **属性页签的顺序与命名不一致 —— 已登记 `DIFF-64`，验收方 round-5 已把 4 类对象的页签条拍全，按表改即可**
   （完整证据与截图清单：`parity/reference/labelshop/PROBE-verifier-object-tabs.md`）：

   | 对象 | 真机页签（左→右） | 复刻版现状 |
   | --- | --- | --- |
   | 文字 | 数据源 / 字体 / 文本 / 常规 | 通用 / 文字 / 字体 / 数据 |
   | 条码 | 数据源 / 条码 / 字体 / 常规（**4 页**） | 通用 / 条码 / 字体 / 数据 / `<码制>`（**5 页**） |
   | 矩形·椭圆·直线 | 图形 / 常规 | 通用 / 方框和圆形（直线为 直线和斜线） |
   | 表格 | 表格 / 常规 | 通用 / 表格 |

   要求：① 按表改 `propertyTabsFor` 的**显示名与顺序**（key 可保留英文）；
   ② **把条码的码制专页合并回「条码」页**——round-6 已把真机「条码」页拍全（`verifier-20c-barcode-page.png`）：
   页首是 `条码符号类型(码制)(B)` 下拉，然后 `尺寸`（`X 尺寸` mil / `码高` 毫米）、`条码特殊选项` 分组
   （**内容随码制变化**，Code 128 下是 `GS1/EAN 128` + `字符集`）、`供人识读字符`（`位置` / `垂直偏移` / `对齐方式` / `字符模板`）、
   页尾 `颜色`；页签数回到 **4**；
   ③ 补**「页签文字与顺序逐项相等」**的 CDP 断言（现在只有存在性断言）；
   ④ 同步 matrix 里涉及页签的 B 章节证据；⑤ 删掉 `propertyTabs.ts` 顶部那句已被实拍证伪的「通用页永远置于首位」注释。
   仍未取证的一类：**图片**属性页（真机未拍；`工具 → 图片(P)` 是菜单第 7 项）。

   **改法（验收方给出目标表与影响面，照此改）**：

   | 对象 | 目标 keys（testid 前缀不变） | 目标 labels（真机原文） |
   | --- | --- | --- |
   | 文字 | `datasource, font, text, general` | 数据源 / 字体 / 文本 / 常规 |
   | 条码 | `datasource, barcode, font, general`（**不再有 `barcodeSpecial` 页签**；码制下拉 + 专属字段进 `barcode` 页的「条码特殊选项」分组） | 数据源 / 条码 / 字体 / 常规 |
   | 矩形·椭圆·直线 | `shape, general` | 图形 / 常规 |
   | 表格 | `table, general` | 表格 / 常规 |
   | 图片 | `image, general` | 图片 / 常规 |
   | RFID | 真机无创建入口（`DIFF-65`）→ 暂不动，按取证再定 | — |

   **影响面（验收方已 grep，必须一起迁移，不许删断言降强度）**：
   - `app/scripts/ui-v109.cjs:263-266` 直接断言**页签文字**：`tabLabels[0] === '通用' && includes('条码') && includes('数据')`
     → 改成真机口径；不改必红。
   - 走 `object-props-tab-barcodeSpecial` 的脚本：`ui-v106.cjs`（**17 处**）、`ui-v125.cjs`、`ui-v126.cjs`、`ui-v127.cjs`、`ui-v56.cjs`；
     单测 `app/scripts/barcode-spec.test.ts`（约 **20 处**）。合并页签后这些导航/断言都要迁移到「条码」页里的分组。
   - 其余脚本点的是 `object-props-tab-<key>`，**key 不变即不受影响**（`ui-v102.cjs:128` 读的是 key 不是标签 ✓；`ui-v71.cjs:132` 是 RFID 页内控件 ✓）。
   - 迁移建议：把合并后的「条码特殊选项」分组容器保留 `data-testid="barcodeSpecial"`（或 `barcode-special-group`）当锚点，
     这样上面那些脚本只需把「点页签」换成「进条码页后滚到该分组」，改动面最小；**但页签数量必须回到 4**，
     并新增断言证明「码制专属字段现在出现在条码页内、且页签数为 4」。矩阵里码制专属页的条目证据同步更新。
2. **「源自其它变量」复选框缺失**：真机「变量共享名称(N):」下拉右侧有一个 **源自其它变量** 复选框
   （勾选后才用共享变量名取别处的数据？语义待取证）；复刻版只有输入框 + datalist，没有这个复选框。
   要求：先真机取证它的勾选/取消对「显示数据/示例」的影响，再按真机实现（或按取证结论登记边界），并补断言。
3. **RFID 工具入口待核对（已登记 `DIFF-65`）**：真机「工具」菜单实测为
   `选取 / 条码 / 文字 / 线条 / 斜线 / 矩形 / 图片 / 数据 / 表格` + 放大缩小适应三项，**没有 RFID**；
   round-6 又把菜单第 8 项 `数据(D)` 建出的对象打开属性，根窗口标题是**「条码属性」**（数据型条码对象，不是 RFID），
   候选入口已排除。复刻版工具栏/菜单有 RFID 入口 → 要求按取证对齐（保留必须有原版入口证据；原版确无则移除或标注等价替代）。

### 优先级 2 · 对象编辑三处真机待确认（round-114 遗留）

见 `parity/reference/labelshop/PROBE-round114.md`：① CTRL+拖动是**复制**还是移动；② 原版拖动有没有**对齐参考线**吸附；③ 点空心矩形**内部**算不算选中。取证手法同优先级 1；结论进 `parity/diffs.md` + 需求清单，行为不一致就对齐实现并补断言。

### 优先级 3 · 两条观察项 + E 区 5 条边界（只取证、写结论，不造功能）

- DIFF-50：真机对象属性「水平/垂直」相对标签边对齐下拉**是灰的**的成因（按对象类型决定可用性只收口了一半）。
- DIFF-60：真机 EAN/UPC 条码页没有「附加条码」「校验字符」、25 码族无独立校验字符，复刻版按原版帮助保留——把「真机实测 vs 帮助原文」的冲突写透，给出保留/删除的理由与证据。
- E 区 5 条：硬件锁激活（E-09）、专业版演示模式（E-10）、三版本分层字段、起始页服务端运营图文（A-271）、内置驱动不支持预览——按 `app/docs/labelshop-compatibility-audit.md` 的单版本策略写「已记录边界 + 理由」，不实现。

### 优先级 4 · 界面清单驱动的逐页并排比对（提升相似度的主线路）

**验收方基线（2026-09-21 09:28 普查）**：工装 `node tools/parity/survey-evidence-coverage.cjs`，报告 `tools/loop/interface-coverage.md`。
矩阵 **605 条**里：有真机证据 **28（4.6%）**、有复刻版证据 **253（41.8%）**、有并排图 **4（0.7%）**、有自动断言 **481（79.5%）**、
**四件套齐 0 条**。注意：真机证据低**主要是没建立链接**（`parity/reference/labelshop` 下已有 **266** 份真机素材、
`parity/review` 有 34 张并排图，但矩阵行基本不引用它们的文件名），所以本项的活是「**按界面建立链接 + 补真缺口**」，
不是从零取证。

做法：以**帮助页为界面主键**（`app/docs/labelshop-help-zh` 共 158 页；矩阵 605 行的「出处文件」列可直接聚合成界面组），
每个界面凑齐四件套 ---- ① 真机图或控件 dump ② 复刻版同界面图 ③ 并排图（`tools/parity/Compare-SideBySide.ps1`）
④ 自动断言（`app/scripts/ui-vNN.cjs` 或 `npm test:*` 用例名）。**每轮做 6–10 个界面**，
并在 `parity/matrix.md` 对应行的证据列**写出这四类文件名**（这样普查脚本就能统计到）。
每轮结束我（验收方）重跑普查脚本，看四件套百分比是否上升——这是本阶段唯一的主指标。

**已有现成清单可用**：`parity/界面清单.md`（验收方 round-104 建的批次 1/1b：主窗口骨架 + 11 个菜单 + 7 个对话框，
每行都写好了 ①真机文件名 ②复刻版文件名 ③现有并排图与其**日期** ④已命中的断言脚本 与「缺口」）。
注意 `parity/review/` 里的并排图都是 **09-14 ~ 09-17 的旧图**，之后跑了 100+ 轮，**必须重抓**才能当证据（新图命名 `cmp-<界面>-<round>.png`）。

顺序：主窗口骨架（菜单 12 项 / 工具栏 / 对齐栏 / 格式栏 / 状态栏）→ 新建与标签格式链路（含 P0）→
对象属性 4 类 × 页签 → 打印链路 → 数据库 / 脚本 → 系统选项 → 起始页 / 关于 / 帮助。

### 优先级 5 · 打印机链路（部分待用户配合）

佳博 GP-1324D 未装官方驱动（无打印队列），USB 直发待用户装驱动后验证；真机残留的 `TSC TSPL-N (203 dpi)` 需用户鼠标移除。你能先做的：对着 `print_printer_cfg_*` 帮助页把「有队列 / 无队列」两条发送路径的分支与错误提示再核一遍，补单测；把「删除已添加打印机」的完整链路（含残留项）与真机行为再对照一次（用户实测过这两处）。

## 四、每轮收尾（硬性）

```powershell
cd app
npm run build            # test:ui 跑 out/ 构建产物：改了 renderer 必须先 build
npm run typecheck
npm test                 # 含 editor / geometry / history / printer / print / render / workspace 等
npm run test:ui          # 79 个脚本，约 50 分钟；同一时刻只允许一套（独占锁，并发造假失败）
cd ..
powershell -File tools\parity\Check-Matrix.ps1
powershell -File tools\parity\Report-Progress.ps1
```

**本机血泪坑**：

- 改 `.ps1`（含用编辑器工具改）会**掉 UTF-8 BOM** → PS 5.1 按 GBK 解析中文报 `Unexpected token '}'`；改完确认首字节是 `EF BB BF`。
- 改 `app/package.json` 等无 BOM 的 UTF-8 文件：必须 `[IO.File]::ReadAllText($p,[Text.UTF8Encoding]::new($false))` 读、同编码写，改完 `node -e "require('./package.json')"` 验证。
- 打包前先 `Get-Process MaxLabel,electron | Stop-Process -Force`；打包脚本的「未检测到代码签名证书」走 stderr 会让退出码变 1，**产物是好的**。
- `parity/evidence` 校验器会把 matrix 证据列里含 `/` 或 `\` 的反引号片段当路径——写证据时避免反引号里放路径。

## 五、出包与推送（有用户可见改动时做）

bump `app/package.json` 版本 → `npm run build` → `node scripts/build-windows.cjs` → 计算 SHA256 写 `RELEASE-SHA256.txt` → 写 `RELEASE-NOTES-vX.Y.Z.md` 与 `tools/loop/logs/vX.Y.Z-gates.md` → `git commit` + `git tag -a vX.Y.Z` + `git push origin main` 与 tag（网络不稳时重试 2-6 次）。
