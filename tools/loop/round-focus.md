# 本轮起（循环 round-104 起）指定任务与硬性口径

> 本文件由验收方维护（2026-09-21 重写；上一版写于 09-17、基线 33%，已全部过期——**不要再按旧内容选活**）。

## 一、当前真实基线（验收方实测，2026-09-21）

| 项 | 值 |
| --- | --- |
| 矩阵 `parity/matrix.md` | 605 条 → **已实现 605 / 部分 0 / 未实现 0 / 待核 0（100%）**；`Check-Matrix.ps1` exit 0 |
| 需求清单 `parity/需求清单-待验证队列.md` | **194/194 全部有结论（待填 0 条）** |
| 全量 UI 回归 | **79 个脚本**（`ui-v48 … ui-v128`）全绿（`tools/loop/logs/round-60g-ui.log`） |
| 单元门禁 | `npm test` 全绿（typecheck / architecture / color / barcode / editor 40 / geometry / history / printer / print / render 54 / workspace）；另 label-spec / label-formats / update / title / license / close / installer / evidence 全 exit 0 |
| 已发布 | **v1.0.17**（`app/release/MaxLabel-Setup-1.0.17.exe`，SHA256 `76A01BCE…6A1E`，冒烟 4/4）；`main` 与 tag `v1.0.0…v1.0.17` 已推送 |
| 未收口差异 | `parity/diffs.md`：**DIFF-63**（待真机点按）、**DIFF-50 / DIFF-60**（观察项） |

**`parity/backlog.md` 是历史档案（900+ 行，最新在最上），里面很多"待办"早已做完**——只按本文件选活；backlog 只在收尾时追加本轮记录。

## 二、做活的硬性口径（用户要求，违反即返工）

1. **结论三态写清**：原版**有** / 原版**无** / 原版**有但受限**，并给可复现证据（真机截图或控件 dump 文件名 + 帮助页名 + 复刻版断言名/命令）。
2. **不许按猜测造功能**。语义不确定 → 先取证（真机 > 帮助）；取不到就在 `parity/diffs.md` 登记为「待取证」并写清**已试过的手法与失败现象**，不许实现猜测版本、不许留 TODO/假实现。
3. **不许为过测试而改弱断言**；测试随行为变更失效时，改测试可以，但断言强度不能降，并在提交信息里说明理由。
4. 每收口 1-2 条就 `git add -A && git commit`（含 matrix/diffs/清单更新），提交信息写清条目编号（DIFF-xx / 清单第 N 条 / 矩阵条目号）。

## 三、优先级清单（按顺序取第一项尚未完成的）

> **验收方 2026-09-21 13:47 整理的当前队列（先看这里，再往下看细节；旧段落保留作证据与施工依据）**
>
> | 序号 | 事项 | 状态 |
> | --- | --- | --- |
> | 1 | **P0 追加 6 的最后一步**：真机**打印输出**里有没有孔 → 已登记为 **DIFF-70（待取证）** | ⏳ **链路已打通但产出空白**：round-110 从编辑器右侧面板「打印」进真机打印对话框 → 打到 Microsoft Print to PDF，`r110-hole-print.pdf`（835B）经**验收方独立解压核对**：单页、`MediaBox=595.32×841.92`（**A4**，不是 100×70mm）、**无 /Resources**、唯一内容流 60 字节且**只有 1 个 `cm`、零个绘制算子** → 整页空白，连标签轮廓都没有，**"孔在不在"无从判断**。下一步（round-110 已写进 DIFF-70）：先放一个满标签的大矩形再打印确认输出非空；查打印对话框的纸张/输出方式（文档 100×70 却出 A4）；或换 XPS Document Writer。**注**：我 round-47 记的"本机不可达（Ctrl+P 弹登录框）"只对**快捷键**成立——**编辑器右侧面板的「打印」按钮不需登录**，这条已由 round-110 推翻 |
> | 2 | **P1.5 属性页签**：名称/顺序按真机（文字=数据源/字体/文本/常规；条码=数据源/条码/字体/常规，**并掉码制专页**；矩形=图形/常规；表格=表格/常规；图片=图片/常规） | ⏳ 施工单与影响面已备（见 P1.5 段） |
> | 3 | **P0 剩余小项**：`OptionsDialog.tsx:266` 的 `直角矩形`（**先取证**真机「系统选项」那处下拉原文） | ⏳ 待取证 |
> | 4 | **P1 DIFF-63**：序列号面板「重置初始值/立即重置」仍需一份能让真机显示重置组的模板/数据库状态 | ⏳ 待取证 |
> | 5 | **P2 对象编辑三处**：CTRL+拖动是复制还是移动；有无对齐参考线吸附；点空心矩形内部算不算选中 | ⏳ 待真机 |
> | 6 | **P4 界面清单**：按 `parity/界面清单.md` 逐界面凑四件套（并排图必须用当前构建重抓） | ⏳ 规模最大的一项 |
> | 7 | **P3/P5**：DIFF-50/60 观察项 + E 区边界；打印机链路待用户装驱动 | ⏳ |
>
> **已结清（别再当待办）**：DIFF-69（孔洞=矩形假功能，已贯通修复）；两对话框字段渲染**结构债**（已抽 `paperHoleFields` 共享）；`应用` 按钮（真机隐藏，已按真机隐藏）；圆角半径（固定 1mm，四条路径都有数值断言）。

**P1.5 验收方 round-56 追加：页签对齐了，但「条码」页内的字段名还没对齐真机原文**

页签（`propertyTabs.ts`）已按真机改对 ✓（文字/条码/图片/表格/矩形=数据源·字体·文本·常规 等），码制专页也已并入条码页的「条码特殊选项」分组 ✓。
但逐行核对 `ObjectPropsDialog.tsx` 的**条码页字段名**，与真机取证（`PROBE-verifier-object-tabs.md` 的条码页表）仍有差：

| 真机原文 | 复刻版（在途，实测行号） | 差异 |
| --- | --- | --- |
| `条码符号类型(码制)(B):` | `条码符号类型(码制)`（L601） | 缺加速键 `(B)` |
| `X 尺寸(X):` | `X 尺寸`（L618） | 缺 `(X)` |
| `码高(H):` | **`码 高（毫米）`**（L645） | **多了空格**、缺 `(H)`、真机单位是「毫米」跟在框后而非写进标签 |
| `供人识读字符` 组的 `位置(P):` / `垂直偏移(O):` / `对齐方式(A):` | `供人识读字符 · 位置` / `… · 垂直偏移（毫米）` / `… · 对齐方式`（L670/683/693） | 命名风格不同、缺加速键 |
| 组内还有 **`字符模板(I)`** 复选框 | 文件里 `字符模板` 出现在 **L1440**（另一个区块），条码页内**疑似没有** | **待核**：可能缺一项 |

要求：① 条码页字段名统一改成**真机原文 + 加速键**（`码高(H):`、`X 尺寸(X):`、`位置(P):`…），单位按真机放在框后；
② 核实 `字符模板(I)` 是否属于「供人识读字符」组、复刻版条码页有没有它；③ 顺手看 `对齐`（L706）在真机条码页是否真的存在（我的取证表里没有这一项，**别把自造项当特性**）；
④ 改完补/改断言时**只准加严**（照 `ui-v109` 那条从"包含"改成"整数组全等"的先例）。

**P1.5 验收方 round-50 复核：当前实现与真机的逐对象对照（照这张表改，别改错方向）**

`app/src/renderer/src/features/object-properties/propertyTabs.ts` 现状（行号实测）：

| 对象 | **复刻版现状**（`propertyTabsFor`） | **真机取证**（`PROBE-verifier-object-tabs.md`） | 差异 |
| --- | --- | --- | --- |
| 文字 | `通用 / 文字 / 字体 / 数据`（L15-18） | **`数据源 / 字体 / 文本 / 常规`** | 名称+顺序全错 |
| 条码 | `通用 / 条码 / 字体 / 数据 / <码制专页>`（L19-23） | **`数据源 / 条码 / 字体 / 常规`** | 名称顺序错，且**多出码制专页**（真机没有） |
| 矩形/椭圆 | `通用 / 方框和圆形`（L31-33） | **`图形 / 常规`** | 名称错（真机叫「图形」） |
| 直线 | `通用 / 直线和斜线` | `图形 / 常规`（同矩形，按真机取证表） | 名称错 |
| 表格 | `通用 / 表格`（L30） | **`表格 / 常规`** | 顺序错（真机「常规」在后） |
| 图片 | `通用 / 图片 / 数据`（L27-29） | **`图片 / 常规`** | 多「数据」、顺序错 |
| RFID | `通用 / RFID / 数据`（L24-26） | 真机**工具菜单无 RFID**（DIFF-65 未收口） | 先按 DIFF-65 结论处理 |

**断言影响面（round-50 实测计数，`app/scripts/ui-v*.cjs` 里命中页签相关字样的次数）**：
`ui-v109` 41、`ui-v102` 26、`ui-v100` 25、`ui-v125` 22、`ui-v123` 18、`ui-v94` 13、`ui-v116`/`ui-v117` 各 12……
（这些计数含"数据源/字体"等**其它上下文**的命中，不是全部都要改；实施时按**具体断言行**逐个确认，别按文件一刀切。）

**建议的落地顺序（每步独立提交 + 同轮改断言，保持门禁可判定）**：
1. 先做**只改名不改结构**的：矩形/椭圆的「方框和圆形」→「图形」、「通用」→「常规」并调序；
2. 再做**文字**（4 页签整组替换 + 顺序）；
3. 再做**表格/图片**（页签增减）；
4. **条码**放最后（要同时删「码制专页」并迁移 `ui-v106`(17 处)/`ui-v125/126/127`、`barcode-spec.test.ts`(约 20 处)、`ui-v109:263` 的页签文字断言）；
5. 每步跑一次门禁；若某步让多个脚本红，**改断言而不是改回产品**（断言口径以真机取证文件为准）。

### 优先级 0（**用户实测反馈**，2026-09-21）· 「选择纸张」里的自定义与原版不同 + 各种纸张的圆角弧度待校准

**验收方已确认的证据**（都是我这次亲手从真机截图里读出来的，可直接引用）：

- 真机 `parity/reference/labelshop/60-dlg-choose-label.png`（**选择标签格式**）：`标签名称(L)` 是下拉，当前值 `[608053] 100mm x 70mm 圆角8枚/页 20页/盒`；
  底部按钮 `选择(Q)`（默认高亮）/ `自定义(N)` / `取消(C)` / `帮助(H)`；提示行原文「如以上列表中没有尺寸适合的标签格式，请点击"自定义"，自行设置标签的尺寸。」
  → **「自定义」在原版是一个按钮，不在标签名称下拉里**。
- 真机 `61-dlg-custom-label.png`（**标签格式设置**，页签 打印机/页面/标签/其它）的 `标签` 页只有：`标签`组（宽度/高度/形状）、`间距`组（列距/行距）、
  `孔洞`组（孔洞下拉 + 毫米，禁用）、`行列`组（列数/行数）——**没有「圆角半径」输入框**。
- **圆角弧度量化**（对 `60-dlg-choose-label.png` 做 1:1 像素分析）：格宽 184px ↔ 100mm（1.84 px/mm），圆角矩形的角弧只有 **2–3px ≈ 1.1–1.6mm**
  （逐像素图：上边自 x=283 起、左边在 x=281、y=88 起稳定）。复刻版 `PaperFields.tsx` / `paper.ts` / `LabelEditor.tsx` 用 `min(w,h)*0.12`
  （100×70 → **8.4mm**），比原版大 5–6 倍 —— 这就是用户看到的「圆角不一样」。
- 复刻版现状（读代码所得）：`NewLabelDialog.tsx` 在 `标签名称(L)` 下拉末尾塞了 `自定义` 项并**内联**展开宽/高输入框；底部「自定义」按钮也只是
  `setCustom(true)` 展开同一对输入框；`PaperFields.tsx` 在形状=圆角矩形时多出「圆角半径（mm）」输入与「标签纸颜色」。

**本轮要求（先取证、再改，不许猜）**：

1. 真机点一次 `自定义(N)`（用 `tools/parity/LabelShopCtl.ps1` 的 `btn:`/`postclick:` 手法），截图 + `Read-LabelShopDialogTree.ps1` 控件树，
   确认它打开的到底是什么（是否就是「标签格式设置」四页签对话框、初始值、确定后回到哪里），结论写进 `parity/reference/labelshop/PROBE-round1xx.md`。
2. 枚举真机 `标签名称(L)` 下拉的**全部条目**（`Probe-LabelShopCombos.ps1`），确认 275 条格式之外**有没有**「自定义」项。
3. 量真机**编辑器画布**上的圆角半径：打开一个 `Corner=1` 的圆角格式（如 `[608053]`），把视图放大到 ≥200% 后截图，
   用状态栏缩放（或画布毫米标尺）做标尺 + 逐像素 ASCII 图数出圆角像素 → 换算成 mm；
   同时确认**预览 / 编辑器画布 / 打印输出**三处的圆角是否一致。
   **验收方已量好的标尺（round-104，可直接引用，别再重复量预览）**：见
   `parity/reference/labelshop/PROBE-verifier-corner-radius.md` —— 真机预览里 100×70 与 100×150 两种圆角标签的角弧
   **都只有约 2px（≈0.9–1.1mm，且与标签尺寸无关）**，而复刻版是按短边 12%（8.4mm / 12mm）画，**大约 7 倍**。
   量测工具：`node tools/parity/measure-corner-radius.cjs <png> <x0> <y0> <x1> <y1> [标签宽mm] [角区x 角区y 宽 高]`
   （打印边线清单 + 1:1 ASCII 角区图；**不要**用自动拟合，真机预览有纸张外框与格子两套近邻描边会误判）。
4. 按取证结论改实现：① `选择标签格式` 的标签名称下拉不再放「自定义」，`自定义(N)` 走真机行为；
   ② 圆角半径采用真机规则（固定值就写常量，随尺寸变化就写公式），并且**只有一个半径来源**，
   让 `NewLabelDialog` 预览 / `PaperFields` 预览 / `LabelEditor` 画布 / 打印场景与位图输出四处一致；
   ③ 真机没有的字段（如「圆角半径」输入框）删掉或改只读展示，依据写进 `parity/diffs.md`；
   ④ 直角矩形=0、圆形=直径、带孔=直径+孔 三种形状一并核对（用户就是要「各种纸张」都对）。

   **验收方静态排查：半径规则的全部消费点（别漏，共 3 处默认值 + 7 处绘制/裁剪）**

   | 位置 | 作用 | 现状 |
   | --- | --- | --- |
   | `app/src/shared/domain/paper.ts:26` | `paperPath()` 里 `cornerRadiusMm ?? min(w,h)*0.12` | **12% 默认值的源头**，应改成真机规则 |
   | `app/src/renderer/src/editor/LabelEditor.tsx:957` | 编辑器裁剪路径（再 `*10*zoom` 换算像素） | 又写了一遍 `*0.12` 默认值 → 应改为调用 paper.ts 的统一函数 |
   | `app/src/renderer/src/dialogs/PaperFields.tsx:24` | 「圆角半径（mm）」输入的显示默认值 | 又写了一遍 `*0.12`；该字段本身真机没有，见 ③ |
   | `NewLabelDialog.tsx:273 / 288 / 303` | 选择标签格式预览（平张 / 卷筒切段 / 多格） | 经 `paperFor()` 只设 `shape='roundRect'`，**不带半径** → 走 12% 默认 |
   | `PaperFields.tsx:39` | 标签格式设置「标签」页的 SVG 预览 | 同上 |
   | `LabelEditor.tsx:960 / 961` | 编辑器纸张大纲 + 裁剪路径 | 同上 |
   | `app/src/renderer/src/print/renderLabel.ts:77` | **打印/位图输出的裁剪**（`g.clip(Path2D(paperPath(...)))`） | 同上 → 打印出来的圆角也会跟着 12% |
   | `app/src/shared/print/scene.ts:231 / 258 / 298` | 把 `doc.layout.cornerRadiusMm` 塞进 `paperGeometry`（打印场景） | 透传点：改规则时确认这里传的仍是最终值 |
   | `app/src/shared/domain/document.ts:612` | 文档归一化时 clamp `cornerRadiusMm` | 若该字段被删/改为内部常量，这里要同步 |

   改法建议：在 `paper.ts` 里出一个**唯一**函数（如 `roundRectRadiusMm(width, height)`），上面 3 处默认值全部改成调它；
   7 处绘制/裁剪点保持只走 `paperPath`，不要在调用侧再算半径。改完**必须有断言钉住每一条路径**
   （预览 SVG 的 `A r r` 半径值、编辑器 clipPath、`renderLabel` 位图裁剪的半径），否则"四处一致"只是口头保证。

   **现有测试的连带影响（验收方查过）**：`app/scripts/render-regression.ts:157-166` 的纸张断言
   **显式传了** `cornerRadiusMm: 10`（并断言 `cornerRadiusMm === 10`、`innerDiameterMm === 20`），
   所以**只改默认值规则不会碰它们**；但如果按真机把「圆角半径」字段/模型字段**删掉**，
   这 4 条（rect/roundRect/ellipse/disc 各一条）+ `document.ts:612` 的归一化必须同步改，
   否则门禁会红——两种改法二选一，别做成"字段删了、测试还断言它"。
5. 断言：新增 `app/scripts/ui-v1NN.cjs` 并注册进 `run-regression.ps1` —— 标签名称下拉不含「自定义」项、`自定义(N)` 行为与真机一致、
   圆角矩形的弧度等于取证值、直角/圆形/带孔 三种形状的弧度分别为 0 / 直径 / 直径+孔。
6. 更新 `parity/diffs.md`（**新登记 DIFF-66**）与 matrix 里相关条目（`A-*` 新建标签 / `C-*` 标签格式设置）的证据。

**P0 追加（验收方读 codex round-105 的取证后整理，同一对话框的其余待办；证据 `probe-round105-custom-label-tree.txt`、`probe-round105-custom-label-values.txt`、`round105-custom-label.png`）**

- **已确认**：`选择标签格式 → 自定义(N)` 打开的就是 **`标签格式设置`**（＝工具栏那个，同页签），
  且 `形状=圆角矩形` 时**仍然没有「圆角半径」字段** —— P0 的 ①③ 到此有真机证据支撑。
- 真机 `标签` 页字段原文：`宽度(W)` / `高度(H)`（＋毫米）、`列距(P)` / `行距(L)`（＋毫米）、
  `列数(C)` / `行数(R)`（Edit+Spin）、`形状` 组 combo（**3 项，`圆角矩形` 是第 2 项**）、
  `孔洞` 组 combo（**3 项，`无` 是第 1 项**）+ 一个 **禁用** 的尺寸 Edit 与 `毫米`；
  预览行文字 = `100.00 x 70.00 毫米 [4行 2列]`；底部按钮 `确定` / `取消` / **`应用(A)`（禁用）** / `帮助`。
  → 待办：① 枚举 `孔洞` combo 的 3 项文本（复刻版现在只有 `无`/`圆洞` 2 项）；
  ② 核对复刻版有没有 `应用` 按钮及其禁用规则；③ 预览行文字格式对齐。
- 该 dump 里还有一个 **不可见** 的 `多行标签` 按钮 → 待办：取证它的显示条件与作用（复刻版未找到该文案）。
- 真机 `打印机` 页（同一对话框的页签，控件树里可见但隐藏）：`名称(N)` combo、按钮 `标准驱动(S)` / `设置(S)` /
  `高级设置(A)` / `安装(I)`、`输出方式:` combo、按钮 `整页反相打印` / `镜像输出` / `单页任务模式`。
  → 待办：逐项核对复刻版是**缺**还是**在别的对话框里**（复刻版 `反相`/`单页` 全库搜不到，
  `镜像` 只在对象属性里、`打印机属性` 在 `PrintDialog`/`PrinterSettings`）——若分散在别处，要在 `diffs.md` 写清
  「等价替代 or 入口不一致」，不要悄悄当成已有。

**P0 追加 2（验收方把真机 `标签` 页与复刻版 `TemplatePropsDialog.tsx` 逐字对完的差距清单，按真机改）**

| # | 真机（`probe-round105-custom-label-tree.txt` 原文） | 复刻版现状 | 待办 |
| --- | --- | --- | --- |
| 1 | **五个分组框**：`标签` / `间距` / `行列` / `形状` / `孔洞` | 全是平铺 `FormField`，**没有分组框** | 加分组框并按真机分组 |
| 2 | `宽度(&W):` / `高度(&H):` | `标签宽度（mm）` / `标签高度（mm）` | 字段名与加速键对齐真机 |
| 3 | `列距(&P):` / `行距(&L):` | `水平间距（mm）` / `垂直间距（mm）` | 同上 |
| 4 | `列数(&C):` / `行数(&R):`（Edit **+ Spin 微调**） | `列数` / `行数`（纯 number） | 改名 + 补上下微调形态 |
| 5 | `形状` 分组内 combo（3 项，`圆角矩形` 第 2 位） | combo 3 项 ✓（同一个 `PaperFields`） | 只需挪进分组框 |
| 6 | `孔洞` 分组内 combo（**3 项**）+ **禁用** 的尺寸 Edit 与 `毫米` | combo **2 项**（无/圆洞）+ 尺寸框（选中时启用） | **枚举真机 3 项文本**；补第 3 项；尺寸框的启用/禁用规则按真机（`无` 时为禁用态） |
| 7 | 页内预览行 `100.00 x 70.00 毫米 [4行 2列]` | 只有 `PaperFields` 右侧的 SVG 小图 | 补这行文字（格式逐字：两位小数 + ` 毫米 ` + `[N行 M列]`） |
| 8 | 底部按钮 `确定` / `取消` / `应用(A)`（**禁用**）/ `帮助` | 需核对（`TemplatePropsDialog` 里未见到；`应用` 文案只在别处出现） | 按真机补按钮与禁用规则 |
| 9 | `标签纸颜色` **不在** 标签页 | 复刻版把 `标签纸颜色` 放在 `PaperFields`（标签页可见） | 核对该字段真机归属页（帮助 `label_page_page.html` 指向 `页面` 页），按真机挪页 |

> 尚未比对：真机 `页面` / `其它` 两个页签的字段名与分组（codex 的 tree dump 只覆盖了 `标签` 与 `打印机` 两页）——
> 下一步取证时把这两页也 dump 出来再比，别凭空改。

**P0 验收要点（验收方会在轮末逐条查，按此自检）**

1. **半径规则已核**：`paper.ts` 新增 `roundRectRadiusMm()` + 常量 `LABELSHOP_ROUND_RECT_RADIUS_MM = 1`，
   验收方用 `node tools/parity/measure-paper-radius.cjs` 在**在途源码**上实测：100×70 / 100×150 / 60×60 / 40×30 **全部 = 1mm**，
   显式传 2mm 仍得 2mm，直角仍为 0 ✓。
2. **还没改的两处（轮末必查）**：
   - `app/src/renderer/src/editor/LabelEditor.tsx:957` 里还留着 `doc.layout?.cornerRadiusMm ?? Math.min(...)*0.12`
     的**第二份默认值** → 若不同步调 `roundRectRadiusMm()`，编辑器裁剪路径会仍按 8.4mm 裁，与 `paperPath` 的 1mm **不一致**（形状与裁剪对不上）；
   - `app/src/renderer/src/dialogs/PaperFields.tsx:24` 的「圆角半径（mm）」输入框（含第三份 `*0.12` 默认值）→ 真机没有该字段，应移除
     （移除后该处默认值自然消失；若保留字段则必须改默认值并说明理由）。
3. **新增的 `app/src/renderer/src/dialogs/CustomLabelFormatDialog.tsx` 要自检**：不要和 `TemplatePropsDialog` 出现两份实现漂移——
   两者要么共用同一组字段组件（`PaperFields` 等），要么明确分工（新建入口 vs 模板属性入口），并在 `diffs.md`/矩阵里写清入口关系。
4. 断言要求不变：预览 SVG 的 `A r r`、编辑器 `clipPath`、`renderLabel` 位图裁剪三条路径都要有**数值断言**钉住半径=1mm。
5. **接线必查（验收方 10:26 实测：新对话框目前还没有被任何地方引用）**：`NewLabelDialog` 的 `自定义(N)` 必须真的打开
   `CustomLabelFormatDialog`，并且**不再**在 `标签名称(L)` 下拉里放 `__custom__` 项、不再内联展开宽高——
   否则用户看到的行为没变（P0 的第一条诉求就没完成）。同时 `确定` 后要按真机**直接创建标签并回编辑器**（不回列表）。
6. **防漂移必查**：新对话框 import 的是 `paperPath` 而不是 `PaperFields`，即标签页字段被**重写了一份**——
   要么改成复用 `PaperFields`（推荐，孔洞/形状规则只留一处），要么在 `diffs.md`/矩阵里写清两个入口的分工与共享边界；
   不允许出现"两个入口各有一套孔洞/形状规则，改一处漏一处"。
7. **`ui-v129.cjs` 验收方审阅结论（断言强度合格，两处要清/补）**：核心断言已是**值级**（下拉恰好 42 项、
   预览 `A 1 1`、页签文本数组、初始值 100/70/2/4、`clipPath` 的 `/^M 10 0 H 990/`=10px=1mm、无圆角半径字段）✓；
   - 要清：第 88 行 `console.log('DEBUG clipPath', …)` 属调试残留，提交前删掉；
   - 要补：**「P0 追加 2」那 9 条还没有断言**——分组框（标签/间距/行列/形状/孔洞）、孔洞下拉 **3 项**、
     `应用` 按钮**禁用**、预览行 `100.00 x 70.00 毫米 [4行 2列]`；实现完必须补断言，不能"做了不钉"。
   - 健壮性提示：第 89 行的断言把文档宽度写死成 100mm，日后推广到其他尺寸需参数化。

**P0 验收方终验（round-105 提交 `68a2a93` 之后，2026-09-21 10:58）**

- ✅ **核心诉求已修并独立复核**：`paper.ts` 提交后复测半径 **各尺寸=1mm**（100×70 / 100×150 / 60×60 / 40×30）、显式 2mm 仍保留、直角=0；
  编辑器 `LabelEditor.tsx` 与 `PaperFields.tsx` 的两处旧默认值已清除；`ui-v129.cjs` + `render-regression.ts`（+6 条，
  含 `renderLabel clips the default roundRect corner with the shared radius`）把三条绘制路径都钉成了**数值断言**。
- ⏳ **「追加 2」的 9 条仍未做**（我在提交后的代码里逐项 grep 过）：
  `CustomLabelFormatDialog.tsx` 里 `孔洞` 下拉仍是 **2 项**（真机 3 项）；`应用` 按钮 **0 处**；`多行标签` **0 处**；
  `间距/行列/形状/孔洞` 只是普通 label（**不是真机的分组框**）；`打印机` 页还是占位（只有只读"名称(N)"+`输出方式`，
  真机是 `标准驱动(S)/设置(S)/高级设置(A)/安装(I)/整页反相打印/镜像输出/单页任务模式`）。
- ⚠️ **遗留**：`ui-v129.cjs` 里还有 **2 处 `DEBUG`** 输出未清（会污染门禁日志）。
- ⚠️ **记账口径**：`parity/diffs.md` 的 `DIFF-66` 已标 `✅ 已修` —— 就 P0 **核心**（自定义入口 + 圆角规则）而言成立，
  但「追加 2」的 9 条**不在其内**；请在 DIFF-66 里写明"核心已修，同对话框其余项另列"，或另开 DIFF 条目跟踪，
  不要把 9 条未做项一起算进"已修"。

**P0 验收方实测（2026-09-21 11:30，用 `tools/parity/Verify-LabelFormatDialog.cjs` 对**当前构建**跑，8/12 PASS）**

- 已通过：到达选择标签格式 ✓、**下拉不含「自定义」** ✓、能点「自定义(N)」✓、**打开的是四页签对话框** ✓、
  含 `标签/间距/行列/形状/孔洞` 文案 ✓、含真机字段名（宽度/高度/列距/行距/列数/行数）✓、形状下拉 3 项 ✓、
  **页内无「圆角半径」** ✓ —— 也就是 **P0 核心两条都过机器检查**。
- **确认为真缺口（必须补）**：
  1. `孔洞` 下拉 **2 项**（实测 `["无","圆洞"]`）→ 真机 3 项（第 3 项文本待真机枚举，见
     `PROBE-verifier-labelformat-strings.md` 第三节）；
  2. **没有「应用」按钮**（真机有且禁用）；
  3. **没有预览行** `100.00 x 70.00 毫米 [4行 2列]`。
- 待细化（可能是我这条检查的范围过宽）：`④b 不再用「标签宽度/水平间距/垂直间距」` 报 FAIL ——
  我扫的是"所有可见对话框的文本"，可能把别的对话框（如 `TemplatePropsDialog` 的字段名）算进来了；
  下一轮我会把它限定到 `custom-label-dialog` 子树内再判定，避免误报。

**P0 验收方终版实测（2026-09-21 11:45，工装已修正 → 10/13 PASS，**只剩 3 条真缺口**）**

修正了两个工装自身的问题（都已写进脚本注释，避免以后再踩）：
① 断言范围从"所有可见 `*-dialog` 的文本"收窄到 **`custom-label-dialog` 子树**（之前把 `TemplatePropsDialog` 的自造名算进来了）；
② `④b` 原用 `/标签宽度/` 判定会**误报**——`textContent` 会把分组框 legend「标签」与字段「宽度(W):」连成「标签宽度(W):」；
改成查带（mm）后缀的自造名（`标签宽度（mm）` 等，那才是 `TemplatePropsDialog` 用的形态）。

| 检查 | 结果 |
| --- | --- |
| 到达选择标签格式 / 下拉不含「自定义」/ 能点「自定义(N)」/ 出现 `custom-label-dialog` / 含四页签 | **PASS** |
| 五个分组框（标签/间距/行列/形状/孔洞） / 字段名 = 宽度(W)·高度(H)·列距(P)·行距(L)·列数(C)·行数(R) | **PASS** |
| ④b 不用自造名 / 形状下拉 3 项 / 页内无「圆角半径」 | **PASS** |
| **⑤ 孔洞下拉 3 项** | **FAIL**（实测 2 项：`无`、`圆洞`） |
| **⑥ 有「应用」按钮且禁用** | **FAIL**（不存在） |
| **⑦ 预览行 `100.00 x 70.00 毫米 [4行 2列]`** | **FAIL**（不存在） |

→ 下一轮就把这 **3 条**补掉（同对话框的其余 6 条：真机字段名/分组框已到位；打印机页 6 个控件与 `多行标签` 另算），
补完我用同一工装复跑，应当 13/13。

**P0 追加 3（round-106 真机枚举带来的两条新证据 + 工装升级，2026-09-21 11:45）**

codex 本轮新增真机枚举 `probe-round106-custom-label-combos.txt`（我已核对原文），**下拉逐项文本终于拿到**：

| 下拉 | 真机 3 项原文 | 复刻版现状 | 结论 |
| --- | --- | --- | --- |
| `形状` | **`方角矩形`** / `圆角矩形` / `圆形` | `直角矩形` / `圆角矩形` / `圆形` | ⚠️ **命名不一致：真机是「方角矩形」，复刻版写成「直角矩形」**（新发现，之前只数了项数没比文本） |
| `孔洞` | **`无` / `圆洞` / `矩形`** | `无` / `圆洞` | 缺第 3 项 **`矩形`**（文本已知，不必再猜） |

我已把验收工装升级为**逐项文本**断言（原来只断言"3 项"），复跑后现在报 **5 条缺口**：
`⑤ 孔洞 3 项`、`⑤b 形状文本`、`⑤b 孔洞文本`、`⑥ 应用按钮`、`⑦ 预览行`。
→ 这 5 条一起补（外加打印机页 6 控件与 `多行标签`），补完复跑应到 15/15。**这也说明"只断数量不断文本"会漏掉真机改名。**

**P0 途中复核（round-106 在途源码，11:39 验收方实测）**：`孔洞` 下拉已补齐 3 项（`无`/`圆洞`/`矩形`）✓；
`形状` 已改名 **`方角矩形`**（`CustomLabelFormatDialog` + `PaperFields`）✓；新增 `ui-v130.cjs` 并已注册 ✓。

- ⚠️ **遗留 1**：`OptionsDialog.tsx:266`（系统选项里的形状下拉）仍写 `直角矩形`。
  → **先取证真机「系统选项」里那个下拉的原文**再决定改不改；不要凭「标签格式设置」一处的证据去改另一个对话框。
- ⚠️ **遗留 2**：`NewLabelDialog.tsx:46`、`PaperFields.tsx:3` 的注释仍写「帮助 label_page_label.html 的形状只有直角矩形…」——
  这其实揭示了一条**帮助与真机 UI 用词不一致**：帮助写 `直角矩形`，真机 UI 是 `方角矩形`。
  → 在 `parity/diffs.md` 记一条「帮助 vs 真机用词差异（以真机 UI 为准）」，注释同步改成注明这一点。

**P0 追加 5（验收方更正自己上一条口径：`应用` 按钮在真机是**隐藏**控件，不能做成可见！2026-09-21 11:55）**

- 证据 1：`probe-round105-custom-label-tree.txt` 里那一行是 **`[ ] class=Button DISABLED text='应用(&A)'`** —— 行首 `[ ]`
  表示**不可见**（我先前只看了文本，把它当成"有且禁用"，这是我的误读）。
- 证据 2：真机截图 `r107-hole-rect-20.png`（claude round-107 抓的「标签格式设置」，孔洞=矩形/尺寸 20）底部**只有**
  `确定 / 取消 / 帮助` 三个按钮，**没有 `应用`**；同一张图也确认了：孔洞尺寸框在矩形下**点亮**、**只有一个尺寸框**、
  预览卡片里**不画孔**、预览行是 `100.00 x 70.00 毫米 [4行 2列]`、列数/行数带微调箭头、分组框与字段名与 dump 一致。
- 要求：
  1. 复刻版**不要显示 `应用` 按钮**（与真机一致）；`ui-v130.cjs` 里那条「应用按钮存在且禁用」的断言要改成真机口径
     （断言**不存在**，或断言它 `hidden`）。**不允许**为了保住断言而留一个真机没有的可见按钮。
  2. 若怀疑 `应用` 在某些条件下会出现（例如改了字段之后），**先取证**再决定；没证据就按"不显示"处理。
  3. 同理注意 `多行标签`（真机也是 `[ ]` 隐藏）——不要实现成可见按钮。
- 顺带确认（这三条**已对上**，无需再改）：孔洞=矩形时尺寸框**可用**、单值＝边长；预览不画孔；预览行文本格式与真机逐字一致。

**P0 追加 7（验收方对 round-107 门禁失败的独立诊断：这是**真阳性**，不是 flaky，也不是测试过时）**

- 现象：round-107 全量门禁 **`test:ui` 失败，`FAILED SCRIPTS: ui-v90.cjs`**；其余门禁全 PASS（含 round-107 新增的 `ui-v130` **14/14 PASS**）。
- 验收方独立复现：`$env:MAXLABEL_UI_SCRIPT='ui-v90.cjs'; npm run test:ui` → **13/14，确定性失败**：
  `FAIL A-42 模板属性包含四页签和关键字段`。
- 根因（代码级）：round-106/107 只把**「自定义」对话框**（`CustomLabelFormatDialog`）与共享的 `PaperFields`
  的字段名改成真机原文（`宽度(W):/高度(H):/列距(P):/行距(L):/列数(C):/行数(R):` + 五个分组框），
  但**工具栏那个「标签格式设置」对话框**（`TemplatePropsDialog.tsx`）**没同步**——它仍写着 `标签宽度（mm）`（`:259`）、
  `水平间距（mm）`（`:267`）、`垂直间距（mm）`（`:270`）。`ui-v90` 的 A-42 现在按真机口径断言，于是如实报错。
- 结论：**产品侧要改**（把 `TemplatePropsDialog` 的标签页字段名与分组框按真机对齐），而不是把断言改回去；
  这正是我在 P0 验收要点第 6 条点过的"两份实现漂移"风险——现在被测试抓住了，属于好消息。
- 要求：① `TemplatePropsDialog.tsx` 的标签页与 `CustomLabelFormatDialog` **共用同一套字段渲染**（推荐抽出共用组件，或至少共用字段名常量），
  确保以后只改一处；② 分组框 `标签/间距/行列/形状/孔洞` 同样补齐；③ 改完 `ui-v90` A-42 应通过，且**强度不降**；
  ④ 顺带清掉 `OptionsDialog.tsx:266` 的 `直角矩形`（**先取证**真机「系统选项」那处下拉的原文再决定）。

  **round-108 在途复核（验收方 12:40）**：`TemplatePropsDialog` 的标签页已经改对——分组框 `标签/间距/行列` 出现在
  `:275/:286/:297`，字段名改成真机原文 `宽度(W):`(:277) / `列距(P):`(:288) / `列数(C):`(:299)，自造名（标签宽度（mm）等）**已清零** ✓。
  但**"共用同一套字段渲染"只做了一半**：没有抽出共用组件/字段常量，两个对话框仍是**两份各自渲染**的实现
  → 建议下一步抽出共用字段组件（或至少共用字段名常量数组），否则同样的漂移以后还会发生；这条**不阻断门禁**，记为结构债。

  **结构债已还清（round-109 提交 `46fae82`，验收方 13:33 逐点核对）**：新增共享模块 `dialogs/paperHoleFields.ts`
  （`PAPER_HOLE_OPTIONS` / `PAPER_SHAPE_OPTIONS` / `maxHoleSizeMm` / `withHoleSelection` / `withHoleSize` / `holeSelectionOf`），
  `CustomLabelFormatDialog` 与 `PaperFields` 都改为从它取选项与规则 ✓；同时把孔形贯通到全链：
  `paper.ts`（几何）→ `shared/print/scene.ts` 三处（打印场景）→ `shared/domain/document.ts`（**持久化/归一化 `innerShape`**，
  注释明确写了"旧实现只留 innerDiameterMm 把 innerShape 丢掉"）→ `LabelEditor`（编辑器裁剪）→ `TemplatePropsDialog`（属性框）
  → `NewLabelDialog`（重开对话框时按 `innerShape` 回填，避免矩形被打回圆洞）✓。这条结构债可结。

**P0 追加 6（验收方读 round-107b 取证后新提的取证点：真机到底在哪些场景"画孔"？）**

`parity/reference/labelshop/PROBE-round107b-hole-rect.md` 已确证：真机在**对话框预览**和**编辑器画布**（点确定后、261% 缩放）
**都看不到孔**（两张截图逐像素一致；试过 `WM_SETTEXT`+`EN_CHANGE` 注入也无效），并据此把"矩形孔的精确画法"登记为**待取证**——
这个处置是对的。但由此浮出一个**新取证点**：真机是否**任何场景都不画孔**？

- 帮助 `label_page_label.html`：「标签的形状/孔洞**只在编辑标签时显示，并不会实际输出**」——即"输出不画"是明文的，
  但"编辑时显示"与实测的"编辑器画布也没孔"**互相矛盾**。
- 复刻版现状：`paperPath()` 的 `cutout` 在**对话框预览、编辑器裁剪、打印裁剪**三处都会画孔。
- 要求取证（不猜）：① 换一个**光盘类/圆洞**标签格式（`Corner=2`，如标签名含 `117mm/40mm` 的那两条）看真机是否画中心孔；
  ② 在真机上把 `孔洞` 从 `无` 切到 `圆洞`、尺寸给 20，看预览/编辑器是否出现孔；③ 打印到 PDF 看输出里有没有孔。
  结论落 `diffs.md`：若"从不画"→ 复刻版把孔只保留在**几何/命中/裁剪**里、不描出来（或在台账写明差异）；
  若"某些类型才画"→ 按类型分别对齐。

**P0 追加 6 的取证进展与下一步（验收方 round-43 上真机做过一轮，见 `PROBE-verifier-round43.md`）**

> ⚠️ **重大更正（round-44 验收方已取证，别再按旧结论干活）**：**真机会画孔**——
> 「标签格式设置」**对话框预览**里 8 个卡片中心各有一个小圆，**编辑器画布**中心也有一个 20mm 的圆
> （配置 `形状=圆角矩形 / 孔洞=圆洞 / 尺寸=20`，值由 `Read-LabelShopDialogValues` 回读确认）。
> 证据：`verifier-r44-hole-circle-20b.png`（预览画孔）、`verifier-r44-editor-hole20.png`（画布画孔）、
> 说明见 `parity/reference/labelshop/PROBE-verifier-round44.md`。
> round-107b 得出"真机不画孔"是**因为当时孔洞尺寸一直是 0.00**（不是真机不画）。
> → 所以**复刻版在预览与编辑器里画孔是对的，不要为了"对齐"把画孔去掉**；
> 仍待取证的只剩一条：**打印输出里有没有孔**（帮助说"不会实际输出"；做法：打印到 PDF 再渲染成位图看中心）。

已拿到：编辑态**文件菜单全项**（`verifier-r43-file-menu.png`，A-41 的直接证据）；`孔洞=圆洞` 时尺寸框**点亮**（黑字可编辑）；
**预览是随字段实时重绘的**——验收方误把 `20` 打进「高度(H)」后，预览立刻变成 `100.00 x 20.00 毫米 [4行 2列]`，
所以"改孔洞尺寸后预览仍无孔"**不能**用"预览不刷新"解释；点确定后的编辑器画布也**看不到孔**。

未拿到（下次照这个来，别重复踩）：
1. **别盲发 Tab 注入尺寸**：本轮 `keydlg:{TAB}` 落到了「高度」而不是「孔洞尺寸」。正确做法：先
   `Read-LabelShopDialogTree.ps1 -TitleLike '*标签格式设置*'` 取该 Edit 的**对话框内坐标**（先前实测在
   `xy=(1335,1087)`，对话框左上 `(762,871)` ⇒ 相对 `(573,216)`），用 `clickdlg:573,216` 点中后再 `keydlg:20`，
   并且**注入后立刻用 `Read-LabelShopDialogValues.ps1` 回读确认**再继续。
2. **从编辑态进「标签格式设置」不要猜项序号**：本轮 `{DOWN 8}{ENTER}` 没打开。先 `keys:%f` + `shotpopup:`
   抓菜单图数序号（菜单共 14 项，含 2 个禁用项：`分享(I)`、`导出打印机指令文件(E)`），或用 `menupick:` 步骤。
3. 冷启动进「选择标签格式」的**成功配方**（已复现两次）：
   `LabelShopCtl.ps1 -Action run -KeepOpen -StepsFile <含 click:900,60 → keys:^{n} → btn:新建标签模板 → btn:下一步>`
   —— 注意**不要先 `close`**（会把向导关掉），要么冷启动（向导自动出现），要么先 `click:900,60` 再 `^n`。
4. 三路问题的验收口径不变：① 光盘类（`Corner=2`）格式是否画中心孔；② `圆洞` + 尺寸 20 后预览/编辑器是否出现孔；
   ③ 打印到 PDF 的输出里有没有孔（PDF 需要渲染成图再判，别只看文本流）。

**P0 追加 4（验收方 11:45 在途复核发现的"半实现"，必须补齐，否则是假功能）**

`孔洞` 下拉现在有了第 3 项 `矩形`，但**几何侧没有实现**——逐行核对：

| 位置 | 现状 | 问题 |
| --- | --- | --- |
| `CustomLabelFormatDialog.tsx:73` | `...(draft.hole === 'circle' && holeSize > 0 ? { innerDiameterMm: holeSize } : {})` | **只有 `circle` 会写几何**；选 `矩形` 时**什么都不写** → 预览/新建出来的标签**没有孔** |
| `CustomLabelFormatDialog.tsx:129` | 尺寸框与「毫米」仅在 `draft.hole === 'circle'` 时点亮 | 选 `矩形` 时尺寸框始终灰 → 无法设尺寸 |
| `shared/domain/paper.ts:33` | `const cutout = hole > 0 ? ' ' + ellipse(...) : ''` | **只支持圆孔**（椭圆切孔），没有矩形切孔的实现；`PaperShape` 也只有 `rect/roundRect/ellipse/disc` |

要求（按"单一来源"原则，别在调用侧各写一份）：
1. 先**真机取证**：在真机「标签格式设置」里把 `孔洞` 切到 `矩形`，看**尺寸框是否点亮、是一个还是两个数值**（树里现在只有一个禁用 Edit + 毫米），
   以及预览里孔变成什么样（方形/圆角方？尺寸含义是边长还是对角？）——**不要猜**。
2. 按取证给 `PaperGeometry` 加"孔形"（如 `innerShape?: 'circle' | 'rectangle'`），在 `paperPath` 里实现矩形切孔；
   `PaperFields` 预览 / `CustomLabelFormatDialog` 预览 / `LabelEditor` 裁剪 / `renderLabel` 打印裁剪**都走同一个 `paperPath`**，
   不要各自实现。
3. 对话框里把 `rectangle` 正确映射成几何（含尺寸框的启用规则按真机）。
4. 补断言：选 `矩形` 后 preview 的 `d` 里出现**矩形切孔路径**（不是圆），以及打印裁剪一致；`ui-v130` 或新脚本里钉住。
5. 在 `diffs.md`/矩阵里写清 `孔洞=矩形` 的实现范围（编辑器预览是否也画孔、打印是否输出——帮助说形状与孔洞"只在编辑标签时显示，并不会实际输出"，请按帮助口径核实后写清）。

### 优先级 1 · DIFF-63：真机序列号数据源面板的「重置初始值: / 立即重置」

**验收方已独立复核 round-104 的取证（2026-09-21 09:2x）**：`PROBE-round104-DIFF63.md` 里 12 份证据文件全部存在且已入库；
`probe-63-serial-advanced-values.txt`（类型 4 项 / 序列 `0123456789` / 步长 1 / 重复 1 / 变化基准 2 项 / 初始值来源 2 项）
与 `probe-63-after-print-values.txt`（打印后显示数据 `12345679`、数据源=序列号 1/7）与文中结论一致，采信。

**版本受限提示（实现时逐字照抄，不要用转述）**——`probe-63-temp-basis-down.png` 标题为「功能提示」，正文与按钮原文：

```
这是一些只有专业版和企业版才可以使用的功能，目前的版本中无法使用。

目前您有两种方式可以使用这些功能：
    1、购买 签赋LabelShop 的专业版授权；
    2、购买云马科技的金牌会员服务。

您还可以进入专业版演示模式，体验和试用此项功能。程序演示模式下打印输出时，将在标签上随机添加一条提示信息！
```
按钮：`申请试用金牌服务` / `进入演示模式` / `了解更多信息` / `取消`（默认按钮＝`取消`）。
进入演示模式后再弹 `probe-63-18-label-mode-demo.png` 那条：标题 `LabelShop`，正文
「程序运行在演示模式下，您可以体验专业版功能。演示模式下打印输出时会在标签上随机添加一条提示信息！您可以关闭程序并重新启动，退出演示模式。」，按钮只有一个 `确定`。
（复刻版要断言这两处文案与按钮，必须与上图逐字一致；**不要**把「仅专业版和企业版可用」这类转述写进断言。）

**循环工装注意（验收方踩过）**：驱动器的卡死判定只看 `app\src`、`app\scripts`、`parity` 三个目录的**文件改动时间**。
codex 卡住时如果验收方往 `parity\` 下写文件（例如补证据），会把卡死计时器**重置**，让一个已经挂住的轮次多活 20 分钟。
所以：轮次在跑期间，验收方的笔记/工装只写到 `tools\loop\` 或 `tools\parity\`（不在 watchDirs 里），
往 `parity\` 的证据文件等轮次结束了再补。

- 已有证据：`parity/reference/labelshop/PROBE-round60.md`。`LabelShop.exe` 字符串资源（UTF-16LE）里确认了这两处字样，同面板的标题集也已拿全；但页面切源不重排（`tools/parity/Probe-LabelShopSerialPage.ps1` 已试过 `CB_SETCURSEL` + `CBN_SELCHANGE/CBN_SELENDOK` + 回车，序列号子面板连隐藏控件都不存在），复刻版**未实现**、也**没有按猜测实现**。
- 要求：① 用**真实鼠标路径**把真机数据源下拉切到「序列号」再 dump 数据源页：可用 `tools/parity/LabelShopCtl.ps1` 的 `postclick:` / `postdrag:` 步骤（本机 `SetCursorPos`+`mouse_event` 无效，务必用 PostMessage 手法），必要时先 `keys:` 聚焦下拉再用 `{DOWN}{ENTER}`；② 点一次「立即重置」，看它对「显示数据」的即时影响；③ 把子面板的完整控件/默认值写进 `parity/reference/labelshop/PROBE-round6x.md`；④ 结论落 `parity/diffs.md` DIFF-63 与 `parity/需求清单-待验证队列.md` 第 201 条；⑤ 语义确认后按真机实现并补 `app/scripts/ui-v1NN.cjs` 断言（含 `run-regression.ps1` 注册）；仍取不到就写清失败现象，保持「待取证」。
- 参考：真机序列号面板的资源标题集＝ 序列号 / 类型(&T): / 序列(&Q): / 步长(&S): / 增量(&A) / 减量(&D) / 重复(&E): / 序列号设置 / 重置 / 提示(&P): / 归位 / 初始值来源(&R): / 数据库连接(&D): / 重置初始值: / 立即重置。

### 优先级 1.5 · 验收方 round-104 实拍发现的两处疑点（**先独立复核，成立就登记 DIFF-64/65 并修**）

来源：`parity/reference/labelshop/probe-63-06-serial-page.png`（round-104 codex 抓的真机「文字属性 → 数据源」页实拍）。

1. **属性页签的顺序与命名不一致 —— 已登记 `DIFF-64`，验收方 round-5 已把 4 类对象的页签条拍全，按表改即可**
   （完整证据与截图清单：`parity/reference/labelshop/PROBE-verifier-object-tabs.md`）：

   | 对象 | 真机页签（左→右） | 复刻版现状 |
   | --- | --- | --- |
   | 文字 | 数据源 / 字体 / 文本 / 常规 | 通用 / 文字 / 字体 / 数据 |
   | 条码 | 数据源 / 条码 / 字体 / 常规（**4 页**） | 通用 / 条码 / 字体 / 数据 / `<码制>`（**5 页**） |
   | 矩形·椭圆·直线 | 图形 / 常规 | 通用 / 方框和圆形（直线为 直线和斜线） |
   | 表格 | 表格 / 常规 | 通用 / 表格 |

   要求：① 按表改 `propertyTabsFor` 的**显示名与顺序**（key 可保留英文）；
   ② **把条码的码制专页合并回「条码」页**——round-6 已把真机「条码」页拍全（`verifier-20c-barcode-page.png`）：
   页首是 `条码符号类型(码制)(B)` 下拉，然后 `尺寸`（`X 尺寸` mil / `码高` 毫米）、`条码特殊选项` 分组
   （**内容随码制变化**，Code 128 下是 `GS1/EAN 128` + `字符集`）、`供人识读字符`（`位置` / `垂直偏移` / `对齐方式` / `字符模板`）、
   页尾 `颜色`；页签数回到 **4**；
   ③ 补**「页签文字与顺序逐项相等」**的 CDP 断言（现在只有存在性断言）；
   ④ 同步 matrix 里涉及页签的 B 章节证据；⑤ 删掉 `propertyTabs.ts` 顶部那句已被实拍证伪的「通用页永远置于首位」注释。
   仍未取证的一类：**图片**属性页（真机未拍；`工具 → 图片(P)` 是菜单第 7 项）。

   **改法（验收方给出目标表与影响面，照此改）**：

   | 对象 | 目标 keys（testid 前缀不变） | 目标 labels（真机原文） |
   | --- | --- | --- |
   | 文字 | `datasource, font, text, general` | 数据源 / 字体 / 文本 / 常规 |
   | 条码 | `datasource, barcode, font, general`（**不再有 `barcodeSpecial` 页签**；码制下拉 + 专属字段进 `barcode` 页的「条码特殊选项」分组） | 数据源 / 条码 / 字体 / 常规 |
   | 矩形·椭圆·直线 | `shape, general` | 图形 / 常规 |
   | 表格 | `table, general` | 表格 / 常规 |
   | 图片 | `image, general` | 图片 / 常规 |
   | RFID | 真机无创建入口（`DIFF-65`）→ 暂不动，按取证再定 | — |

   **影响面（验收方已 grep，必须一起迁移，不许删断言降强度）**：
   - `app/scripts/ui-v109.cjs:263-266` 直接断言**页签文字**：`tabLabels[0] === '通用' && includes('条码') && includes('数据')`
     → 改成真机口径；不改必红。
   - 走 `object-props-tab-barcodeSpecial` 的脚本：`ui-v106.cjs`（**17 处**）、`ui-v125.cjs`、`ui-v126.cjs`、`ui-v127.cjs`、`ui-v56.cjs`；
     单测 `app/scripts/barcode-spec.test.ts`（约 **20 处**）。合并页签后这些导航/断言都要迁移到「条码」页里的分组。
   - 其余脚本点的是 `object-props-tab-<key>`，**key 不变即不受影响**（`ui-v102.cjs:128` 读的是 key 不是标签 ✓；`ui-v71.cjs:132` 是 RFID 页内控件 ✓）。
   - 迁移建议：把合并后的「条码特殊选项」分组容器保留 `data-testid="barcodeSpecial"`（或 `barcode-special-group`）当锚点，
     这样上面那些脚本只需把「点页签」换成「进条码页后滚到该分组」，改动面最小；**但页签数量必须回到 4**，
     并新增断言证明「码制专属字段现在出现在条码页内、且页签数为 4」。矩阵里码制专属页的条目证据同步更新。
2. **「源自其它变量」复选框缺失**：真机「变量共享名称(N):」下拉右侧有一个 **源自其它变量** 复选框
   （勾选后才用共享变量名取别处的数据？语义待取证）；复刻版只有输入框 + datalist，没有这个复选框。
   要求：先真机取证它的勾选/取消对「显示数据/示例」的影响，再按真机实现（或按取证结论登记边界），并补断言。
3. **RFID 工具入口待核对（已登记 `DIFF-65`）**：真机「工具」菜单实测为
   `选取 / 条码 / 文字 / 线条 / 斜线 / 矩形 / 图片 / 数据 / 表格` + 放大缩小适应三项，**没有 RFID**；
   round-6 又把菜单第 8 项 `数据(D)` 建出的对象打开属性，根窗口标题是**「条码属性」**（数据型条码对象，不是 RFID），
   候选入口已排除。复刻版工具栏/菜单有 RFID 入口 → 要求按取证对齐（保留必须有原版入口证据；原版确无则移除或标注等价替代）。

### 优先级 2 · 对象编辑三处真机待确认（round-114 遗留）

见 `parity/reference/labelshop/PROBE-round114.md`：① CTRL+拖动是**复制**还是移动；② 原版拖动有没有**对齐参考线**吸附；③ 点空心矩形**内部**算不算选中。取证手法同优先级 1；结论进 `parity/diffs.md` + 需求清单，行为不一致就对齐实现并补断言。

### 优先级 3 · 两条观察项 + E 区 5 条边界（只取证、写结论，不造功能）

- DIFF-50：真机对象属性「水平/垂直」相对标签边对齐下拉**是灰的**的成因（按对象类型决定可用性只收口了一半）。
- DIFF-60：真机 EAN/UPC 条码页没有「附加条码」「校验字符」、25 码族无独立校验字符，复刻版按原版帮助保留——把「真机实测 vs 帮助原文」的冲突写透，给出保留/删除的理由与证据。
- E 区 5 条：硬件锁激活（E-09）、专业版演示模式（E-10）、三版本分层字段、起始页服务端运营图文（A-271）、内置驱动不支持预览——按 `app/docs/labelshop-compatibility-audit.md` 的单版本策略写「已记录边界 + 理由」，不实现。

### 优先级 4 · 界面清单驱动的逐页并排比对（提升相似度的主线路）

**验收方基线（2026-09-21 09:28 普查）**：工装 `node tools/parity/survey-evidence-coverage.cjs`，报告 `tools/loop/interface-coverage.md`。
矩阵 **605 条**里：有真机证据 **28（4.6%）**、有复刻版证据 **253（41.8%）**、有并排图 **4（0.7%）**、有自动断言 **481（79.5%）**、
**四件套齐 0 条**。注意：真机证据低**主要是没建立链接**（`parity/reference/labelshop` 下已有 **266** 份真机素材、
`parity/review` 有 34 张并排图，但矩阵行基本不引用它们的文件名），所以本项的活是「**按界面建立链接 + 补真缺口**」，
不是从零取证。

做法：以**帮助页为界面主键**（`app/docs/labelshop-help-zh` 共 158 页；矩阵 605 行的「出处文件」列可直接聚合成界面组），
每个界面凑齐四件套 ---- ① 真机图或控件 dump ② 复刻版同界面图 ③ 并排图（`tools/parity/Compare-SideBySide.ps1`）
④ 自动断言（`app/scripts/ui-vNN.cjs` 或 `npm test:*` 用例名）。**每轮做 6–10 个界面**，
并在 `parity/matrix.md` 对应行的证据列**写出这四类文件名**（这样普查脚本就能统计到）。
每轮结束我（验收方）重跑普查脚本，看四件套百分比是否上升——这是本阶段唯一的主指标。

**已有现成清单可用**：`parity/界面清单.md`（验收方 round-104 建的批次 1/1b：主窗口骨架 + 11 个菜单 + 7 个对话框，
每行都写好了 ①真机文件名 ②复刻版文件名 ③现有并排图与其**日期** ④已命中的断言脚本 与「缺口」）。
注意 `parity/review/` 里的并排图都是 **09-14 ~ 09-17 的旧图**，之后跑了 100+ 轮，**必须重抓**才能当证据（新图命名 `cmp-<界面>-<round>.png`）。

顺序：主窗口骨架（菜单 12 项 / 工具栏 / 对齐栏 / 格式栏 / 状态栏）→ 新建与标签格式链路（含 P0）→
对象属性 4 类 × 页签 → 打印链路 → 数据库 / 脚本 → 系统选项 → 起始页 / 关于 / 帮助。

### 优先级 5 · 打印机链路（部分待用户配合）

佳博 GP-1324D 未装官方驱动（无打印队列），USB 直发待用户装驱动后验证；真机残留的 `TSC TSPL-N (203 dpi)` 需用户鼠标移除。你能先做的：对着 `print_printer_cfg_*` 帮助页把「有队列 / 无队列」两条发送路径的分支与错误提示再核一遍，补单测；把「删除已添加打印机」的完整链路（含残留项）与真机行为再对照一次（用户实测过这两处）。

## 四、每轮收尾（硬性）

```powershell
cd app
npm run build            # test:ui 跑 out/ 构建产物：改了 renderer 必须先 build
npm run typecheck
npm test                 # 含 editor / geometry / history / printer / print / render / workspace 等
npm run test:ui          # 79 个脚本，约 50 分钟；同一时刻只允许一套（独占锁，并发造假失败）
cd ..
powershell -File tools\parity\Check-Matrix.ps1
powershell -File tools\parity\Report-Progress.ps1
```

**本机血泪坑**：

- 改 `.ps1`（含用编辑器工具改）会**掉 UTF-8 BOM** → PS 5.1 按 GBK 解析中文报 `Unexpected token '}'`；改完确认首字节是 `EF BB BF`。
- 改 `app/package.json` 等无 BOM 的 UTF-8 文件：必须 `[IO.File]::ReadAllText($p,[Text.UTF8Encoding]::new($false))` 读、同编码写，改完 `node -e "require('./package.json')"` 验证。
- 打包前先 `Get-Process MaxLabel,electron | Stop-Process -Force`；打包脚本的「未检测到代码签名证书」走 stderr 会让退出码变 1，**产物是好的**。
- `parity/evidence` 校验器会把 matrix 证据列里含 `/` 或 `\` 的反引号片段当路径——写证据时避免反引号里放路径。

## 五、出包与推送（有用户可见改动时做）

bump `app/package.json` 版本 → `npm run build` → `node scripts/build-windows.cjs` → 计算 SHA256 写 `RELEASE-SHA256.txt` → 写 `RELEASE-NOTES-vX.Y.Z.md` 与 `tools/loop/logs/vX.Y.Z-gates.md` → `git commit` + `git tag -a vX.Y.Z` + `git push origin main` 与 tag（网络不稳时重试 2-6 次）。
