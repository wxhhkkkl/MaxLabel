# MaxLabel v1.0.21 发布说明

> 本版汇总 **v1.0.20 之后**的**用户可见界面改动**，全部按真机实拍/控件树 dump 对齐 ✓，
> 每处改动都带真机证据与自动断言 ✓（证据与逐条比对见 `parity/验收方逐项比对-施工清单.md` ✓）。
> 门禁：`typecheck` ✓ + 7 套单元门禁 ✓ + 全量 `test:ui`（**91 个脚本**）✓。

## 一、对象属性「常规」页 —— 按真机控件树 dump 重构（DIFF-86）

| 项 | 变化 |
| --- | --- |
| 分组框 | 补齐真机的**四个**：`位置` / `对齐` / `颜色` / `其它` ✓（顺序也一致 ✓） |
| `位置` 组 | 文案改 `水平(&H):` / `垂直(&V):` ✓，数值框右侧各带 `毫米` ✓，**只有两个输入框（不多不少）** ✓ |
| `对齐` 组 | 补 `水平(&W):` / `垂直(&T):` 两个下拉，并按真机在文字对象下**置灰（DISABLED）** ✓ —— 真机是"有控件但禁用"，不是"整组没有" ✓ |
| `其它` 组 | 补 `色彩反相`（该状态真机不显示 → 登记待取证 ✓）、`位置锁定(&L)`、`不打印输出(&N)` ✓；文案补 `旋转(&R):` / `镜像(&M):` / `背景(&B):` ✓ |
| 新增字段 | `对象名称标识：` 与 `图层：` ✓（**全角冒号**，`图层` 按真机置灰 ✓） |
| 底排按钮 | 改为真机顺序 **`确定 / 取消 / 帮助`** ✓（原先 `取消 / 确定` 且缺 `帮助` ✗） |

## 二、加速键（`(&X)`）接线 —— 从"只显示"变成"真能按"

- 真机实测：`Alt+C` 在「选择标签格式」里**确实触发"取消"** ✓（证据 `r189-before/after-alt-c.png`）→ 说明这些括号字母是**可用**加速键，不是标题装饰 ✓。
- 本版把**常用的那批**接上（`accessKey` + `data-access-suffix`）✓：**4 处 → 18 处** ✓，覆盖对象属性常规页与相邻对话框 ✓；
- 渲染仍走 `displayMfcCaption` ✓ —— **屏幕上不会出现 `&` 字面量** ✓（并有静态不变量守着 ✓）。

## 三、标签预览（选择标签格式 / 标签格式设置）—— DIFF-84

| 项 | 变化 |
| --- | --- |
| 每格**序号** | 补 `1…列数×行数`（真机 4×2=8 格带编号 ✓） |
| **尺寸标注** | 补红色 `100mm`（上）/ `70mm`（右）✓ |
| **数值格式** | 毫米字段按真机**两位小数** ✓（`100.00` / `2.00` ✓） |

## 四、其它已收口项（v1.0.20 之后）

- **DIFF-83**：标签里不再把 MFC 加速键标记 `&` 渲染成字面量 ✓（87 处 + 新增静态检查，出现即红 ✓）；
- **DIFF-65**：`工具(T)` 菜单按真机菜单资源改正 —— 移除真机没有的 `RFID` 项 ✓、把 `数据(D)` 提到 `表格(G)` 之前 ✓；
- **DIFF-80/81/82**：选项/账户菜单项集按真机取证收口 ✓（电子称保留 ✓、登录不再静默 ✓）；
- **DIFF-79**：启始页「最新文章」列表按真机样式表逐字照抄 ✓（竖条/间距/日期位置/摘要灰字 ✓）；
- **DIFF-76/77/78**：条码页「尺寸」组与二维码制字段按真机对齐（`X 尺寸` 61 项、层数 89 项、列数 31 项 ✓）+ 新增 `ui-v135/136` 41 条断言 ✓；
- **DIFF-71**：系统设置四页签结构与真机一致 ✓（`常规 / 打印和数据库 / 编辑 / 系统` ✓）。

## 五、已知边界（**如实列出，不装作没有**）

- `色彩反相(&E)`：真机在该对象状态下**不显示**该控件 ✓，且复刻版**全库没有 invert 字段/实现** ✓ → 登记「**待取证**」✓（**不写假实现** ✓）；
- `图层：`：属「原版有但受限」✓（复刻版无图层面板模型 ✓）；
- 打印对话框的「打印机 名称 / 位置」：真机显示真实设备名与端口 ✓，复刻版目前显示占位词 ✗ → **下一版修**（数据现成：`listPrinters()` 给名称、`ports:list` 给端口 ✓）；
- 打印机属性窗口标题：真机是 `<设备名> 属性` ✓，复刻版目前写死 `打印机设置` ✗ → **下一版修**；
- 对象属性「字体 / 文本 / 数据源」三页仍待按 dump 对齐 ✓（依据与逐条清单已备 ✓）。

## 六、门禁与验证

```
npm run typecheck / test:architecture / test:editor / test:geometry / test:history / test:print / test:render / test:workspace   → 全通过 ✓
npm run build → ✓
npm run test:ui（全量）→ ALL SCRIPTS PASSED (91/91) ✓
tools/parity/Check-Matrix.ps1 → 609 条校验通过 ✓
tools/parity/check-evidence-files.cjs → 引用 188 个文件，缺失 0 ✓
```

**验收方独立复验**（不看循环自述 ✓）：`Verify-LabelFormatDialog` **18/18** ✓、`Verify-ObjectTabs` **5/5** ✓、
`Verify-BarcodePage` **5/5** ✓、`Verify-ObjectProps` **10/32**（其余为上述待办 ✓）、打印链路**已通到系统打印对话框** ✓。
