# MaxLabel v1.0.15 发布说明（需求清单「对象属性」全部 56 条取证完成）

**基线**：v1.0.14（2026-09-21）
**本次类型**：需求清单逐条取证 + 条码码制专属字段形态对齐
**日期**：2026-09-21

---

## 一、需求清单新增 38 条结论（累计 **119/194**；对象属性 56/56 全部取证）

| 分组 | 条数 | 结论要点 |
| --- | --- | --- |
| 图片属性（94–99） | 6 | 原版有：文件名 / 缩放方式 / 保持长宽比 / 高度和宽度 / 对齐方式 / 无效图片（帮助 `label_object_page_picture.html`）；复刻版逐项对齐（`ui-v102`、`ui-v109` 第 9–10 步） |
| 文本属性（103–113） | 11 | 原版有：单行（文字停靠、字符模板）、多行（水平对齐 4 项、垂直对齐 3 项、行距 4 项、行宽度）、圆形（回绕方向、文字方向、半径、角度、弧度）；真机文本页逐页读值取证（`probe-text-文本页-*.txt`），复刻版字段齐全 |
| 条码码制专属（128–146） | 18 | 真机把 20 个码制逐个切过去读全量控件（`probe-sym-*-values.txt`）；逐条写明「原版有 / 真机页未见」，并修了四处形态差异（DIFF-61） |
| 汉信码（158–160） | 3 | 纠错级别 4 项 / 字符编码 2 项 / 版本 85 项（复刻版版本列表待补全） |

## 二、对照修复（DIFF-61）

| 项 | 真机读数 | 复刻版修法 |
| --- | --- | --- |
| 供人识读字符 · 位置 | Code 128 等 4 项；**EAN/UPC 族 3 项**（无「条码上方」） | 按码制生成选项 |
| ITF 14 保护框 | **3 项**（无 / 方框 / 保护条） | 复选框 → 3 项下拉（新增 `itf14BearerMode`） |
| ITF 14 粗细 / 空白区 | 各 **15 档** 1X–15X（默认 5X / 10X） | 数值框 → 15 档下拉 |
| PDF 417 条宽比 | **9 档** 1 X…9 X（默认 3 X） | 按码制给 9 档 |
| QR / Data Matrix / Micro QR 符号版本 | 41 / 31 / 5 项（含「自动」） | 新增「符号版本」下拉并透传 `version` |
| Micro QR 纠错级别 | 3 项（L/M/Q） | 新增纠错级别 + 字符编码 + 符号版本 |

> **DIFF-60（观察项）**：真机 EAN/UPC 条码页没有「附加条码」、25 码族没有独立「校验字符」下拉，
> 而帮助里有这两组说明；复刻版按帮助保留，已在结论里注明「帮助有 / 真机页未见」，暂不改。

## 三、验证

| 门禁 | 结果 |
| --- | --- |
| `ui-v126.cjs`（新增，条码码制专属字段） | **10/10** |
| 全量 UI 回归（77 个脚本） | **ALL SCRIPTS PASSED (77/77)**，exit 0 |
| `test:barcode` | 通过（20 种码制与真机下拉逐项一致） |
| `test:render` / `test:print` / `test:editor` / `test:geometry` / `test:history` / `test:color` / `test:printer` / `test:workspace` / `typecheck` | 全绿 |
| `Check-Matrix.ps1` / `test:evidence` | 605/605；证据校验通过 |
| 未收口差异 | 3 条：DIFF-57（缩减量字段）、DIFF-60（观察项）、DIFF-61 的汉信码版本列表 |

## 四、产物

| 产物 | 路径 |
| --- | --- |
| 安装包 | `app/release/MaxLabel-Setup-1.0.15.exe` |
| 校验和 | `RELEASE-SHA256.txt` |
| 清单队列 | `parity/需求清单-待验证队列.md`（119/194 已填） |

## 五、清单进度与下一步

- 已填 **119/194**：标签格式 13 + 打印机属性 6 + 系统选项 6 + 对象编辑 15 + 打印和预览 15 + 其它 8 + **对象属性 56**；
- 余 **75 条**：数据源 55 / 数据库 19 / 授权 1。
- 下一轮：**数据源 55 + 数据库 19 + 授权 1**（以原版帮助 `label_object_page_data*` / `database_*` + 复刻版数据源编辑器断言为主证据），
  顺带补 DIFF-57 与汉信码版本列表。
